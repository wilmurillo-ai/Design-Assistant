# memorial-skill 依赖

# 必需
# （纯标准库，无强制依赖）

# 可选：中文名转 slug（pypinyin）
# pip install pypinyin
pypinyin

# 可选：照片 EXIF 读取（photo_analyzer.py）
# pip install Pillow
Pillow

# 可选：音频转录（audio_transcriber.py）
# pip install openai-whisper
openai-whisper

# 可选：音频预处理（voice_preprocessor.py）
# pip install pilk noisereduce soundfile
pilk
noisereduce
soundfile

# 可选：声音训练/合成（voice_trainer.py + voice_synthesizer.py）
# GPT-SoVITS 需独立安装，见 voice_trainer.py --action setup
# CosyVoice 需独立安装，见 https://github.com/FunAudioLLM/CosyVoice
