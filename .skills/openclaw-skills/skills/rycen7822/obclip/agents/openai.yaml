interface:
  display_name: "Obclip"
  short_description: "Install and run obclip for reliable page clipping"
  default_prompt: "Use $obclip to install or run the obclip CLI and capture a target page into a note."
policy:
  allow_implicit_invocation: true
