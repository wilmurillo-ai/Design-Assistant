Cloud Code 源码泄露，目前公认最强的编码 Agent。核心竞争力不在模型，而在模型周围那套精密的控制系统。核心骨架是一个 While 循环加一组工具，叫 Query Engine，用户输入一句话，构建提示词，调用模型，执行工具，循环直到输出结果。第一层是上下文管理，三级压缩流水线：微压缩保留最近结果，记忆压缩提取结构化事实，完整压缩总结对话。第二层是安全约束，五步评估加双层分类器，规则是法律不是建议。第三层是多 Agent 协作，Leader 生成 Teammate，上下文隔离但关键发现同步。模型是引擎，Harness 是整辆车，引擎一样的情况下，谁的车造得好，谁就跑得快。
