# OpenSpec OPSX Configuration
# Generated by workspace-init skill

schema: spec-driven

context: |
  ## Workspace: {project_name}

  {description}

  ### Repositories
{repos_list}

rules:
  proposal:
    - Reference recent 2-3 similar changes in `openspec/changes/archive/`
    - Include rollback plan when applicable

  design:
    - Reference recent changes' design.md structure
    - Must include Context, Goals/Non-Goals, Decisions, Risks
    - Simple changes (single-file, no design decisions) may skip design

  tasks:
    - Reference recent changes' tasks.md structure
    - Small changes: single PR; large changes: one PR per phase
    - Each phase should have a clear gate

  verification:
    automated: []
    manual: []
