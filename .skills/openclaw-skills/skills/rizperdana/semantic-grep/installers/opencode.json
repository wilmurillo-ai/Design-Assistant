{
  "name": "semgrepll",
  "description": "Local semantic code search",
  "install": "pip install semgrepll",
  "env": {
    "EMBED_BACKEND": "auto"
  },
  "commands": {
    "index": "semgrep index",
    "search": "semgrep search"
  }
}
