# API and Data（接口与数据层）

设计请求封装、错误与 Loading/空态、缓存与数据流。

## What It Does

- 请求封装：基地址、鉴权、取消、重试
- 错误分层与 UI 展示、Loading 与空态/失败态
- 缓存与更新、数据流选型（React Query/SWR 等）

## How to Use

当用户提到「接口封装」「请求」「错误处理」「loading」「空态」「缓存」「数据流」时启用。

## Requirements

- 无额外依赖；按项目栈推荐 axios/fetch、React Query/SWR 等
