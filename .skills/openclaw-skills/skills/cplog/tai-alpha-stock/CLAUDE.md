# Claude Code — Tai Alpha Stock

Read the full hub: [setup/docs/core/AGENTS.md](setup/docs/core/AGENTS.md)

Same repo rules as other agents: Markdown/SQL in `/setup` (except `SKILL.md`, `AGENTS.md`, `CLAUDE.md` at root); use conda env `llm_base` when running Python; SQLite runs table stores collect/score/persona metadata.
