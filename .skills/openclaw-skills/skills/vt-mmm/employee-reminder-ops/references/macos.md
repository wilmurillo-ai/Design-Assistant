# Employee Reminder Ops on macOS

## Hỗ trợ hiện tại
- Plan A reminder workflow: supported
- Google Sheets reading: supported
- current scheduler example in workspace uses `launchd`

## Runtime
- Node.js
- gog CLI

## Scheduler
- use `launchd`
- example plist already bundled in scripts

## Notes
macOS là môi trường hiện tại đã test Plan A nhiều nhất.
