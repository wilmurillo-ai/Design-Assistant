# Error Catalog

One entry per novel error. Consult before retrying a previously-failed approach.

## Failure buckets (pick one)

`missing dependency` · `incompatible versions` · `PATH / shell config` · `Anchor/Solana version mismatch` · `declare_id! / Anchor.toml / program-keypair drift` · `Rust crate conflict / MSRV` · `Cargo.lock version drift` · `cargo-build-sbf toolchain` · `Node package issue` · `lockfile / package-manager mix` · `keypair or signer` · `insufficient SOL` · `RPC / network` · `public-RPC deploy failure` · `wrong cluster` · `wrong program ID` · `IDL mismatch` · `PDA / seed mismatch` · `--max-len too small` · `priority-fee too low` · `workspace config` · `test validator` · `.anchor / target cache` · `permissions / filesystem` · `upgrade-authority mismatch` · `buffer-authority mismatch`

---

## Entry Template

- **Date (UTC):**
- **Project:**
- **Cluster:**
- **Failure bucket:**
- **Raw error (first and last lines, redact secrets):**
- **Commands that triggered it:**
- **Toolchain at time of failure:**
  - Rust / Solana / Anchor / Node / pnpm:
- **Suspected root cause:**
- **Confirmed root cause:**
- **Fix attempted:**
- **Did the fix work?** yes / no / partial
- **Time to diagnose:**
- **Safer future pattern / prevention:**
- **Related entries in this file:**

---
