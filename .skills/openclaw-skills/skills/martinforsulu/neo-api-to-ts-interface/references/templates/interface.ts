/**
 * TypeScript Interface Template
 * Placeholders:
 *   {{name}} - Interface name
 *   {{extends}} - Optional extends clause (including " extends " keyword)
 *   {{fields}} - Field definitions (with proper indentation)
 *   {{description}} - Optional interface description
 */

{{description}}
export interface {{name}}{{extends}} {
{{fields}}
}
