Executable scripts for deterministic tasks.

- `niche_fit_score.py` — Scores a candidate category on five community-fit dimensions (0–10 each, weighted to 0–100). Run with `--professional`, `--scarcity`, `--identity`, `--repeat`, `--supply`.
