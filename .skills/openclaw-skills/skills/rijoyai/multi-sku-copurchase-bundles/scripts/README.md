# Scripts

No required scripts — association metrics can be computed in-sheet or described in prose. Add Python here for basket analysis (e.g. pairwise lift from CSV) if needed; document when to run in SKILL.md.
