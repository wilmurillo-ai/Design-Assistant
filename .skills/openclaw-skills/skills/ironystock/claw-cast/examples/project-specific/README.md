# Project-specific examples

These files are optional references from prior project work.

- They are **not required** for ClawCast baseline setup.
- They are intentionally kept out of `assets/overlays/` so the default skill package stays generic.
- Use them only as inspiration when building your own branded overlays.
