interface:
  display_name: "Lance"
  short_description: "High-signal Web3 vulnerability triage"
  default_prompt: "Use $lance to review this Solidity/Move codebase for reportable vulnerabilities only."

policy:
  allow_implicit_invocation: true