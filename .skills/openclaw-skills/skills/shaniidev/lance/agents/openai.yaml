interface:
  display_name: "Lance"
  short_description: "Evidence-first Web3 vulnerability hunter"
  default_prompt: "Use $lance to audit this protocol and output only triage-ready exploitable findings."

policy:
  allow_implicit_invocation: true