[package]
name = "simple_counter"
version = "0.1.0"
description = "A minimal Solana Anchor program for audit demonstration"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "simple_counter"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.30.1"
