# STT Notes

## Defaults

- model: `nova-2`
- language: `zh`
- punctuation: enabled
- smart formatting: enabled

## Outputs

For output path `X`:

- transcript text: `X`
- raw API JSON: `X.json`

## Caveat

The script defaults to `audio/ogg`. For other containers, pass `--content-type`.
