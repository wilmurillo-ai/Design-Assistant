## 1.0.1

- Fix code examples to use `);` and `;` for copy/paste reliability.
- Normalize OpenClaw `metadata` frontmatter to YAML mapping.
- Clarify that `/{network}/xfollowers` also supports X community URLs (`https://x.com/i/communities/<id>`).

## 1.0.0

- Initial release.

