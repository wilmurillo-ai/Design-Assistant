interface:
  display_name: "UAPI Adcode 国内外行政区域查询 接口"
  short_description: "使用 UAPI 的Adcode 国内外行政区域查询接口，并说明参数、调用方式和返回码"
  default_prompt: "使用 $uapi-get-misc-district 直接调用 GET /misc/district，并先确认必填参数、默认值和生效条件。"
policy:
  allow_implicit_invocation: true
