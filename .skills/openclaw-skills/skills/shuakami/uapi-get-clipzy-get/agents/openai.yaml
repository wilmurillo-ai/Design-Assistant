interface:
  display_name: "UAPI 步骤2 (方法一): 获取加密数据 接口"
  short_description: "使用 UAPI 的步骤2 (方法一): 获取加密数据接口，并说明参数、调用方式和返回码"
  default_prompt: "使用 $uapi-get-clipzy-get 直接调用 GET /api/get，并先确认必填参数、默认值和生效条件。"
policy:
  allow_implicit_invocation: true
