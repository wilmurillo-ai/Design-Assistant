interface:
  display_name: "UAPI 程序员历史事件 接口"
  short_description: "使用 UAPI 的程序员历史事件接口，并说明参数、调用方式和返回码"
  default_prompt: "使用 $uapi-get-history-programmer 直接调用 GET /history/programmer，并先确认必填参数、默认值和生效条件。"
policy:
  allow_implicit_invocation: true
