interface:
  display_name: "UAPI 每日新闻图 接口"
  short_description: "使用 UAPI 的每日新闻图接口，并说明参数、调用方式和返回码"
  default_prompt: "使用 $uapi-get-daily-news-image 直接调用 GET /daily/news-image，并先确认必填参数、默认值和生效条件。"
policy:
  allow_implicit_invocation: true
