interface:
  display_name: "UAPI Epic 免费游戏 接口"
  short_description: "使用 UAPI 的Epic 免费游戏接口，并说明参数、调用方式和返回码"
  default_prompt: "使用 $uapi-get-game-epic-free 直接调用 GET /game/epic-free，并先确认必填参数、默认值和生效条件。"
policy:
  allow_implicit_invocation: true
