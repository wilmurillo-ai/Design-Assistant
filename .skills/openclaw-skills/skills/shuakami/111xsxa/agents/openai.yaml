interface:
  display_name: "UAPI AI翻译配置 接口"
  short_description: "使用 UAPI 的AI翻译配置接口，并说明参数、调用方式和返回码"
  default_prompt: "使用 $uapi-get-ai-translate-languages 直接调用 GET /ai/translate/languages，并先确认必填参数、默认值和生效条件。"
policy:
  allow_implicit_invocation: true
