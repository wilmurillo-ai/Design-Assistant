interface:
  display_name: "UAPI 必应壁纸 接口"
  short_description: "使用 UAPI 的必应壁纸接口，并说明参数、调用方式和返回码"
  default_prompt: "使用 $uapi-get-image-bing-daily 直接调用 GET /image/bing-daily，并先确认必填参数、默认值和生效条件。"
policy:
  allow_implicit_invocation: true
