interface:
  display_name: "UAPI 获取Gravatar头像 接口"
  short_description: "使用 UAPI 的获取Gravatar头像接口，并说明参数、调用方式和返回码"
  default_prompt: "使用 $uapi-get-avatar-gravatar 直接调用 GET /avatar/gravatar，并先确认必填参数、默认值和生效条件。"
policy:
  allow_implicit_invocation: true
