#!/bin/bash

# This script is intentionally left blank.
# The AudioMind v3 skill uses a remote backend and does not require a local server.
# See SKILL.md for details on the new workflow.

echo "AudioMind v3 does not require a local server. Please follow the workflow described in SKILL.md."
exit 0
