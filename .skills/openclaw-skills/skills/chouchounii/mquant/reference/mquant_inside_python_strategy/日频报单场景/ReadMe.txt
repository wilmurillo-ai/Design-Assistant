场景说明：
该策略针对的场景是一些在第三方平台（如聚宽、米筐等）做投研的客户，如果需要做低频交易，不需要更改自己的投研策略，只需要将投研策略产生的交易信号写入指定文件，MQuant策略负责从文件读取报单信息报单。

优点：
1、MQuant策略可以在报单文件生成前的任意时刻启动，使用简便
2、支持定时撤补单，支持资金持仓检查

限制：
一个策略仅允许监控一个文件

使用说明：
（1）请将testdata.csv放在D:\\TestData目录下
（2）将“OrderMgr.py”和“日频报单场景.py”导入自己的策略目录
（3）运行“日频报单场景.py”

