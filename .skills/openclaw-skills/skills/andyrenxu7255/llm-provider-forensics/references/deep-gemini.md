# Gemini Native Deep Tests

Use when native Gemini-compatible route works.

## Deep probes
- long context recall through `generateContent`
- strict JSON-only output
- refusal style on edge-case unsafe prompt
- repeated variance on same free-form prompt
- malformed body error schema check

## Native clues
- `candidates/content/parts` structure stays coherent
- Gemini usage metadata appears consistently
- key-based auth and versioned endpoints behave as expected
