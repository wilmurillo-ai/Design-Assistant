# GLM / Zhipu Family Fingerprint

Stronger GLM-like signals:
- route claims `glm-*` models and behaves consistently across repeated runs
- vendor docs or endpoint hints point to Zhipu/GLM lineage
- Chinese-first deployment context and naming are coherent

Wrapper suspicion:
- only generic OpenAI-compatible behavior is visible
- catalog is highly mixed and GLM ids are just a few among many unrelated families
- no vendor-native hints beyond the model name
