# MiniMax Family Fingerprint

Stronger MiniMax-like signals:
- model ids include `minimax`
- provider catalog or metadata consistently points to MiniMax
- repeated outputs are coherent and the route looks focused rather than massively mixed

Wrapper suspicion:
- MiniMax id is exposed only through a broad aggregation pool
- no MiniMax-specific evidence beyond name matching
- token/accounting/schema are entirely gateway-generic
