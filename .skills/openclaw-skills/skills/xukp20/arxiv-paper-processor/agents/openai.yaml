interface:
  display_name: "ArXiv Paper Processor"
  short_description: "Download paper artifacts (single/batch) and write manual summary.md"
  default_prompt: "Process paper directories: batch-download artifacts if needed, then manually read source/PDF and write summary.md."
