interface:
  display_name: "Free Quota Image Skill"
  short_description: "Generate images via free-quota-first routing"
  default_prompt: "Use $free-quota-image-skill to generate images with provider fallback, token pooling, and prompt optimization."
