## Logic Check

- Read `./skills/logic/heartbeat-rules.md`
- Use `~/logic/heartbeat-state.md` for last-run markers and action notes
- If no file inside `~/logic/` changed since the last reviewed change, return `HEARTBEAT_OK`
