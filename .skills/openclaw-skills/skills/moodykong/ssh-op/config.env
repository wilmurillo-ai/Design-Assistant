# ssh-op config (machine-specific)
# Keep this file alongside the skill.

# 1Password location of the SSH private key
SSH_OP_VAULT_NAME=""
SSH_OP_ITEM_TITLE=""
SSH_OP_KEY_FIELD="private key"

# Optional: if set, ssh-op will skip re-loading when this fingerprint is already in ssh-agent
# Example format (from `ssh-add -l`): SHA256:abc123...
SSH_OP_KEY_FINGERPRINT_SHA256=""

# Optional: ssh config snippet file to manage
SSH_OP_HOSTS_FILE="hosts.conf"
