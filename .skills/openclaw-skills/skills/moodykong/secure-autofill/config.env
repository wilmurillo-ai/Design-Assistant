# secure-autofill (machine-specific env template)
#
# Copy the relevant variables into the gateway env file (commonly: ~/.config/openclaw/env)
# and restart the gateway so it picks them up.

# Non-headless browser display settings for WSLg / GUI Chrome
DISPLAY=":0"
WAYLAND_DISPLAY="wayland-0"

# 1Password service account token (DO NOT COMMIT)
# OP_SERVICE_ACCOUNT_TOKEN="ops_..."
