[project]
name = "libvips-image-skill"
version = "1.0.0"
description = "High-performance image processing with libvips"
readme = "SKILL.md"
requires-python = ">=3.9"
dependencies = [
    "pyvips>=2.2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
]

[tool.uv]
dev-dependencies = []
