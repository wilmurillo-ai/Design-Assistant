# AIGC Image Generator Config

## API Configuration
BASE_URL=http://localhost:8082
CLIENT_ID=openclaw-kinggu
PROVIDER=4

## ⚠️ 重要：环境变量（生产环境必须设置）
# AIGC_OSS_TOKEN - 用户 OSS JWT Token（从 config.local.json 或 shell 环境读取，不要硬编码）
# 建议在 ~/.openclaw/workspace/scripts/set_aigc_env.sh 中设置：
#   export AIGC_OSS_TOKEN="your_token_here"

## Default Settings
DEFAULT_ASPECT_RATIO=1
DEFAULT_BATCH_SIZE=1
DEFAULT_TIMEOUT=120

## Aspect Ratio Options
# 1 = 1:1 正方形
# 2 = 3:4 竖屏
# 3 = 4:3 横屏
# 4 = 9:16 手机竖屏
# 5 = 16:9 宽屏
