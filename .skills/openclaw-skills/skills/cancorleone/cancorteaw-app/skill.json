{
  "name": "cancorteaw-app",
  "description": "Local Expo/React Native app builder (controlled runner).",
  "entrypoint": {
    "type": "shell",
    "command": "/home/patron/apps/_bin/appctl",
    "args": [
      "{{args}}"
    ]
  },
  "safety": {
    "notes": "Runs only the allowlisted runner script appctl under /home/patron/apps."
  },
  "version": "0.1.1"
}
