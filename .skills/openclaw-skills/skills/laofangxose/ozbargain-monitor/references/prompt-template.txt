在 <TIME> 检查 OzBargain 最新 deals，按以下优先级筛选：
<TOPIC_PRIORITY_LIST>

去重规则（必须执行）：
- 先读取 <DEDUPE_STATE_FILE>（若不存在则创建，初始为 {"sent": []}）。
- 仅发送未出现过的新 deal 链接（按链接去重，优先使用 ozbargain node/goto 链接）。
- 已在 sent 中的链接不要再次发送。
- 本次发送完成后，把本次已发送链接追加写回 sent（去重后保存）。

输出语言：<LANGUAGE>
输出格式：仅保留最实惠的 <MIN_MAX_COUNT> 条；每条包含：标题、价格/优惠、为什么值得买、直达链接。
并读取每条贴子的评论，补充“评论补充”字段（可空）：
- 是否有过期/失效迹象（有则标记“可能过期”）
- 是否有更优叠加省钱方法（如返现+信用卡offer+优惠码）
- 是否有关键限制条件（新用户、地区、门店、时间窗、排除品牌等）

质量要求：宁缺毋滥；若当天没有真正有价值的好价，明确写“<NO_DEAL_LINE>”，不要凑数。
避免纯广告和低价值噪音。
