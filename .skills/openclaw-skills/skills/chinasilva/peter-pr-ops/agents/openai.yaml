interface:
  display_name: "peter-pr-ops"
  short_description: "PR 收口自动化；命令：merge-pr、batch-pr、auto-merge"
  default_prompt: "Use $peter-pr-ops to merge-pr or batch-pr, then enable auto-merge when checks pass."
