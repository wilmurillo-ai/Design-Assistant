interface:
  display_name: "peter-commit-ops"
  short_description: "提交与建 PR 桥接；命令：commit-ready、push-branch、create-pr"
  default_prompt: "Use $peter-commit-ops to do commit-ready, push-branch, and create-pr for current changes."
