interface:
  display_name: "peter-ci-gate"
  short_description: "CI 绿灯门禁；命令：ci-status、ci-diagnose、ci-rerun-once"
  default_prompt: "Use $peter-ci-gate to run ci-status, then ci-diagnose and ci-rerun-once if flaky."
