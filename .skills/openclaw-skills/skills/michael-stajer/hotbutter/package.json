{
  "name": "@hotbutter/voice-skill-hotbutter",
  "version": "0.1.0",
  "private": true,
  "description": "OpenClaw skill — voice chat via HOSTED hotbutter.ai relay. Voice transcripts and agent responses transit through hotbutter.ai. Executes the local 'openclaw' CLI binary to process each agent turn. For a fully local/private setup, use hotbutter-os instead.",
  "bin": {
    "voice-bridge": "./bin/voice-bridge.js"
  },
  "scripts": {
    "start": "node bin/voice-bridge.js start"
  },
  "requiredBinaries": ["openclaw"],
  "openclaw": {
    "skill": true,
    "requires": {
      "binaries": ["openclaw"],
      "description": "Runs 'openclaw agent --session-id <id> -m <text>' for each voice message to get agent responses"
    }
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
