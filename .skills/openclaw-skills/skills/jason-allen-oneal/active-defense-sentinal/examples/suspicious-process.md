# Example: Suspicious Process

A new privileged process or listener appears without a clear reason.

Expected behavior:
- note process tree and parentage
- record ports and paths
- avoid killing it until approved
- recommend containment if the risk is high