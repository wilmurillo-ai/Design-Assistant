# Example: Session Overflow

The active OpenClaw or Hermes thread becomes bloated and starts failing or stalling.

Expected behavior:
- verify the active session
- collect overflow evidence
- recommend a reset or fresh thread
- avoid continuing the poisoned session