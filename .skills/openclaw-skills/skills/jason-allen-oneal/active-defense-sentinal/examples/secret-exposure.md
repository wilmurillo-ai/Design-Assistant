# Example: Secret Exposure

A command, log, or page exposes a token, key, or credential.

Expected behavior:
- stop sharing the secret
- avoid echoing it back
- preserve evidence safely
- recommend rotation and cleanup