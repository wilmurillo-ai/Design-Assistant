# 版本快照 · {产物类型} v{N} · {YYYY-MM-DD}

> 本快照记录 {产物类型} v{N} 版本的完整状态。

## 快照元数据

| 项目 | 内容 |
|------|------|
| **版本** | v{N} |
| **快照时间** | {YYYY-MM-DD HH:MM:SS} |
| **快照原因** | {Phase N 完成 / 审核前 / 回退前 / 最终交付} |
| **快照根目录** | `__snapshots/v{N}_{YYYYMMDD}/` |
| **对应任务ID** | |

## 本版本概述

（一句话说明本版本的主要变化）

## 主要变化

| 变更类型 | 变更位置 | 变更内容 | 变更原因 |
|---------|---------|---------|---------|
| | | | |

## 包含文件

| 文件路径 | 类型 | 大小 | 说明 |
|---------|------|------|------|
| | | | |

## 审核状态

| 审核轮次 | 审核结果 | 主要问题 |
|---------|---------|---------|
| | | |

## 回退指南

（如需回退到此版本，请执行以下步骤）

1. 从 `__snapshots/v{N}_{YYYYMMDD}/` 恢复文件到工作目录
2. 更新 PROJECT_STATE.yaml 中的 `current_version`
3. 在 DECISION_LOG 中记录回退原因
4. 通知相关 Agent
