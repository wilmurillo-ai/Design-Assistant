name: pcap_analyzer
description: Analyze a PCAP file and return a structured network summary.
type: command

command:
  run: /home/tom/openclaw-tools/pcap_summary.sh {{pcap_path}}

inputs:
  - name: pcap_path
    description: Full path to the PCAP file
    required: true

output:
  format: text
