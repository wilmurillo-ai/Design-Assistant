{
  "id": "dcg-guard",
  "name": "DCG Guard",
  "description": "Blocks dangerous shell commands via DCG (Dangerous Command Guard) before execution. Zero noise on safe commands, hard block on destructive ones.",
  "version": "1.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": { "type": "boolean" },
      "dcgBin": { "type": "string", "description": "Path to DCG binary" }
    }
  }
}
