provider_id: moomoo_opend
provider_name: Moomoo OpenD

connection:
  host: 127.0.0.1
  port: 11111
  python_bin: /path/to/python

market_defaults:
  timeframe: daily
  adjustment_mode: qfq
  max_stale_days: 7

notes: Example provider config only. Keep real local values in workspace deployment config.
