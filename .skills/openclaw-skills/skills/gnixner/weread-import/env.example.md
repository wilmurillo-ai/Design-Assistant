# 手动 cookie（从浏览器开发者工具中复制）
WEREAD_COOKIE=

# 导出模式：api
WEREAD_IMPORT_MODE=api

# Chrome 远程调试地址（用于浏览器 cookie 提取）
WEREAD_CDP_URL=http://127.0.0.1:9222

# 输出目录
WEREAD_OUTPUT=

# Frontmatter tags（逗号分隔）
WEREAD_TAGS=reading,weread

# 自定义 User-Agent（可选，默认 Chrome 146）
WEREAD_USER_AGENT=
