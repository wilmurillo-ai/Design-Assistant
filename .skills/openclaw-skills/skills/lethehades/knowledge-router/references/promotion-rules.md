# promotion rules

Emit a promotion hint when one of these seems true:
- the same workflow appears repeatedly in daily memory
- a durable rule still lives only in daily memory
- a repeated project method would be more reusable as a skill reference
- audit/evidence repeatedly supports the same workaround but no reusable method note exists yet

Do not auto-promote content in the first version. Only suggest likely destinations.
