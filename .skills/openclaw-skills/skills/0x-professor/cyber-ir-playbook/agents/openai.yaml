interface:
  display_name: "Cyber IR Playbook"
  short_description: "Build incident response timelines and reports"
  default_prompt: "Use $cyber-ir-playbook to convert incident events into a structured IR report."
