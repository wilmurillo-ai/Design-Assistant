interface:
  display_name: "DL Transformer Finetune"
  short_description: "Generate transformer fine-tuning run plans"
  default_prompt: "Use $dl-transformer-finetune to produce a reproducible transformer fine-tuning plan."
