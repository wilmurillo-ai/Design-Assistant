# Changelog

## 1.0.2

- Remove whitelist requirement; protocol is now permissionless

## 1.0.1

- Add `requires.env: ["BACKEND_URL"]` for credential transparency
- Add homepage to repo for provenance
- Add Security & Signing section (external signer, no raw keys, user confirmation)
- Fix whitelist contact email

## 1.0.0

- Initial release
- Agent workflow: list, fund, get key, use Venice API
- Provider workflow: list, store key, settle
- DiemAgent / DiemProvider SDK usage
- Backend API and contract reference (Base mainnet)
