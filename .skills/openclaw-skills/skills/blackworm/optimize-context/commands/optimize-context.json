{
  "name": "/optimize-context",
  "description": "Optimizes conversation context by summarizing important information and cleaning up old messages to prevent context overflow and save tokens",
  "handler": "optimize-context.js",
  "permissions": ["user"],
  "category": "utility",
  "usage": "/optimize-context",
  "examples": [
    "/optimize-context"
  ],
  "aliases": [
    "/context-optimize",
    "/opt-context"
  ]
}