{
  "name": "safe-multisig-skill",
  "version": "2.1.0",
  "description": "OpenClaw skill for operating Safe multisig wallets — propose, confirm, and execute transactions across multiple chains",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit --strict",
    "test": "vitest run",
    "test:smoke": "bash tests/smoke.sh"
  },
  "dependencies": {
    "@safe-global/api-kit": "^4.0.1",
    "@safe-global/protocol-kit": "^6.1.2",
    "commander": "^14.0.3",
    "ethers": "^6.16.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^4.0.0"
  }
}
