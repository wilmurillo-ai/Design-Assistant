# Legacy Scripts (v1)

These scripts were the original implementation before the two-layer architecture.

**Not recommended for new evaluations.** Use the agent-driven workflows in `USAGE.md` instead.

Kept for reference only.
