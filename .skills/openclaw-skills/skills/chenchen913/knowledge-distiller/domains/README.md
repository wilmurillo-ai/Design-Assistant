# domains/

此目录用于存放**领域档案文件**，由 Knowledge Distiller 在运行时自动创建和更新。

- 每个领域对应一个 `.md` 文件，文件名即领域名（如 `产品设计.md`、`AI.md`）
- 文件内容遵循 `@reference/domain_template.md` 的格式
- **请勿手动删除此目录**，否则跨对话的知识积累将无法持久化

> 首次使用时此目录为空，属于正常状态。第一次对话结束后会自动生成对应的领域档案。
