{
  "name": "bocha_web_search",
  "description": "Search the web using Bocha AI Search API. Optimized for Chinese content and queries. Requires BOCHA_API_KEY environment variable.",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "The search query string (supports Chinese and English)"
      },
      "count": {
        "type": "number",
        "description": "Number of results to return (default: 10, max: 50)",
        "minimum": 1,
        "maximum": 50,
        "default": 10
      },
      "freshness": {
        "type": "string",
        "description": "Time filter for results",
        "enum": ["oneDay", "oneWeek", "oneMonth", "oneYear", "noLimit"],
        "default": "noLimit"
      }
    },
    "required": ["query"]
  },
  "command": "node ${baseDir}/scripts/bocha_search.js",
  "env": {
    "BOCHA_API_KEY": "${BOCHA_API_KEY}"
  }
}