[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "smart-eye"
version = "0.1.0"
description = "Camera PTZ control skill for OpenClaw"
readme = "SKILL.md"
requires-python = ">=3.10"
dependencies = ["opencv-python>=4.8.0"]

[tool.hatch.build.targets.wheel]
packages = ["src"]
