# Blade Spirits macro example

Stored as markdown for ClawHub publishing compatibility. Copy the serialized lines below into a local file with a `.macro` extension before testing in Razor.

```text
Assistant.Macros.MacroCastSpellAction|33
Assistant.Macros.WaitForTargetAction|30
Assistant.Macros.TargetRelLocAction|3|1
```
