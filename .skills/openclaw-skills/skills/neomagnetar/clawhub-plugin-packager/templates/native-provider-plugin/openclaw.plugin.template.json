{
  "id": "{{PLUGIN_ID}}",
  "name": "{{DISPLAY_NAME}}",
  "description": "{{SUMMARY}}",
  "configSchema": {
    "type": "object",
    "properties": {
      "apiBase": {
        "type": "string",
        "description": "Optional API base URL override."
      }
    },
    "additionalProperties": false
  }
}
