[project]
name = "calorie-lookup"
version = "0.3.0"
description = "Spoonacular nutrition lookup with search optimization, cooking modifiers, and multimodal image recognition — agent skill"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
]

[project.urls]
Repository = "https://github.com/zengxuanliu2005/calorie-lookup"
