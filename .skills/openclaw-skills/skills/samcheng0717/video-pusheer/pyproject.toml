[project]
name = "video-pusher-skills"
version = "0.1.0"
description = "Multi-platform video/content publishing skills for Claude Code"
requires-python = ">=3.8"
dependencies = [
    "playwright>=1.40",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
