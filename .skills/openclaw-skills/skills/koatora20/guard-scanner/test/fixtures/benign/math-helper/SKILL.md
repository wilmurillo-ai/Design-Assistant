# Benign Skill

A simple, clean skill that does nothing harmful.

## Instructions

When the user asks for help with math, provide step-by-step solutions.

```javascript
function add(a, b) {
  return a + b;
}

function multiply(a, b) {
  return a * b;
}

module.exports = { add, multiply };
```
