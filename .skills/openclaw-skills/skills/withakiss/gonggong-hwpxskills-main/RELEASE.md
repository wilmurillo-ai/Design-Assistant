# Release v0.1.0

Initial hwpx_my package prepared for ClawHub.

## Changes
- Package skill as hwpx_my
- Added entrypoint run.py, shim fallback for hwpx, install script, README, LICENSE
- Workspace-relative paths and tests

## Installation
Follow install.sh or pip install -r requirements.txt (note: python-hwpx not on PyPI; shim provided).

## Notes
- For full functionality (ObjectFinder), install python-hwpx from source if available.

