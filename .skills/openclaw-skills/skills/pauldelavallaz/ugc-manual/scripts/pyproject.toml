[project]
name = "ugc-manual"
version = "1.0.0"
description = "Generate lip-sync videos from image + audio using ComfyDeploy"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28.0",
]

[tool.uv]
dev-dependencies = []
