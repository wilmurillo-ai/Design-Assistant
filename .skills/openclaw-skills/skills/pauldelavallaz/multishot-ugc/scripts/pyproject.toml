[project]
name = "multishot-ugc"
version = "1.0.0"
description = "Generate 10 perspective variations of an image for UGC video production"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28.0",
]

[tool.uv]
dev-dependencies = []
