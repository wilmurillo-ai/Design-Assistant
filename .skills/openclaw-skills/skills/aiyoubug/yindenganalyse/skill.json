{
  "name": "yindeng_crawler",
  "version": "1.0.0",
  "description": "Crawl Yindeng announcements and results, optionally analyzing them with LLM.",
  "entry_point": "skill.py:run_yindeng_crawler",
  "parameters": {
    "type": "object",
    "properties": {
      "date": {
        "type": "string",
        "description": "Start date in YYYY-MM-DD format. Defaults to auto-calculation based on weekday.",
        "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
      },
      "source": {
        "type": "string",
        "enum": ["result", "announcement", "all"],
        "default": "all",
        "description": "The source type to crawl."
      },
      "analyze": {
        "type": "boolean",
        "default": false,
        "description": "Whether to perform LLM analysis on downloaded documents."
      }
    },
    "required": []
  }
}
