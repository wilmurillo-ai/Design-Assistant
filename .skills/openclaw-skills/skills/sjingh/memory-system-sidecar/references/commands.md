# Commands

Use these from the repo root (`/Users/shaojian/memorylab`).

## Refresh end-to-end

```bash
./skills/memory-system-sidecar/scripts/refresh_memory_system.sh
```

Underlying flow:
- `./scripts/context-refresh-session --session-key agent:main:main`

## Verify end-to-end

```bash
./skills/memory-system-sidecar/scripts/verify_memory_system.sh
```

Runs:
- `python3 -m unittest memory-system.tests.test_transcript_pipeline`
- `python3 -m unittest memory-system.tests.test_retrieval_foundation`
- `python3 -m unittest memory-system.tests.test_context_bridge`
- `python3 -m unittest memory-system.tests.test_semantic_retrieval`
- `python3 memory-system/eval/run_eval.py`

## Rebuild indexes only

```bash
./skills/memory-system-sidecar/scripts/rebuild_memory_indexes.sh
```

Runs:
- `python3 memory-system/ingest/build_index.py`

## Important caveat

If eval unexpectedly fails, rebuild indexes first, then rerun verify.

## Key artifacts regenerated by refresh

- `memory/ACTIVE_TASK_STATE.md`
- `memory/LIVE_CONTEXT_PACKET.md`
- `memory/RETRIEVAL_CONTEXT_BUNDLE.json`
- `experiments/phase-logs/context-manager-working-set.md`
- `experiments/phase-logs/context-manager-working-set.json`
