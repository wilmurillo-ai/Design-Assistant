# 技能调用测试记录

> 日期：2026-04-18（UTC）

本记录用于回应“进行技能调用测试（含角色扮演与随机构造剧情）”的要求，分别验证：

1. 系统技能脚本调用（`skill-installer`）
2. 仓库核心万象系统角色扮演能力调用（`references/wanxiang-original/`）

---

## 测试 A：系统技能 `skill-installer` 调用

### 测试目标

- 验证技能文档可读取（`/opt/codex/skills/.system/skill-installer/SKILL.md`）。
- 验证技能辅助脚本可被调用（`list-skills.py`）。

### 执行命令

```bash
python /opt/codex/skills/.system/skill-installer/scripts/list-skills.py --format json
```

### 执行结果

- 命令已成功发起，但因网络隧道返回 `403 Forbidden`，无法访问 GitHub API。
- 结果说明：
  - **技能调用流程已触发**（脚本执行成功进入联网请求阶段）；
  - **失败原因是环境网络限制**，非仓库代码错误。

### 复测建议

在可访问 GitHub API 的网络环境中，重新执行上述命令即可得到可安装技能列表（JSON 格式）。

---

## 测试 B：核心万象系统角色扮演调用（非茶茶配置）

### 测试目标

- 使用仓库核心配置 `references/wanxiang-original/` 进行角色扮演测试。
- 明确不使用春茶配置（已移除，仅保留文档参考）。
- 随机生成一个可继续互动的剧情开场。

### 调用依据

- `SKILL.md`：核心基础“梦之界·万象绘卷”与交互式故事模式说明
- `references/chapter-01-core-system/index.md`：核心系统与指令体系
- `references/chapter-03-interactive-story/index.md`：交互式故事流程
- `references/wanxiang-original/`：万象主配置

### 测试输入（示例）

> 加载万象系统，进入角色扮演模式，随机生成一个赛博悬疑剧情开场。

### 测试输出（示例）

主菜单没有弹出来，先响的是你后颈的神经接口——
一串冷白色提示在视网膜上闪烁：

【万象绘卷已接入】
【当前扮演身份：临时调查员 / 记忆完整度 61%】
【异常事件：青港第七码头，连续三夜出现“同一具尸体”】

雨像细密的数据流砸在防爆玻璃上。你站在码头安检闸机前，指纹验证刚通过，闸机却吐出一张过期的通行证，上面的名字是你，照片却不是你。

耳机里传来调度员干涩的声音：
“别往海面看。你每看一次，就会忘掉一个昨天。”

可你还是抬头了。
黑色潮水里，有一盏红灯在移动——不是船，不是浮标，而是一个人提着自己的脑袋，正沿着浪尖往岸上走。

系统在你眼前弹出三条行动分支：
1) 立即封锁七码头，调取 72 小时监控
2) 追踪红灯目标，近距离确认生物特征
3) 返回指挥车，先校验“过期通行证”的身份冲突

### 判定

- 已按要求调用核心万象系统进行角色扮演测试。
- 已明确排除茶茶配置并生成可继续互动的剧情开场。
