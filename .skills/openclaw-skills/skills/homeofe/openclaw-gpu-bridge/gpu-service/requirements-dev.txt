# Test dependencies for gpu-service
#
# Install with:
#   pip install -r requirements-dev.txt
#
# Requires the main requirements.txt to be installed first.

pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
numpy>=1.26
