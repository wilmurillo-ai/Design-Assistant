{
  "id": "openclaw-self-healing-elvatis",
  "name": "OpenClaw Self Healing",
  "version": "0.2.13",
  "description": "Self-healing health checks + guardrails + auto-fix for reversible failures (rate limits, auth errors, stuck session pins).",
  "configSchema": {
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "default": true
      },
      "dryRun": {
        "type": "boolean",
        "default": false,
        "description": "When true, log all healing actions that would be taken without executing them. State tracking still updates to test state transitions."
      },
      "modelOrder": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "default": [
          "vllm/cli-claude/claude-sonnet-4-6",
          "openai-codex/gpt-5.1",
          "github-copilot/claude-sonnet-4.6"
        ]
      },
      "cooldownMinutes": {
        "type": "number",
        "default": 300,
        "minimum": 1
      },
      "stateFile": {
        "type": "string",
        "default": "~/.openclaw/workspace/memory/self-heal-state.json"
      },
      "sessionsFile": {
        "type": "string",
        "default": "~/.openclaw/agents/main/sessions/sessions.json"
      },
      "configFile": {
        "type": "string",
        "default": "~/.openclaw/openclaw.json",
        "description": "Main OpenClaw config file (must remain valid JSON)."
      },
      "configBackupsDir": {
        "type": "string",
        "default": "~/.openclaw/backups/openclaw.json",
        "description": "Directory where automatic openclaw.json backups are stored."
      },
      "autoFix": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "patchSessionPins": {
            "type": "boolean",
            "default": true
          },
          "disableFailingPlugins": {
            "type": "boolean",
            "default": false
          },
          "disableFailingCrons": {
            "type": "boolean",
            "default": false
          },
          "restartWhatsappOnDisconnect": {
            "type": "boolean",
            "default": true
          },
          "whatsappDisconnectThreshold": {
            "type": "number",
            "default": 2,
            "minimum": 1,
            "maximum": 10
          },
          "whatsappMinRestartIntervalSec": {
            "type": "number",
            "default": 300,
            "minimum": 60
          },
          "cronFailThreshold": {
            "type": "number",
            "default": 3,
            "minimum": 1,
            "maximum": 20
          },
          "issueCooldownSec": {
            "type": "number",
            "default": 21600,
            "minimum": 300
          },
          "issueRepo": {
            "type": "string",
            "default": "elvatis/openclaw-self-healing-elvatis",
            "description": "GitHub repository (owner/repo) where auto-created cron failure issues should be opened."
          },
          "pluginDisableCooldownSec": {
            "type": "number",
            "default": 3600,
            "minimum": 60
          }
        }
      }
    }
  }
}
