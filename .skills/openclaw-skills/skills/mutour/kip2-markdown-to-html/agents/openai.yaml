interface:
  display_name: "Markdown to HTML"
  short_description: "Convert Markdown into polished HTML with built-in or custom templates"
  default_prompt: "Use $markdown-to-html to convert a Markdown file or Markdown string into a polished HTML page."
