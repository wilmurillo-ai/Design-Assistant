# AI-Animation 工作流参考

## 核心流程图

```
用户输入科普文本 + 输出路径
           │
           ▼
┌─────────────────────────────┐
│  Step 1: 生成基础 HTML      │
│  - 拼接文本 + PPT Prompt   │
│  - AI 生成 HTML 代码         │
│  - 保存到用户指定路径        │
└─────────────────────────────┘
           │
           ▼
┌─────────────────────────────┐
│  Step 2: PPT 模板重构       │
│  - 用户选择模板 (1-4)       │
│  - AI 重构页面样式           │
│  - 强制执行 emoji 转图标     │
└─────────────────────────────┘
           │
           ▼
    用户是否要求
    「生成流程图」？
           │
        是  │  否
           ▼       ▼
┌──────────────┐    完成
│ Step 3: RNN  │
│ 模板重构     │
│ - 合并相邻页 │
│ - RNN模板    │
└──────────────┘
```

## Prompt 模板

### Step 1 Prompt（基础生成）

```
根据以上内容生成基于纯前端页面单页布局仿PPT换页轮播进行直观图形化可视化的介绍，
加大文字大小，并运用加粗、下划线、斜体、文字颜色、文字背景等强调方式方便进行视频演示。
添加每次切换页面时页面中的各个元素依次"缓入"出现的动画效果(细化到每行文字)。
```

### Step 2 Prompt（PPT 重构）

```
以 templates/PPT-Template/[模板名] 为模板，重构 [文件路径] 的内容。
将网页的风格转为暖色调风格(比如白色背景黑色文字加页面粉色点缀)，
减少PPT演示的字数，增大文字大小，加强图形化显示，图文并茂，方便视频展示。
将emoji图标换成平面ui库的图标。
```

### Step 3 Prompt（流程图重构）

```
将 [文件路径] 网页的相邻两页内容合并后按照 templates/RNN-Template/[RNN模板] 的
平面UI样式进行视觉重构，保持现有颜色方案不变。
精确还原模板中的布局结构、元素间距、字体样式、图标设计和视觉层次；
确保所有交互元素（按钮、表单、导航等）的视觉表现与参考网页一致；
维持原有的颜色值和配色方案；保证修改后的页面在不同设备和浏览器上具有良好的响应式表现；
优化DOM结构以匹配UI设计，同时保持原有功能完整性和交互逻辑不变。
完成后需进行视觉一致性检查，确保实现效果与参考网页达到95%以上的相似度。
```

## 模板选择建议

### PPT 模板

| 模板 | 特点 | 适用场景 |
|------|------|----------|
| PPT-Generate-1.html | 简洁风格 | 基础演示 |
| PPT-Generate-2.html | 图表丰富 | 数据类内容 |
| **PPT-Generate-3.html** | **视觉效果最佳** | **通用推荐** |
| PPT-Generate-4.html | 布局灵活 | 复杂内容 |

### RNN 模板（流程图）

| 模板 | 特点 | 适用场景 |
|------|------|----------|
| RNN-2.html | 对比风格 | 传统 vs 新方案 |
| **RNN-3.html** | **分层卡片** | **步骤类内容** |
| RNN-4.html | 纵向流程 | 线性步骤 |
| RNN-5.html | 分支结构 | 条件分支 |
| RNN-6.html | 树状图 | 层级关系 |
| RNN-7.html | 网格布局 | 并列模块 |

## 输出路径配置

```
用户指定路径 / AI_Animation.html
         │
         ├── 默认：桌面/AI_Animation.html
         ├── 用户指定：D:\Videos\AI_Animation.html
         └── 当前项目：./output/AI_Animation.html
```

## 动画效果关键词

- **缓入动画**：元素依次"缓入"出现
- **强调效果**：加粗、下划线、斜体、文字颜色、文字背景
- **图形化**：图标、数据可视化、流程图
- **PPT翻页**：轮播图形式的页面切换
- **3D效果**：模块整体添加3D旋转效果
- **发光效果**：元素添加"发光"或"泛光"效果
