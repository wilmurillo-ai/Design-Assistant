# @spectyra/openclaw-skill

Published skill content lives in **`SKILL.md`** (OpenClaw / ClawHub). **`skill.json`** and **`config-fragment.json`** are optional metadata for merge-based installs that wire `spectyra/*` to the companion on **127.0.0.1** (default companion port **4111**).
