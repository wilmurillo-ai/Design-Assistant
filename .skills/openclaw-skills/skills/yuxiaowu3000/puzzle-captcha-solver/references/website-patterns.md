# 各网站验证码特征库

## 大麦网 (damai.cn)

### 验证码特征

| 特征 | 描述 |
|------|------|
| **类型** | 箭头拼图滑块 |
| **弹窗背景** | 粉色渐变 (#FF6B9D 到 #FFB6C1) |
| **弹窗尺寸** | 约 340x200 像素 |
| **滑块图标** | 右箭头 (→) 白色 |
| **滑块背景** | 粉色/玫红色 |
| **滑块尺寸** | 约 50x50 像素 |
| **轨道位置** | 弹窗底部 |
| **轨道长度** | 约 260 像素 |
| **验证提示** | "Please drag the slider as instructed" |
| **Code 显示** | 底部显示验证码 Code |

### 选择器

```javascript
// 滑块按钮
'.slider-btn'
'.yidun_slider'
'button[class*="slider"]'

// 轨道
'.slider-track'
'.yidun_track'

// 背景图
'.yidun_bg'

// 弹窗
'.yidun_modal'
'[class*="captcha"]'
```

### 识别要点

```python
# 1. 颜色检测（粉色区域）
lower_pink = np.array([140, 50, 50])
upper_pink = np.array([170, 255, 255])

# 2. 滑块在底部区域
bottom_mask = mask[int(height*0.4):, :]

# 3. 偏移量范围
offset_range = (100, 200)  # 常见偏移范围
```

### 注意事项

- 大麦使用易盾验证码（网易）
- 验证码 Code 可用于反馈错误
- 热门演出可能有多次验证

---

## 淘宝/天猫 (taobao.com / tmall.com)

### 验证码特征

| 特征 | 描述 |
|------|------|
| **类型** | 缺口拼图滑块 |
| **弹窗背景** | 白色 |
| **弹窗尺寸** | 约 380x300 像素 |
| **滑块图标** | 拼图块（不规则形状） |
| **滑块颜色** | 蓝色 (#1E80FF) |
| **滑块尺寸** | 约 60x60 像素 |
| **背景图片** | 随机风景/建筑/动物 |
| **轨道位置** | 图片下方 |
| **验证提示** | "向右滑动填充拼图" |

### 选择器

```javascript
// 滑块
'.slider'
'#slider'
'.nc-slider'
'.nc_icon'

// 背景图
'.nc_bg'
'[class*="bg"]'

// 弹窗
'.nc_modal'
'#captcha-modal'
```

### 识别要点

```python
# 1. 使用模板匹配
# 需要同时获取背景图和滑块图

# 2. 边缘检测
edges = cv2.Canny(image, 50, 150)

# 3. 偏移量范围
offset_range = (100, 250)
```

### 注意事项

- 淘宝验证码会动态更新背景图
- 可能需要多次尝试
- 建议使用第三方打码平台作为备用

---

## 京东 (jd.com)

### 验证码特征

| 特征 | 描述 |
|------|------|
| **类型** | 缺口拼图滑块 |
| **弹窗背景** | 白色 |
| **弹窗尺寸** | 约 360x280 像素 |
| **滑块图标** | 拼图块 |
| **滑块颜色** | 蓝色渐变 |
| **滑块尺寸** | 约 50x50 像素 |
| **背景图片** | 随机图片 |
| **轨道位置** | 图片下方 |
| **验证提示** | "请向右滑动验证" |

### 选择器

```javascript
// 滑块
'.JD_slider'
'.slide-btn'
'#slide-btn'

// 背景
'.JD_bg'

// 弹窗
'.JD_modal'
```

### 识别要点

```python
# 类似淘宝，使用模板匹配
# 京东的拼图块形状相对规则
```

---

## 12306 (铁路购票)

### 验证码特征

| 特征 | 描述 |
|------|------|
| **类型** | 图片点击 / 滑块混合 |
| **弹窗背景** | 白色 |
| **验证方式** | 点击指定文字/图案 |
| **提示文字** | "请点击下面的【xx】" |

### 注意事项

- 12306 验证码非常复杂
- 建议使用官方 APP
- 自动识别成功率较低

---

## 微信/腾讯系

### 验证码特征

| 特征 | 描述 |
|------|------|
| **类型** | 图形对齐 / 滑块 |
| **弹窗背景** | 白色/浅色 |
| **验证方式** | 拖动对齐两个图形 |
| **图形类型** | 动物、文字、图标等 |

### 选择器

```javascript
'.tcaptcha_slider'
'.slider-btn'
```

---

## 抖音/字节系

### 验证码特征

| 特征 | 描述 |
|------|------|
| **类型** | 缺口拼图 / 图形旋转 |
| **弹窗风格** | 深色/渐变 |
| **验证方式** | 滑动或旋转 |

### 注意事项

- 部分验证码需要旋转拼图
- 需要额外的旋转角度识别

---

## 验证码识别策略对比

| 网站 | 识别方法 | 成功率 | 备注 |
|------|----------|--------|------|
| 大麦网 | 颜色检测 + 边缘检测 | 85%+ | 相对简单 |
| 淘宝 | 模板匹配 | 75%+ | 需要两张图 |
| 京东 | 模板匹配 | 75%+ | 类似淘宝 |
| 12306 | OCR + 图像识别 | 50%+ | 非常复杂 |
| 微信 | 模板匹配 | 80%+ | 相对规则 |
| 抖音 | 模板匹配 + 旋转检测 | 70%+ | 需要旋转 |

---

## 通用识别流程

```python
def identify_captcha_type(image):
    """识别验证码类型"""
    
    # 1. 检测主色调
    hsv = cv2.cvtColor(image, cv2.COLOR_BGR2HSV)
    hist = cv2.calcHist([hsv], [0, 1], None, [180, 256], [0, 180, 0, 256])
    
    # 2. 检测是否有粉色（大麦特征）
    pink_mask = cv2.inRange(hsv, (140, 50, 50), (170, 255, 255))
    pink_ratio = np.sum(pink_mask > 0) / pink_mask.size
    
    if pink_ratio > 0.1:
        return "damai_arrow"
    
    # 3. 检测是否有拼图块形状
    edges = cv2.Canny(image, 50, 150)
    contours, _ = cv2.findContours(edges, cv2.RETR_EXTERNAL, cv2.CHAIN_APPROX_SIMPLE)
    
    for contour in contours:
        area = cv2.contourArea(contour)
        if 1000 < area < 10000:
            # 可能是拼图块
            return "puzzle_gap"
    
    # 4. 默认
    return "unknown"
```

---

## 快速参考表

### 常见偏移量范围

| 网站 | 最小偏移 | 最大偏移 | 常见偏移 |
|------|----------|----------|----------|
| 大麦网 | 80 | 200 | 120-160 |
| 淘宝 | 100 | 250 | 150-200 |
| 京东 | 100 | 250 | 150-200 |
| 微信 | 50 | 180 | 100-140 |

### 常见滑块尺寸

| 网站 | 宽度 | 高度 |
|------|------|------|
| 大麦网 | 45-55 | 45-55 |
| 淘宝 | 55-65 | 55-65 |
| 京东 | 45-55 | 45-55 |
| 微信 | 40-50 | 40-50 |

### 验证超时时间

| 网站 | 超时时间 | 重试次数 |
|------|----------|----------|
| 大麦网 | 30 秒 | 3 次 |
| 淘宝 | 60 秒 | 5 次 |
| 京东 | 60 秒 | 5 次 |
| 微信 | 30 秒 | 3 次 |
