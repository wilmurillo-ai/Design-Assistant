#include <bits/stdc++.h>
#include "testlib.h"
using namespace std;

int main(int argc, char *argv[]) {
    // 本题不需要特殊校验器，使用默认的文本比较即可
    registerLemonChecker(argc, argv);
    quitf(_ok, "Use standard checker");
    return 0;
}
