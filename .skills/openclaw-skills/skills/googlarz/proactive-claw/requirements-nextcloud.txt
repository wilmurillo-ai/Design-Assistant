# Pinned dependencies for Nextcloud CalDAV backend.
# Install with: python3 -m pip install -r requirements-nextcloud.txt
# Hashes are not included in this file; generate a hash-locked file in your environment if required.
caldav==1.3.9
icalendar==5.0.13
