[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "detect-file-type-local"
version = "0.2.0"
description = "Security-focused local file type detection powered by Google Magika"
readme = "README.md"
license = "MIT"
requires-python = ">=3.8"
dependencies = [
    "magika>=1.0.0,<2.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "ruff>=0.4",
]

[project.scripts]
detect_file_type = "detect_file_type.cli:main"

[tool.ruff]
target-version = "py38"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I"]

[tool.pytest.ini_options]
testpaths = ["tests"]
