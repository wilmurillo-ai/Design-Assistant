interface:
  display_name: "Voice Log"
  short_description: "OpenClaw live conversation journal powered by Soniox."
  default_prompt: "Use $voice-log to start or stop Soniox voice journaling (requires SONIOX_API_KEY), then summarize recent minutes from the rolling 1-hour transcript."
policy:
  allow_implicit_invocation: false
