{
  "id": "rednote-mac",
  "name": "rednote-mac",
  "description": "Control RedNote (Xiaohongshu) Mac app via macOS Accessibility API. Supports DMs, comment reply, video comments, search, like, collect, author stats. Requires Mac + rednote App visible + Terminal accessibility permission.",
  "configSchema": {
    "type": "object",
    "properties": {},
    "required": []
  }
}
