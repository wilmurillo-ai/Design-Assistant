# Agentfarm-Finder Configuration
# ====================================

# Subject keywords (what is it) - more precise phrases
SUBJECT_KEYWORDS="virtuals protocol erc-8004 x402 aiagent autonomous agent ai-bot"

# Action keywords (what can you do)
ACTION_KEYWORDS="mint yield farm stake claim launch"

# Search Parameters
COUNT=30
SORT_BY="engagement"

ENABLE_TELEGRAM=false
OUTPUT_DIR="./output"
LOG_FILE="./logs/agentfarm.log"
DRY_RUN=false
DEBUG=false
