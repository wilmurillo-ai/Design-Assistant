# 📱 QR Code Generator for OpenClaw

A lightweight, efficient skill for **OpenClaw** that allows your AI agent to generate high-quality QR codes instantly via the command line.

## ✨ Features
- **Instant Generation:** Create QR codes for URLs, plain text, or Wi-Fi credentials.
- **Auto-Formatting:** Uses optimal error correction (fit=True) for readability.
- **Local Storage:** Saves directly to your workspace as a `.png` file.
- **Zero Configuration:** Works out of the box with Python 3.

## 🚀 Installation

Install directly via **ClawHub**:

```bash
clawhub install your-github-username/qr-generator