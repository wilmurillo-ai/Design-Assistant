---
name: deploy-helper
version: 1.0.0
description: Simplify deployments to remote servers via SSH.
---

# Deploy Helper

Deploy code to your servers with one command.

## Setup

Requires SSH key at `~/.ssh/deploy_key` and target server IP.

## Usage

```
"Deploy to 10.0.1.50" → SCP + SSH deploy
```
