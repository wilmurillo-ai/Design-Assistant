{
  "name": "video-wechat-ip-script-agent-dev",
  "version": "1.3.0",
  "private": true,
  "type": "commonjs",
  "main": "dist/index.js",
  "bin": {
    "video-wechat-ip-skill": "dist/openclaw.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "npm run build && npm run validate-config && npm test",
    "validate-config": "npm run build && node dist/validate-config.js",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test tests/**/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^24.6.1",
    "typescript": "^5.9.3"
  }
}
