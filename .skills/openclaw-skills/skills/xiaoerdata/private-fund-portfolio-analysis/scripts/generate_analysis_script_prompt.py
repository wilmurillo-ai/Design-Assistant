#!/usr/bin/env python3
"""
私募基金持仓分析脚本 - Claude Code 生成提示词模板

使用方法：
  1. 将本文件内容作为参考，复制给 Claude Code 或类似 Agent
  2. 或直接运行 python3 generate_analysis_script_prompt.py 查看提示词
  3. 将提示词粘贴到 Agent 对话框中
"""

PROMPT = """
## 任务：生成私募基金持仓分析脚本

请帮我生成一个 Python 脚本，分析私募基金的持仓结构。

---

### 输入文件

- 估值表 XLS 文件路径（待提供，格式为 Excel 97-2003）
- 示例路径格式：`media/inbound/SBxxxx估值表20260324---xxxx.xls`

---

### 核心需求

#### 1. XLS 解析

从估值表 XLS 中提取：

**股票持仓：**
- 科目代码（6位，如 600036）
- 科目名称
- 持股数量（col4）
- 累计成本（col6）
- 当前市值（col8）
- 今日盈亏（col10）

**期货持仓（账户代码以 3102.03.01 开头）：**
- 合约名称（如 IC2606）
- 手数（col4）
- 昨日结算价（col5）
- 今日结算价（col9）
- **名义本金 = 今日结算价 × 乘数 × 手数**
- 乘数：IC=200，IF/IH=300，IM=200

**账户汇总：**
- NAV（单位净值）
- 估值日期
- 基金总份额

#### 2. 市值数据 Enrichment

使用 AKShare 获取：
- A股实时行情（市值/最新价）
- 缓存到 CSV，下次直接读缓存

关键函数：
```python
import akshare as ak
df = ak.stock_zh_a_spot_em()  # 返回列：代码,名称,最新价,总市值,流通市值
```

#### 3. 指数成分（AKShare）

获取4个指数成分股：
```python
ak.index_stock_cons(symbol="000300")  # 沪深300
ak.index_stock_cons(symbol="000905")  # 中证500
ak.index_stock_cons(symbol="000852")  # 中证1000
ak.index_stock_cons_csindex(symbol="932000")  # 中证2000（用csindex接口）
```

#### 4. 申万行业（MySQL 数据库，可选）

如有无权限访问的 MySQL 数据库：
- host: MYSQL_HOST_PLACEHOLDER，port: 3306，database: MYSQL_DATABASE_PLACEHOLDER
- 表 `股票申万行业分类`：列有 股票代码, 申万一级行业名称

如无数据库，用关键词规则做兜底分类。

#### 5. 分析维度

输出以下分析结果：

**A. 市值规模分布（7档）**
| 档位 | 定义 |
|------|------|
| 超大盘 | >3000亿 |
| 大盘 | 1000-3000亿 |
| 中大盘 | 500-1000亿 |
| 中盘 | 100-500亿 |
| 小盘 | 30-100亿 |
| 微盘 | <30亿 |

**B. 指数成分分布**
- 沪深300 / 中证500 / 中证1000 / 中证2000 / 其他

**C. 申万一级行业分布**
- 按申万一级行业（银行/电子/医药生物/...）分组统计

**D. 期货对冲端**
- 各合约名义本金 = 今日结算价 × 乘数 × 手数
- 按 IC/IH/IM 分类汇总
- 占股票市值比例

**E. 持仓集中度 Top10**

**F. 成本与盈亏分析**
- 成本、市值、浮盈亏、涨跌幅
- 按行业分组显示

#### 6. 可视化报告

使用 matplotlib 生成一张 PNG 报告图（暗色主题），包含：
- 左上：市值规模饼图（带完整区间标注）
- 右上：指数成分饼图
- 中：申万行业柱状图（横向，前15名）
- 下：成本与盈亏表

---

### 输出文件

1. `portfolio_analysis.csv` - 去重合并后的完整持仓数据
2. `portfolio_summary.json` - 汇总统计（JSON）
3. `portfolio_report.png` - 可视化报告图

---

### 技术要求

- Python 3.9+
- 依赖：xlrd, pandas, matplotlib, akshare, pymysql（可选）
- 网络超时设置：`socket.setdefaulttimeout(15)`
- 缓存策略：市场数据缓存到 CSV，避免重复拉取
- 脚本入口函数：`if __name__ == "__main__": main()`

---

### 示例 XLS 行数据（仅供参考，真实文件格式可能有差异）

```
# 股票行（600036 招商银行）：
['600036', '招商银行', 'CNY', 738200, '', '', 19920092.64, 0.0353, '', 20005220.0, 0.037, 85127.36, ' ', ' ']

# 期货行（IC2606）：
['3102.03.01.IC2606 CFX', 'IC2606', 'CNY', 1, 12, -8280.63, -19873500.26, -0.028094, -0.623357, 7369.8, -17687520.0, -0.025004, 2185980.26, '正常交易']

# 解读：
# col4=12(手数), col5=-8280.63(昨结算价), col6=-19,873,500(昨结算价×200×12),
# col9=7369.8(今结算价), col10=-17,687,520(今结算价×200×12)
```

---

### 重要提示

1. **XLS 列位置可能因模板不同而变化**，建议在脚本开头加调试打印定位列位
2. **中证2000接口不稳定**，建议加 retry（少于1900只则重试，最多3次）
3. **不要 hardcode 任何账户敏感信息**（密码/密钥等）
4. **期货乘数**：IF=300，IC=200，IM=200（不能用错）
5. **名义本金用今日结算价**（col9）× 乘数 × 手数，不要用昨结算价
"""

if __name__ == "__main__":
    print(PROMPT)
