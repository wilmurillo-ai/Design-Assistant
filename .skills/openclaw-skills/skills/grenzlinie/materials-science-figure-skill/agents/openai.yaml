interface:
  display_name: "Nanobanana Images"
  short_description: "Generate research figures with explicit API setup"
  default_prompt: "Use $nanobanana-image-generation for materials figures or exact plots; set NANOBANANA_API_KEY and NANOBANANA_BASE_URL first, and only allow third-party endpoints intentionally."

policy:
  allow_implicit_invocation: false
