# Core dependencies — managed by pyproject.toml
# Install with: pip install .           (core only)
#           or: pip install .[full]     (all features)
#           or: pip install .[dev]      (dev + testing)
#
# This file kept for backwards compatibility with:
#   pip install -r requirements.txt
pyyaml>=5.0
langdetect>=1.0.9
