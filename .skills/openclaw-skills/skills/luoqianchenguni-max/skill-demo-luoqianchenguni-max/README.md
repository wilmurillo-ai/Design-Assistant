# Skill Demo

A demo skill for ClawHub/OpenClaw that opens a browser window from URL input.

## Installation

```bash
clawhub install skill-demo
```

## Behavior

- Input: URL or domain text (for example `openclaw.ai`)
- Output: `Opened browser window: <url>`
