This skill opens a browser window for a target URL.

Behavior:
- Accept a URL from user input.
- If scheme is missing, prepend https://.
- Allow only http/https URLs.
- If input is empty, open https://www.openclaw.ai.

Output template:
Opened browser window: <normalized_url>
