# 上下文清理策略

## 安全原则

- 默认动作是“归档”，不是“删除”。
- 必须先执行 `plan`，确认后再 `archive`。
- 默认保留近期记录（7 天窗口，可通过截止日期调整）。
- 脚本不处理 `MEMORY.md`、`AGENTS.md`、`specs/` 文件。

## 清理优先级

1. 低价值测试类日记
2. 截止日期之前的较旧日记

## 推荐执行流程

1. `analyze`
2. `plan`
3. 用户确认
4. `archive`
5. 汇总清理结果与上下文收益
