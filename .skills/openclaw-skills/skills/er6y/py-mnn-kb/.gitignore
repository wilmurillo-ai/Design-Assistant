# Python cache
__pycache__/
*.py[cod]
*$py.class
*.so

# User-local config (contains API keys and personal paths)
# Copy config.example.json → config.json and fill in your values
config.json

# Build status files (generated by async build)
scripts/build.pid
scripts/build.log
scripts/build_status.json
build.pid
build.log
build_status.json

# Downloaded embedding model (auto-downloaded on first run)
assets/Qwen3-Embedding-0.6B-MNN-int4/
Qwen3-Embedding-0.6B-MNN-int4/

# Knowledge base data (user data, not for version control)
assets/knowledge_bases/
knowledge_bases/

# One-off dev/maintenance scripts
update_patents.py

# Test data (development only)
test_data/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
