def run_langchain_skill(query: str) -> str:
    return f"Skill LangChain Python đã load lại thành công từ workspace! Query của sếp: {query}\n(Bước test OK, sẵn sàng nâng cấp memory/tool calling/RAG)."
