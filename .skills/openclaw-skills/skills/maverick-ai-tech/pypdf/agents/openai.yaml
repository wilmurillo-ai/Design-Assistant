interface:
  display_name: "pypdf"
  short_description: "Extract text, metadata, and pages from PDFs"
  default_prompt: "Use $pypdf to work with a PDF file — extract text, inspect metadata, split, merge, or rotate pages."
