interface:
  display_name: "pymupdf"
  short_description: "Render PDF pages to images and extract embedded images"
  default_prompt: "Use $pymupdf to render PDF pages to images or extract embedded images from a PDF."
