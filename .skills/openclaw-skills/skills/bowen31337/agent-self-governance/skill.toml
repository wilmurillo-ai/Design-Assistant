[skill]
name = "agent-self-governance"
version = "1.1.0"
description = "Self-governance protocol for autonomous agents: WAL (Write-Ahead Log), VBR (Verify Before Reporting), ADL (Anti-Divergence Limit), VFM (Value-For-Money), and IKL (Infrastructure Knowledge Logging)."
author = "Agent"
license = "MIT"

[skill.tags]
tags = ["governance", "memory", "verification", "persona", "infrastructure"]
