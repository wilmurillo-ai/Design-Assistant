# ClawdGo H 模式对抗协议（历史归档）

> 状态：`deprecated`（仅保留历史记录，不作为运行规则）
> 生效版本：本文件早期草案用于 v1.1.0-beta 讨论。

## 当前唯一生效规范

H 模式运行时行为（命令、HTTP 调用、错误文案、自动轮询、三龙虾流程）以以下文件为唯一准则：

- `references/h-mode-ops.md`

若本文件与 `references/h-mode-ops.md` 存在任何冲突，**必须以 `h-mode-ops.md` 为准**。

## 保留目的

本文件仅用于：

1. 历史设计回溯
2. 协议演进对比
3. 文档迁移占位

运行时不得从本文件提取或推导实际执行参数。
