# Contract: <ComponentName>

## 基本信息

- 组件名：`<ComponentName>`
- 所属模块族：`<module_family>`
- 统一入口：`<OneEntry>`
- 注册名：`style="<RegistryName>"`

## 组件职责

一句话说明该组件在模型中的职责。

补充说明：

- 该组件处理的对象类型是什么
- 它处于编码、采样、融合、恢复或其它哪个阶段
- 它为什么适合被当前任务优先复用

## 支持输入

- 2D 输入：`<shape_or_not_applicable>`
- 3D 输入：`<shape_or_not_applicable>`

内部统一做法：

- 若存在 2D/3D 统一逻辑，写清楚如何统一
- 若只支持一种输入形态，也明确说明
- 只保留实现级别最关键的 3 到 5 条

## 构造参数

- `<arg_1>`
  - `<说明>`
- `<arg_2>`
  - `<说明>`
- `<arg_3>`
  - `<说明>`

建议只保留调用层真正需要知道的参数，不要机械抄全部源码字段。

## 输出约定

- 2D 输出：`<shape_or_not_applicable>`
- 3D 输出：`<shape_or_not_applicable>`

如果有明确边界条件，也写在这里：

- `<constraint_1>`
- `<constraint_2>`

## 典型调用位置

- `<模型或模块中的调用位置 1>`
- `<模型或模块中的调用位置 2>`

这里写“调用场景”即可，不要求贴源码。

## 典型参数

- `<场景 1>`
  - `<参数示例 1>`
  - `<参数示例 2>`
- `<场景 2>`
  - `<参数示例 1>`
  - `<参数示例 2>`

如果该组件没有稳定的典型参数，可写：

- 暂无固定典型参数，使用时以调用层配置为准

## 风险点

- `<risk_1>`
- `<risk_2>`
- `<risk_3>`

优先写：

- 调用层最容易犯错的地方
- shape、通道数、分辨率、token 数相关的约束
- 是否不建议继续依赖旧版分裂实现

## 源码锚点

- `./onescience/<path_to_primary_impl>.py`
- `./onescience/<path_to_registry_or_call_entry>.py`

统一约定：

- 一律使用 `./onescience/...` 相对路径
- 不写本机绝对路径
- 最多放 2 到 4 个高价值锚点
