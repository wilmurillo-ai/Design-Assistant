interface:
  display_name: "PostalForm Machine Order"
  short_description: "Send real mail via PostalForm"
  default_prompt: "Send real postal mail through PostalForm by validating the print-and-mail payload, creating the machine order, settling x402 payment (with purl or another x402 client), and polling until completion. Use strict first-pass checks for PDF/address/request_id correctness and handle transient webhook delays safely."
