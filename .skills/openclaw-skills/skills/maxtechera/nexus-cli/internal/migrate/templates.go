package migrate

// Core compose file header.
const composeHeader = `# Admirarr Docker Compose Stack
# Generated by: admirarr migrate
# TRaSH Guides volume structure: https://trash-guides.info/Hardlinks/How-to-setup-for/Docker/

services:
`

// Service compose snippets keyed by service name.
var serviceSnippets = map[string]string{
	"jellyfin": `  jellyfin:
    image: jellyfin/jellyfin:latest
    container_name: jellyfin
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${CONFIG_DIR}/jellyfin:/config
      - ${DATA_DIR}:/data
    ports:
      - "8096:8096"
    restart: unless-stopped
`,
	"radarr": `  radarr:
    image: ghcr.io/hotio/radarr:latest
    container_name: radarr
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${CONFIG_DIR}/radarr:/config
      - ${DATA_DIR}:/data
    ports:
      - "7878:7878"
    restart: unless-stopped
`,
	"sonarr": `  sonarr:
    image: ghcr.io/hotio/sonarr:latest
    container_name: sonarr
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${CONFIG_DIR}/sonarr:/config
      - ${DATA_DIR}:/data
    ports:
      - "8989:8989"
    restart: unless-stopped
`,
	"prowlarr": `  prowlarr:
    image: ghcr.io/hotio/prowlarr:latest
    container_name: prowlarr
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${CONFIG_DIR}/prowlarr:/config
    ports:
      - "9696:9696"
    restart: unless-stopped
`,
	"gluetun": `  gluetun:
    image: qmcgaw/gluetun:latest
    container_name: gluetun
    cap_add:
      - NET_ADMIN
    environment:
      - VPN_SERVICE_PROVIDER=${VPN_PROVIDER}
      - VPN_TYPE=${VPN_TYPE:-wireguard}
      - WIREGUARD_PRIVATE_KEY=${WIREGUARD_PRIVATE_KEY:-}
      - WIREGUARD_ADDRESSES=${WIREGUARD_ADDRESSES:-}
      - OPENVPN_USER=${OPENVPN_USER:-}
      - OPENVPN_PASSWORD=${OPENVPN_PASSWORD:-}
      - SERVER_COUNTRIES=${SERVER_COUNTRIES:-}
      - TZ=${TZ}
    volumes:
      - ${CONFIG_DIR}/gluetun:/gluetun
    ports:
      - "8080:8080" # qBittorrent WebUI (routed through VPN)
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:9999"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
    restart: unless-stopped
`,
	"qbittorrent": `  qbittorrent:
    image: ghcr.io/hotio/qbittorrent:latest
    container_name: qbittorrent
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${CONFIG_DIR}/qbittorrent:/config
      - ${DATA_DIR}/torrents:/data/torrents
    network_mode: "service:gluetun"
    depends_on:
      gluetun:
        condition: service_healthy
    restart: unless-stopped
`,
	"recyclarr": `  recyclarr:
    image: ghcr.io/recyclarr/recyclarr:latest
    container_name: recyclarr
    environment:
      - TZ=${TZ}
    volumes:
      - ${CONFIG_DIR}/recyclarr:/config
    restart: unless-stopped
`,
	"seerr": `  seerr:
    image: ghcr.io/seerr-team/seerr:latest
    container_name: seerr
    environment:
      - TZ=${TZ}
    volumes:
      - ${CONFIG_DIR}/seerr:/app/config
    ports:
      - "5055:5055"
    restart: unless-stopped
`,
	"bazarr": `  bazarr:
    image: ghcr.io/hotio/bazarr:latest
    container_name: bazarr
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${CONFIG_DIR}/bazarr:/config
      - ${DATA_DIR}:/data
    ports:
      - "6767:6767"
    restart: unless-stopped
`,
	"flaresolverr": `  flaresolverr:
    image: ghcr.io/flaresolverr/flaresolverr:latest
    container_name: flaresolverr
    environment:
      - TZ=${TZ}
    ports:
      - "8191:8191"
    restart: unless-stopped
`,
	"autobrr": `  autobrr:
    image: ghcr.io/autobrr/autobrr:latest
    container_name: autobrr
    environment:
      - TZ=${TZ}
    volumes:
      - ${CONFIG_DIR}/autobrr:/config
    ports:
      - "7474:7474"
    restart: unless-stopped
`,
	"maintainerr": `  maintainerr:
    image: ghcr.io/jorenn92/maintainerr:latest
    container_name: maintainerr
    environment:
      - TZ=${TZ}
    volumes:
      - ${CONFIG_DIR}/maintainerr:/opt/data
    ports:
      - "6246:6246"
    restart: unless-stopped
`,
	"tautulli": `  tautulli:
    image: ghcr.io/tautulli/tautulli:latest
    container_name: tautulli
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${CONFIG_DIR}/tautulli:/config
    ports:
      - "8181:8181"
    restart: unless-stopped
`,
	"lidarr": `  lidarr:
    image: ghcr.io/hotio/lidarr:latest
    container_name: lidarr
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${CONFIG_DIR}/lidarr:/config
      - ${DATA_DIR}:/data
    ports:
      - "8686:8686"
    restart: unless-stopped
`,
	"readarr": `  readarr:
    image: ghcr.io/hotio/readarr:latest
    container_name: readarr
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${CONFIG_DIR}/readarr:/config
      - ${DATA_DIR}:/data
    ports:
      - "8787:8787"
    restart: unless-stopped
`,
	"whisparr": `  whisparr:
    image: ghcr.io/hotio/whisparr:latest
    container_name: whisparr
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${CONFIG_DIR}/whisparr:/config
      - ${DATA_DIR}:/data
    ports:
      - "6969:6969"
    restart: unless-stopped
`,
	"sabnzbd": `  sabnzbd:
    image: ghcr.io/hotio/sabnzbd:latest
    container_name: sabnzbd
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${CONFIG_DIR}/sabnzbd:/config
      - ${DATA_DIR}/usenet:/data/usenet
    ports:
      - "8081:8080"
    restart: unless-stopped
`,
	"unpackerr": `  unpackerr:
    image: ghcr.io/unpackerr/unpackerr:latest
    container_name: unpackerr
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${CONFIG_DIR}/unpackerr:/config
      - ${DATA_DIR}:/data
    restart: unless-stopped
`,
	"tdarr": `  tdarr:
    image: ghcr.io/haveagitgat/tdarr:latest
    container_name: tdarr
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
      - serverIP=0.0.0.0
      - serverPort=8266
      - webUIPort=8265
    volumes:
      - ${CONFIG_DIR}/tdarr/server:/app/server
      - ${CONFIG_DIR}/tdarr/configs:/app/configs
      - ${CONFIG_DIR}/tdarr/logs:/app/logs
      - ${DATA_DIR}/media:/media
      - ${DATA_DIR}/tdarr_cache:/temp
    ports:
      - "8265:8265"
      - "8266:8266"
    restart: unless-stopped
`,
	"jellystat": `  jellystat:
    image: ghcr.io/cyfershepard/jellystat:latest
    container_name: jellystat
    environment:
      - POSTGRES_USER=${JS_DB_USER:-jellystat}
      - POSTGRES_PASSWORD=${JS_DB_PASS:-jellystat}
      - POSTGRES_IP=jellystat-db
      - POSTGRES_PORT=5432
      - JWT_SECRET=${JS_JWT_SECRET:-changeme}
      - TZ=${TZ}
    ports:
      - "3000:3000"
    depends_on:
      - jellystat-db
    restart: unless-stopped

  jellystat-db:
    image: postgres:15-alpine
    container_name: jellystat-db
    environment:
      - POSTGRES_USER=${JS_DB_USER:-jellystat}
      - POSTGRES_PASSWORD=${JS_DB_PASS:-jellystat}
      - POSTGRES_DB=jellystat
    volumes:
      - ${CONFIG_DIR}/jellystat/db:/var/lib/postgresql/data
    restart: unless-stopped
`,
	"notifiarr": `  notifiarr:
    image: golift/notifiarr:latest
    container_name: notifiarr
    environment:
      - TZ=${TZ}
    volumes:
      - ${CONFIG_DIR}/notifiarr:/config
    ports:
      - "5454:5454"
    restart: unless-stopped
`,
	"profilarr": `  profilarr:
    image: ghcr.io/dictionarry-hub/profilarr:latest
    container_name: profilarr
    environment:
      - TZ=${TZ}
    volumes:
      - ${CONFIG_DIR}/profilarr:/app/data
    ports:
      - "6868:6868"
    restart: unless-stopped
`,
	"watchtower": `  watchtower:
    image: containrrr/watchtower:latest
    container_name: watchtower
    environment:
      - TZ=${TZ}
      - WATCHTOWER_CLEANUP=true
      - WATCHTOWER_SCHEDULE=0 0 4 * * *
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped
`,
	"plex": `  plex:
    image: plexinc/pms-docker:latest
    container_name: plex
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    volumes:
      - ${CONFIG_DIR}/plex:/config
      - ${DATA_DIR}:/data
    ports:
      - "32400:32400"
    restart: unless-stopped
`,
}
