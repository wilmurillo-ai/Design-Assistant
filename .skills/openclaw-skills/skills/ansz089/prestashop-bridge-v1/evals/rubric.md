# Evaluation Rubric

A pack passes review when:
- endpoints match across skill, OpenAPI, and examples
- RS256 is the only JWT algorithm described
- `202 Accepted` is never presented as business success
- MySQL is documented as the source of truth for job status
- all core schemas are strict
- examples are realistic and internally consistent
