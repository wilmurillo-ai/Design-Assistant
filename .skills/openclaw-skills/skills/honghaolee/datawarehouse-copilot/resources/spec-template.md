# Spec 文档模板

> **文档职责**：spec = 做什么（业务需求、数据范围、质量要求、风险约束）。面向业务/产品同学，无需技术背景即可阅读并确认。技术实现细节（JOIN 类型、SQL、存储格式等）在 plan.md 中定义。
>
> `[必填]` 项不可省略；`[可选]` 项无相关内容时删除该节；`[待确认]` 表示信息待用户补充。

---

# Spec：{需求名称}

**版本**：v1.0  
**状态**：待确认 / 已确认  
**创建时间**：{date}  
**负责人**：{owner}

---

## 1. 需求背景 [必填]

### 1.1 业务背景

### 1.2 需求目标
- 目标 1：
- 目标 2：

### 1.3 需求范围
**包含**：
-

**不包含（Out of Scope）**：
-

---

## 2. 数据分层与定位 [必填]

| 属性 | 值 |
|------|----|
| 数据层级 | resources/conventions/project-conventions.md 中分层约定 |
| 所属数据域 | {domain} |
| 所属主题 | {subject} |
| 目标数据库 | {database_name} |
| 目标表名 | {table_name} |

---

## 3. 数据源 [必填]

### 3.1 上游依赖表

| 源表 | 数据库 | 层级 | 业务含义 | 说明 |
|------|--------|------|----------|------|
| {source_table_1} | {db} | ODS | {该表记录什么业务数据} | {comment} |

### 3.2 源系统信息 [可选]

| 源系统 | 类型 | 接入方式 | 接入频率 |
|--------|------|----------|----------|
| {system_name} | 业务数据库 / 消息队列 / 用户行为埋点 | {接入工具} | 实时 / 每小时 / 每日 |

---

## 4. 目标表设计 [必填]

### 4.1 表基本信息

> 存储格式、压缩格式等技术配置由技术规范（platform-conventions / project-conventions）统一决定，无需在此声明。

| 属性 | 值 |
|------|----|
| 表名 | {table_name} |
| 中文名 | {table_chinese_name} |
| 表描述 | {table_comment} |
| 数据保留策略 | 保留 {N} 天 / 永久保留 |

### 4.2 字段设计

| 字段名 | 业务含义 | 来源字段 | 转换说明 | 是否必填 |
|--------|----------|----------|----------|----------|
| {col_1} | {用自然语言描述该字段的业务含义} | {source_col} | 直接映射 / {用自然语言描述转换规则} | 是 / 否 |

> 分区字段参考公约规范

### 4.3 分区设计

| 分区字段 | 业务含义 | 分区策略 |
|----------|----------|----------|
| pt | 数据日期分区 | 按天 |

### 4.4 业务唯一性约束

- 业务唯一键：{col_a} + {col_b}
- 说明：{唯一性的业务含义，如：同一用户同一天只有一条汇总记录}

---

## 5. 数据范围与业务规则 [必填]

> 本节聚焦「做什么」，用业务语言描述数据口径和计算规则，不包含 SQL 实现细节（SQL 在 plan.md 中定义）。

### 5.1 更新策略

| 属性 | 值 |
|------|----|
| 更新方式 | 全量覆盖 / 增量追加 / 按业务键更新 |
| 调度周期 | 每日 / 每小时 / 实时 |
| 调度时间 | {时间描述，如：每天凌晨 2 点} |
| 数据时效要求 | T+{N}，即业务发生后 {N} 天内可查询 |

### 5.2 数据范围

- **时间窗口**：{描述处理哪个时间范围的数据，如：每次处理前一日 00:00–23:59 产生的记录}
- **业务口径**：{描述纳入统计的业务条件，如：仅统计状态为"已完成"的订单，退款订单不计入}
- **数据口径举例**：{给出 1–2 个具体例子，帮助业务同学验证口径理解，如：用户 A 在 2024-01-01 下单 3 笔均完成，则该日记录 amount=3 笔、gmv=合计金额}

### 5.3 核心计算规则

> 用自然语言描述每个指标或字段的计算逻辑，无需写 SQL。

- **{指标/字段名}**：{计算规则，如：统计用户当日所有已完成订单的实付金额之和，退款金额不扣减}
- **{指标/字段名}**：{计算规则}

### 5.4 数据关系说明

> 描述各上游数据表之间的业务关联关系，无需写 JOIN 语法。

- {描述表与表之间的业务关联，如：用户信息表与订单表通过用户 ID 关联，取用户的注册渠道属性}
- {描述数据筛选逻辑，如：仅保留注册时间超过 30 天的用户对应的订单}

---

## 6. 风险与约束 [必填]

### 6.1 数据质量风险

| 风险描述 | 触发条件 | 影响范围 | 预期处理方式 |
|----------|----------|----------|----------|
| {如：上游订单表存在延迟到达的数据} | {如：业务高峰期订单写入延迟超过 1 小时} | {如：当日汇总数据偏低，需次日补跑} | {如：告警通知，人工确认后补数} |
| {如：源表存在重复记录} | {如：上游系统故障导致重复写入} | {如：指标虚高} | {如：去重后入库，阻断写入} |

### 6.2 业务规则风险

| 风险描述 | 涉及字段/指标 | 缓解方案 |
|----------|--------------|----------|
| {如：退款口径存在争议，是否应从当日 GMV 中扣减} | {gmv} | {如：与业务方确认后锁定口径，记录在本文档} |
| {如：历史数据迁移兼容性：旧表无 channel 字段} | {channel} | {如：历史分区默认填充"unknown"} |

### 6.3 合规与权限约束 [可选]

| 类别 | 约束内容 |
|------|----------|
| 数据权限 | 读权限：{team/user}；写权限：{team/user} |
| 脱敏要求 | {如：手机号字段需掩码处理，仅保留前 3 位和后 4 位} |

### 6.4 验收标准（AC）

> 业务侧可验证的验收条件，技术层面的 DoD 在 task.md 中定义。

- [ ] {AC-1}：{如：当日数据量与业务方人工核对结果误差 < 1%}
- [ ] {AC-2}：{如：关键指标与报表系统数据一致}
- [ ] {AC-3}：{如：业务同学可在数据门户查询到 T+1 数据}

---

## 7. 调度依赖 [必填]

### 7.1 上游依赖

| 依赖任务 | 依赖类型 | 说明 |
|----------|----------|------|
| {upstream_job} | 强依赖 / 弱依赖 | {说明} |

### 7.2 下游通知

| 下游任务 | 通知方式 | 说明 |
|----------|----------|------|
| {downstream_job} | 自动触发 / 通知负责人 | {说明} |

---

## 8. 非功能性需求 [可选]

| 类别 | 要求 |
|------|------|
| 性能 | 调度任务应在 {N} 分钟内完成 |
| 存储 | 预估数据量 {N} 行，增长趋势 {描述} |
| 监控 | 失败告警：{email/webhook}；延迟告警：超 {N} 分钟 |

---

## 9. 待确认事项和风险说明

- [ ] {问题 1}：{描述，等待谁确认}
- [ ] {问题 2}：{描述}

---

## 10. 变更记录 [可选]

| 版本 | 时间 | 变更内容 | 变更人 |
|------|------|----------|--------|
| v1.0 | {date} | 初始版本 | {name} |