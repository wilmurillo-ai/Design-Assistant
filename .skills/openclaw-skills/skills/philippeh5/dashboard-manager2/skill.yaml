id: dashboard-manager
name: "Dashboard Manager"
entrypoint: "index.js"
category: "workspace"
tools:
  - name: getPendingNotes
    description: "Récupère les notes"
    parameters: { type: object, properties: {} }
  - name: addLog
    description: "Ajoute un log"
    parameters:
      type: object
      properties:
        action: { type: string }
      required: [action]