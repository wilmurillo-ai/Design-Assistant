# vod_scene_aigc_image — Parameters & Examples
> This file is generated by splitting references, corresponding script: `scripts/vod_scene_aigc_image.py`

### ⚠️ Common Parameter Errors

| Incorrect Usage | Correct Usage | Description |
|---------|---------|------|
| `change-clothes ...` | `generate --scene-type change_clothes` | Use the `generate` subcommand for scene-based image generation |
| `--prompt "background"` (product image) | `--product-prompt "background"` | Use `--product-prompt` for product image prompts; use `--change-prompt` for outfit changes |
| `--storage-mode Permanent` | `--output-storage-mode Permanent` | Storage mode parameter name starts with `--output-` |
| Generate without providing FileId | Upload image first to get FileId | Must ask the user when no image is provided; do not use placeholders |

## Parameter Reference
## §11 Scene-Based AIGC Image Generation Parameters (vod_scene_aigc_image.py)


Based on the `CreateSceneAigcImageTask` API, supports scene-based image generation including AI outfit change, AI product image generation, and AI outpainting.

### Common Parameters

| Parameter | Type | Description |
|------|------|------|
| `--sub-app-id` | int | VOD application ID (required; can also be set via environment variable `TENCENTCLOUD_VOD_SUB_APP_ID`) |
| `--region` | string | Region, default `ap-guangzhou` |
| `--json` | flag | JSON format output |
| `--dry-run` | flag | Preview request parameters without calling the API |
| `--no-wait` | flag | Submit task only, do not wait for result (default: auto-wait) |
| `--max-wait` | int | Maximum wait time (seconds, default 600) |

### generate Parameters (Create Scene-Based Image Generation Task)

> ⚠️ **Input File Handling Rules**:
> - User provides a **local image path** → Upload first using `vod_upload.py upload --file <path>`, then fill the returned FileId into `"File:<FileId>"`
> - User provides an **image URL** → Use directly in `"Url:<URL>"` format
> - User **provides no image/FileId** → **FileId is a required parameter; you must ask the user to provide a FileId or local file path**; do not generate commands with placeholders

#### Required Parameters

| Parameter | Type | Required | Description |
|------|------|------|------|
| `--sub-app-id` | int | ✅ | VOD application ID |
| `--scene-type` | enum | ✅ | AI image generation scene type: `change_clothes` (AI outfit change), `product_image` (AI product image), `outpainting` (AI outpainting) |

#### Input File Parameters

| Parameter | Type | Required | Description |
|------|------|------|------|
| `--input-files` | string list | - | Input image list, format: `File:FileId` or `Url:URL` |

> **Input requirements per scene**:
> - AI outfit change scene: Only 1 model image can be provided
> - AI product image scene: 1–10 images of the same product from different angles
> - AI outpainting scene: 1 image to be expanded

#### AI Outfit Change Scene Parameters (scene-type=change_clothes)

| Parameter | Type | Required | Description |
|------|------|------|------|
| `--clothes-files` | string list | - | Clothing image list, format: `File:FileId` or `Url:URL`, up to 4 images |
| `--change-prompt` | string | - | Prompt for AI outfit change |

#### AI Product Image Scene Parameters (scene-type=product_image)

| Parameter | Type | Required | Description |
|------|------|------|------|
| `--product-prompt` | string | - | Prompt for generating the image background; if omitted, inspiration is auto-generated internally |
| `--negative-prompt` | string | - | Prompt to prevent the model from generating certain content |
| `--product-desc` | string | - | Detailed description of the product to help generate more accurate images |
| `--more-requirement` | string | - | Special requirements; use this field to pass in any special requests |
| `--output-image-count` | int | - | Number of images to generate (1–10, default 1) |

#### Output Configuration Parameters (Output Config)

| Parameter | Type | Description |
|------|------|------|
| `--output-storage-mode` | enum | Storage mode: `Permanent` (permanent storage), `Temporary` (temporary storage, default) |
| `--media-name` | string | Output filename (max 64 characters) |
| `--class-id` | int | Category ID (default 0) |
| `--expire-time` | string | Expiration time (ISO 8601 format, e.g. `2026-12-31T23:59:59+08:00`) |
| `--aspect-ratio` | enum | Output image aspect ratio: `1:1`, `3:2`, `2:3`, `3:4`, `4:3`, `4:5`, `5:4`, `16:9`, `9:16`, `21:9` |
| `--image-width` | int | Output image width (valid for AI outpainting scene only) |
| `--image-height` | int | Output image height (valid for AI outpainting scene only) |
| `--resolution` | enum | Output resolution: `1K`, `2K`, `4K` (valid for AI outfit change scene only) |

#### Encoding Configuration Parameters (Valid for AI Outfit Change Scene Only)

| Parameter | Type | Description |
|------|------|------|
| `--encode-format` | enum | Image format: `JPEG`, `PNG` |
| `--encode-quality` | int | Relative image quality (1–100, relative to original image quality) |

#### Other Optional Parameters

| Parameter | Type | Description |
|------|------|------|
| `--session-id` | string | Deduplication identifier (max 50 characters; duplicate within 3 days returns an error) |
| `--session-context` | string | Source context for passing through user request information (max 1000 characters) |
| `--tasks-priority` | int | Task priority (–10 to 10; higher value means higher priority, default 0) |
| `--ext-info` | string | Reserved field; use for special purposes |

### query Parameters (Query Task)

| Parameter | Type | Required | Description |
|------|------|------|------|
| `--task-id` | string | ✅ | Task ID |
| `--sub-app-id` | int | - | Sub-application ID (can also be set via environment variable `TENCENTCLOUD_VOD_SUB_APP_ID`) |
| `--region` | string | - | Region, default `ap-guangzhou` |
| `--json` | flag | - | JSON format output |
| `--dry-run` | flag | - | Preview request parameters without calling the API |

### Data Structure Reference

#### Scene Info Scene Information

```json
{
  "Type": "change_clothes",
  "Change Clothes Config": {
    "Clothes File Infos": [
      {"Type": "File", "FileId": "xxx"}
    ],
    "Prompt": "Open the shirt a little"
  }
}
```

| Field | Type | Description |
|------|------|------|
| `Type` | string | Scene type: `change_clothes`, `product_image`, `outpainting` |
| `ChangeClothesConfig` | object | AI outfit change configuration (valid when Type=change_clothes) |
| `ProductImageConfig` | object | AI product image configuration (valid when Type=product_image) |

#### Change Clothes Config Outfit Change Configuration

| Field | Type | Description |
|------|------|------|
| `ClothesFileInfos` | array | Clothing image list, up to 4 images |
| `Prompt` | string | AI outfit change prompt |

#### Product Image Config Product Image Configuration

| Field | Type | Description |
|------|------|------|
| `Prompt` | string | Background generation prompt |
| `NegativePrompt` | string | Negative prompt |
| `ProductDesc` | string | Product description |
| `MoreRequirement` | string | Special requirements |
| `OutputImageCount` | int | Number of images to generate (1–10) |

#### File Infos File Information

```json
[
  {"Type": "File", "FileId": "3704211xxx509819"},
  {"Type": "Url", "Url": "https://example.com/img.jpg"}
]
```

| Field | Type | Description |
|------|------|------|
| `Type` | string | Type: `File` (VOD media file), `Url` (accessible URL) |
| `FileId` | string | Media file ID (required when Type=File) |
| `Url` | string | File URL (required when Type=Url) |

#### Output Config Output Configuration

```json
{
  "Storage Mode": "Temporary",
  "Media Name": "output.jpg",
  "ClassId": 0,
  "Expire Time": "2026-12-31T23:59:59+08:00",
  "Aspect Ratio": "1:1",
  "Resolution": "2K",
  "Encode Config": {
    "Format": "JPEG",
    "Quality": 90
  }
}
```

| Field | Type | Description |
|------|------|------|
| `StorageMode` | string | Storage mode |
| `MediaName` | string | Output filename |
| `ClassId` | int | Category ID |
| `ExpireTime` | string | Expiration time |
| `AspectRatio` | string | Aspect ratio |
| `ImageWidth` | int | Image width (outpainting scene) |
| `ImageHeight` | int | Image height (outpainting scene) |
| `Resolution` | string | Resolution (outfit change scene) |
| `EncodeConfig` | object | Encoding configuration (outfit change scene) |

### Task Status Reference

| Status | Description |
|------|------|
| `PROCESSING` | Processing |
| `SUCCESS` | Success |
| `FAIL` | Failed |

### API Mapping

| Function | API | Documentation |
|------|---------|---------|
| Create scene-based AIGC image task | `CreateSceneAigcImageTask` | https://cloud.tencent.com/document/api/266/126968 |
| Query task details | `DescribeTaskDetail` | https://cloud.tencent.com/document/api/266/33431 |

### Error Code Reference

| Error Type | Cause | Recommended Action |
|---------|------|---------|
| Missing Parameter | Missing required parameter | Check that Sub AppId and Scene Info are provided |
| Invalid Parameter Value.Scene Type | Unsupported scene type | Use a supported scene type: change_clothes, product_image, outpainting |
| Invalid Parameter Value.Aspect Ratio | Unsupported aspect ratio | Check the list of supported aspect ratios for the scene |
| Invalid Parameter Value.Resolution | Unsupported resolution | Outfit change scene supports 1K/2K/4K |
| Invalid Parameter Value.File Infos | Input image count does not meet requirements | Outfit change scene requires 1 image; product image scene requires 1–10 images |
| Invalid Parameter Value.Clothes File Infos | Clothing image count exceeds limit | Up to 4 clothing images are supported |
| Invalid Parameter | Parameter format error | Check that File Infos format is Type:Value |
| Limit Exceeded | Rate limit exceeded | Default limit: 20 requests/second |

---


---


## Usage Examples
## §11 Scene-Based AIGC Image Generation (vod_scene_aigc_image.py)


Based on the `CreateSceneAigcImageTask` API, supports scene-based image generation including AI outfit change, AI product image generation, and AI outpainting.

### §11.1 AI Outfit Change Scene

#### Basic Outfit Change (Using VOD File)
```bash
python scripts/vod_scene_aigc_image.py generate \
    --sub-app-id 251007502 \
    --scene-type change_clothes \
    --input-files "File:3704211509819" \
    --clothes-files "File:3704211509820" \
    --change-prompt "Open the shirt a little"
```

#### Outfit Change Using URL Images
```bash
python scripts/vod_scene_aigc_image.py generate \
    --sub-app-id 251007502 \
    --scene-type change_clothes \
    --input-files "Url:https://example.com/model.jpg" \
    --clothes-files "Url:https://example.com/shirt.jpg" \
    --change-prompt "Change to a casual style"
```

#### Outfit Change with Multiple Clothing Items
```bash
python scripts/vod_scene_aigc_image.py generate \
    --sub-app-id 251007502 \
    --scene-type change_clothes \
    --input-files "File:3704211509819" \
    --clothes-files "File:3704211509820" "File:3704211509821" "File:3704211509822" \
    --change-prompt "Match this outfit"
```

#### Outfit Change with Specified Output Resolution
```bash
python scripts/vod_scene_aigc_image.py generate \
    --sub-app-id 251007502 \
    --scene-type change_clothes \
    --input-files "File:3704211509819" \
    --clothes-files "File:3704211509820" \
    --change-prompt "Open the shirt a little" \
    --resolution 4K \
    --aspect-ratio 3:4
```

#### Outfit Change with Specified Encoding Format
```bash
python scripts/vod_scene_aigc_image.py generate \
    --sub-app-id 251007502 \
    --scene-type change_clothes \
    --input-files "File:3704211509819" \
    --clothes-files "File:3704211509820" \
    --change-prompt "Open the shirt a little" \
    --encode-format JPEG \
    --encode-quality 95
```

#### Outfit Change and Wait for Completion
```bash
python scripts/vod_scene_aigc_image.py generate \
    --sub-app-id 251007502 \
    --scene-type change_clothes \
    --input-files "File:3704211509819" \
    --clothes-files "File:3704211509820" \
    --change-prompt "Open the shirt a little" \

```

#### Store Generated Image Permanently
```bash
python scripts/vod_scene_aigc_image.py generate \
    --sub-app-id 251007502 \
    --scene-type change_clothes \
    --input-files "File:3704211509819" \
    --clothes-files "File:3704211509820" \
    --change-prompt "Open the shirt a little" \
    --output-storage-mode Permanent \
    --media-name "outfit-change-result"
```

---

### §11.2 AI Product Image Scene

#### Basic Product Image Generation (Single Product Image)
```bash
python scripts/vod_scene_aigc_image.py generate \
    --sub-app-id 251007502 \
    --scene-type product_image \
    --input-files "File:3704211509819" \
    --product-prompt "Red Chinese New Year background with festive atmosphere"
```

#### Multi-Angle Product Image Generation
```bash
python scripts/vod_scene_aigc_image.py generate \
    --sub-app-id 251007502 \
    --scene-type product_image \
    --input-files "File:3704211509819" "File:3704211509820" "File:3704211509821" \
    --product-prompt "Blue tech background with futuristic feel"
```

#### Product Image with Detailed Description
```bash
python scripts/vod_scene_aigc_image.py generate \
    --sub-app-id 251007502 \
    --scene-type product_image \
    --input-files "File:3704211509819" \
    --product-prompt "Forest background with natural lighting" \
    --product-desc "A premium mineral water bottle with a transparent glass body" \
    --more-requirement "Add sunlight effect to highlight the texture of the bottle"
```

#### Batch Generate Multiple Product Images
```bash
python scripts/vod_scene_aigc_image.py generate \
    --sub-app-id 251007502 \
    --scene-type product_image \
    --input-files "File:3704211509819" \
    --product-prompt "Various scene backgrounds" \
    --output-image-count 10 \
    --aspect-ratio 1:1
```

#### Specify Negative Prompt
```bash
python scripts/vod_scene_aigc_image.py generate \
    --sub-app-id 251007502 \
    --scene-type product_image \
    --input-files "File:3704211509819" \
    --product-prompt "Clean white background" \
    --negative-prompt "cluttered, blurry, low quality" \
    --product-desc "An elegant watch"
```

#### Specify Category and Expiration Time
```bash
python scripts/vod_scene_aigc_image.py generate \
    --sub-app-id 251007502 \
    --scene-type product_image \
    --input-files "File:3704211509819" \
    --product-prompt "Business setting" \
    --class-id 100 \
    --expire-time "2026-12-31T23:59:59+08:00" \
    --output-storage-mode Permanent
```

---

### §11.3 AI Outpainting Scene

#### Basic Outpainting (Aspect Ratio Only)
```bash
python scripts/vod_scene_aigc_image.py generate \
    --sub-app-id 251007502 \
    --scene-type outpainting \
    --input-files "File:3704211509819" \
    --aspect-ratio 16:9
```

#### Outpainting with Specific Dimensions
```bash
python scripts/vod_scene_aigc_image.py generate \
    --sub-app-id 251007502 \
    --scene-type outpainting \
    --input-files "File:3704211509819" \
    --aspect-ratio 16:9 \
    --image-width 1920 \
    --image-height 1080
```

#### Outpainting to Portrait Aspect Ratio
```bash
python scripts/vod_scene_aigc_image.py generate \
    --sub-app-id 251007502 \
    --scene-type outpainting \
    --input-files "File:3704211509819" \
    --aspect-ratio 9:16 \
    --image-width 1080 \
    --image-height 1920
```

---

### §11.4 Query Task

#### Basic Query
```bash
python scripts/vod_scene_aigc_image.py query \
    --task-id "251007502-Scene Aigc Image Task-abc123"
```

#### Query with Sub-Application ID
```bash
python scripts/vod_scene_aigc_image.py query \
    --task-id "251007502-Scene Aigc Image Task-abc123" \
    --sub-app-id 251007502
```

#### JSON Format Output
```bash
python scripts/vod_scene_aigc_image.py query \
    --task-id "251007502-Scene Aigc Image Task-abc123" \
    --json
```

---