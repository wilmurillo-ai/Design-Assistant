# vod_process_media — Detailed Parameters and Examples
> This file is generated by splitting references, corresponding script: `scripts/vod_process_media.py`

### ⚠️ Common Parameter Mistakes

| Incorrect Usage | Correct Usage | Notes |
|---------|---------|------|
| `transcode --template same` | `transcode --quality same` | Use `--quality` for TESHD transcoding |
| `scene-transcode --resolution 720` | `transcode --quality hd` | Use `transcode --quality` for TESHD; use `scene-transcode --scene` for scene-based transcoding |
| `process --file-id <id> --animated-graphic-task-set ...` | `gif --file-id <id> --definition 10` | Use the dedicated `gif` subcommand for GIF conversion; there is no `process` subcommand |
| `tccli vod Process Media --Ai Analysis Task ...` | `python scripts/vod_process_media.py ai-analysis --file-id <id> --definition <id>` | AI content analysis must use the `ai-analysis` subcommand — **tccli is forbidden**; `--definition` is required |
| `tccli vod Process Media --Ai Recognition Task ...` | `python scripts/vod_process_media.py ai-recognition --file-id <id> --definition <id>` | AI content recognition must use the `ai-recognition` subcommand — **tccli is forbidden**; `--definition` is required |
| `adaptive-streaming --subtitle-ids "z8gnlH,abc123"` | `adaptive-streaming --subtitle-set "z8gnlH,abc123"` | **🚨 Use `--subtitle-set` to bind subtitles for adaptive bitrate streaming — not `--subtitle-ids`** |

---

## Table of Contents

- [Common Parameters](#common-parameters-shared-by-all-subcommands)
- [procedure — Task Flow Processing](#procedure-parameters-task-flow-processing)
- [transcode — TESHD Transcoding](#transcode-parameters-teshd-transcoding)
- [remux — Remuxing](#remux-parameters-remuxing)
- [enhance — Video Enhancement](#enhance-parameters-video-enhancement)
- [snapshot — Screenshot](#snapshot-parameters-screenshot)
- [gif — Animated GIF](#gif-parameters-animated-gif)
- [cover-by-snapshot — Screenshot as Cover](#cover-by-snapshot-parameters-screenshot-as-cover)
- [adaptive-streaming — Adaptive Bitrate Streaming](#adaptive-streaming-parameters-adaptive-bitrate-streaming)
- [sample-snapshot — Sample Snapshot](#sample-snapshot-parameters-sample-snapshot)
- [image-sprite — Image Sprites](#image-sprite-parameters-image-sprites)
- [ai-review — AI Content Review](#ai-review-parameters-ai-content-review)
- [ai-analysis — AI Content Analysis](#ai-analysis-parameters-ai-content-analysis)
- [ai-recognition — AI Content Recognition](#ai-recognition-parameters-ai-content-recognition)
- [scene-transcode — Scene Transcoding](#scene-transcode-parameters-scene-transcoding)
- [Usage Examples](#usage-examples)

---

## Parameter Reference

> Subcommand list: `procedure`, `transcode`, `remux`, `enhance`, `scene-transcode`, `snapshot`, `gif`, `sample-snapshot`, `image-sprite`, `cover-by-snapshot`, `adaptive-streaming`, `ai-review`, `ai-analysis`, `ai-recognition`

### Common Parameters (Shared by All Subcommands)

> The following parameters are available in all subcommands and are not repeated in individual parameter tables.

| Parameter | Type | Description |
|------|------|------|
| `--sub-app-id` | int | Sub-application ID (required for accounts created after 2023-12-25; can also be set via the `TENCENTCLOUD_VOD_SUB_APP_ID` environment variable) |
| `--region` | string | Region (default: `ap-guangzhou`) |
| `--no-wait` | flag | Submit the task only, without waiting for the result (by default, the script waits automatically) |
| `--max-wait` | int | Maximum wait time in seconds (default: 600) |
| `--json` | flag | Output in JSON format |
| `--verbose` / `-v` | flag | Output verbose information (including full API response) |
| `--dry-run` | flag | Preview request parameters without actually executing |

---

### procedure Parameters (Task Flow Processing)

> For common parameters, see [Common Parameters](#common-parameters-shared-by-all-subcommands) above.

| Parameter | Type | Required | Description |
|------|------|------|------|
| `--file-id` | string | ✅ | Media file ID |
| `--procedure` | string | ✅ | Task flow name |
| `--media-storage-path` | string | - | Media storage path (only available for sub-applications in FileID+Path mode) |
| `--tasks-notify-mode` | enum | - | Task flow status change notification mode: `Finish` / `Change` / `None` |
| `--session-id` | string | - | Deduplication identifier (returns an error if duplicated within three days) |
| `--ext-info` | string | - | Extended information |
| `--tasks-priority` | int | - | Task priority (-10 to 10) |

---

### transcode Parameters (TESHD Transcoding)

> For common parameters, see [Common Parameters](#common-parameters-shared-by-all-subcommands) above.

| Parameter | Type | Required | Description |
|------|------|------|------|
| `--file-id` | string | ✅ | Media file ID |
| `--quality` | enum | - | Transcoding quality: `same` (same as source, default) / `flu` (smooth 360P) / `sd` (standard definition 540P) / `hd` (high definition 720P) |
| `--watermark-set` | string | - | Watermark template ID list (comma-separated, e.g., `10001,10002`) |
| `--session-context` | string | - | Pass-through source context (up to 1000 characters) |
| `--tasks-priority` | int | - | Task priority (-10 to 10) |

---

### remux Parameters (Remuxing)

> For common parameters, see [Common Parameters](#common-parameters-shared-by-all-subcommands) above.

| Parameter | Type | Required | Description |
|------|------|------|------|
| `--file-id` | string | ✅ | Media file ID |
| `--target-format` | enum | ✅ | Target container format: `mp4` / `hls` |
| `--session-context` | string | - | Source context for passing through user request information |
| `--tasks-priority` | int | - | Task priority (-10 to 10) |

---

### enhance Parameters (Video Enhancement)

> Large model enhancement V2, supporting denoising + super-resolution + comprehensive enhancement. Suitable for general, anime, and live-action drama scenarios, from 720P to 4K.  
> For common parameters, see [Common Parameters](#common-parameters-shared-by-all-subcommands) above.

| Parameter | Type | Required | Default | Description |
|------|------|------|--------|------|
| `--file-id` | string | ✅ | - | Media file ID |
| `--scene` | enum | - | `general` | Enhancement scene: `general` (general) / `anime` (anime/comics) / `live_action` (live-action/drama) |
| `--resolution` | enum | - | `1080` | Target resolution: `720` / `1080` / `2k` / `4k` |
| `--session-context` | string | - | - | Source context (up to 1000 characters) |
| `--tasks-priority` | int | - | - | Task priority (-10 to 10) |

**Scene Descriptions:**

| Scene Identifier | Applicable Content |
|---------|---------|
| `general` | General video (default) |
| `anime` | Anime / comics |
| `live_action` | Live-action / drama / film and television |

---

### snapshot Parameters (Screenshot)

> For common parameters, see [Common Parameters](#common-parameters-shared-by-all-subcommands) above.

| Parameter | Type | Required | Description |
|------|------|------|------|
| `--file-id` | string | ✅ | Media file ID |
| `--definition` | int | ✅ | Screenshot template ID |
| `--ext-time-offset-set` | string | - | Screenshot time point list (comma-separated, e.g., `5s,10s,15s` or `10pct,20pct`) |
| `--watermark-set` | string | - | Watermark template ID list (comma-separated) |
| `--media-storage-path` | string | - | Media storage path (only available for sub-applications in FileID+Path mode) |
| `--session-id` | string | - | Deduplication identifier (returns an error if duplicated within three days) |
| `--ext-info` | string | - | Extended information |
| `--tasks-priority` | int | - | Task priority (-10 to 10) |
| `--tasks-notify-mode` | enum | - | Notification mode: `Finish` / `Change` / `None` |

---

### gif Parameters (Animated GIF)

> For common parameters, see [Common Parameters](#common-parameters-shared-by-all-subcommands) above.

| Parameter | Type | Required | Description |
|------|------|------|------|
| `--file-id` | string | ✅ | Media file ID |
| `--definition` | int | ✅ | Animated GIF template ID |
| `--start-time` | float | - | Start time offset (seconds) |
| `--end-time` | float | - | End time offset (seconds) |
| `--media-storage-path` | string | - | Media storage path (only available for sub-applications in FileID+Path mode) |
| `--session-id` | string | - | Deduplication identifier |
| `--ext-info` | string | - | Extended information |
| `--tasks-priority` | int | - | Task priority (-10 to 10) |
| `--tasks-notify-mode` | enum | - | Notification mode: `Finish` / `Change` / `None` |

---

### cover-by-snapshot Parameters (Screenshot as Cover)

> For common parameters, see [Common Parameters](#common-parameters-shared-by-all-subcommands) above.

| Parameter | Type | Required | Description |
|------|------|------|------|
| `--file-id` | string | ✅ | Media file ID |
| `--definition` | int | ✅ | Screenshot template ID |
| `--position-type` | enum | ✅ | Screenshot method: `Time` (time offset) / `Percent` (percentage) |
| `--position-value` | float | ✅ | Screenshot position (in seconds for `Time` mode; 0–100 for `Percent` mode) |
| `--watermark-set` | string | - | Watermark template ID list (comma-separated) |
| `--media-storage-path` | string | - | Media storage path (only available for sub-applications in FileID+Path mode) |
| `--session-id` | string | - | Deduplication identifier |
| `--ext-info` | string | - | Extended information |
| `--tasks-priority` | int | - | Task priority (-10 to 10) |
| `--tasks-notify-mode` | enum | - | Notification mode: `Finish` / `Change` / `None` |

---

### adaptive-streaming Parameters (Adaptive Bitrate Streaming)

> For common parameters, see [Common Parameters](#common-parameters-shared-by-all-subcommands) above.

| Parameter | Type | Required | Description |
|------|------|------|------|
| `--file-id` | string | ✅ | Media file ID |
| `--definition` | int | ✅ | Adaptive bitrate streaming template ID |
| `--subtitle-set` | string | - | Subtitle ID list (comma-separated) ⚠️ Not `--subtitle-ids` |
| `--media-storage-path` | string | - | Media storage path (only available for sub-applications in FileID+Path mode) |
| `--session-id` | string | - | Deduplication identifier |
| `--ext-info` | string | - | Extended information |
| `--tasks-priority` | int | - | Task priority (-10 to 10) |
| `--tasks-notify-mode` | enum | - | Notification mode: `Finish` / `Change` / `None` |

---

### sample-snapshot Parameters (Sample Snapshot)

> For common parameters, see [Common Parameters](#common-parameters-shared-by-all-subcommands) above.

| Parameter | Type | Required | Description |
|------|------|------|------|
| `--file-id` | string | ✅ | Media file ID |
| `--definition` | int | ✅ | Sample snapshot template ID |
| `--watermark-set` | string | - | Watermark template ID list (comma-separated) |
| `--media-storage-path` | string | - | Media storage path (only available for sub-applications in FileID+Path mode) |
| `--session-id` | string | - | Deduplication identifier |
| `--ext-info` | string | - | Extended information |
| `--tasks-priority` | int | - | Task priority (-10 to 10) |
| `--tasks-notify-mode` | enum | - | Notification mode: `Finish` / `Change` / `None` |

---

### image-sprite Parameters (Image Sprites)

> For common parameters, see [Common Parameters](#common-parameters-shared-by-all-subcommands) above.

| Parameter | Type | Required | Description |
|------|------|------|------|
| `--file-id` | string | ✅ | Media file ID |
| `--definition` | int | ✅ | Image sprite template ID |
| `--watermark-set` | string | - | Watermark template ID list (comma-separated) |
| `--media-storage-path` | string | - | Media storage path (only available for sub-applications in FileID+Path mode) |
| `--session-id` | string | - | Deduplication identifier |
| `--ext-info` | string | - | Extended information |
| `--tasks-priority` | int | - | Task priority (-10 to 10) |
| `--tasks-notify-mode` | enum | - | Notification mode: `Finish` / `Change` / `None` |

---

### ai-review Parameters (AI Content Review)

> ⚠️ Not recommended; use the `ReviewAudioVideo` API instead.  
> For common parameters, see [Common Parameters](#common-parameters-shared-by-all-subcommands) above.

| Parameter | Type | Required | Description |
|------|------|------|------|
| `--file-id` | string | ✅ | Media file ID |
| `--definition` | int | ✅ | Audio/video review template ID |
| `--media-storage-path` | string | - | Media storage path (only available for sub-applications in FileID+Path mode) |
| `--session-id` | string | - | Deduplication identifier |
| `--ext-info` | string | - | Extended information |
| `--tasks-priority` | int | - | Task priority (-10 to 10) |
| `--tasks-notify-mode` | enum | - | Notification mode: `Finish` / `Change` / `None` |

---

### ai-analysis Parameters (AI Content Analysis)

> **A content analysis template ID must be specified via `--definition`** — tccli is forbidden.  
> For common parameters, see [Common Parameters](#common-parameters-shared-by-all-subcommands) above.


| Parameter | Type | Required | Description |
|------|------|------|------|
| `--file-id` | string | ✅ | Media file ID |
| `--definition` | int | ✅ | Video content analysis template ID |
| `--media-storage-path` | string | - | Media storage path (only available for sub-applications in FileID+Path mode) |
| `--session-id` | string | - | Deduplication identifier |
| `--ext-info` | string | - | Extended information |
| `--tasks-priority` | int | - | Task priority (-10 to 10) |
| `--tasks-notify-mode` | enum | - | Notification mode: `Finish` / `Change` / `None` |

---

### ai-recognition Parameters (AI Content Recognition)

> **Must specify the intelligent recognition template ID via `--definition`**; using tccli is prohibited.  
> For common parameters, see [Common Parameters](#common-parameters-shared-by-all-subcommands) above.

| Parameter | Type | Required | Description |
|------|------|------|------|
| `--file-id` | string | ✅ | Media file ID |
| `--definition` | int | ✅ | Video intelligent recognition template ID |
| `--media-storage-path` | string | - | Media storage path (only available for sub-applications in FileID+Path mode) |
| `--session-id` | string | - | Deduplication identifier |
| `--ext-info` | string | - | Extended information |
| `--tasks-priority` | int | - | Task priority (-10 to 10) |
| `--tasks-notify-mode` | enum | - | Notification mode: `Finish` / `Change` / `None` |

---

### scene-transcode Parameters (Scene Transcoding)

> Based on Top Speed Codec technology, optimized for short drama / e-commerce / feed stream scenarios.  
> For common parameters, see [Common Parameters](#common-parameters-shared-by-all-subcommands) above.

| Parameter | Type | Required | Default | Description |
|------|------|------|--------|------|
| `--file-id` | string | ✅ | - | Media file ID |
| `--scene` | enum | ✅ | - | Scene: `short_drama` (short drama) / `ecommerce` (e-commerce) / `feed` (feed stream) |
| `--resolution` | enum | - | `1080` | Output resolution: `1080` / `720` / `480` |
| `--priority` | enum | - | `best` | Transcoding strategy: `best` (overall best) / `quality` (quality first) / `bitrate` (bitrate first) |
| `--session-context` | string | - | - | Source context (max 1000 characters) |
| `--tasks-priority` | int | - | - | Task priority (-10 to 10) |

**Scene Descriptions:**

| Scene | Identifier | Core Optimization |
|------|------|----------|
| Short Drama | `short_drama` | Improves character recognition, optimizes video size |
| E-commerce | `ecommerce` | Efficient compression, reduces storage and distribution costs |
| Feed Stream | `feed` | Improves playback smoothness, reduces loading time |

**Scene Transcoding Template IDs (for reference; automatically selected by the script):**

| Scene | Resolution | best | quality | bitrate |
|------|--------|------|---------|---------|
| short_drama | 1080P | 101300 | 101301 | 101302 |
| short_drama | 720P | 101303 | 101304 | 101305 |
| short_drama | 480P | 101306 | 101307 | 101308 |
| ecommerce | 1080P | 101309 | 101310 | 101311 |
| ecommerce | 720P | 101312 | 101313 | 101314 |
| ecommerce | 480P | 101315 | 101316 | 101317 |
| feed | 1080P | 101318 | 101319 | 101320 |
| feed | 720P | 101321 | 101322 | 101323 |
| feed | 480P | 101324 | 101325 | 101326 |

---

## Usage Examples

### Template ID Quick Reference

#### Top Speed Codec Transcoding (transcode)

| `--quality` | Template ID | Resolution |
|-------------|---------|--------|
| `same` (default) | 100800 | Same as source |
| `flu` | 100810 | 360P |
| `sd` | 100820 | 540P |
| `hd` | 100830 | 720P |

#### Remuxing (remux)

| `--target-format` | Template ID |
|-------------------|---------|
| `mp4` | 875 |
| `hls` | 876 |

#### Video Enhancement (enhance)

| Scene | Resolution | Template ID |
|------|--------|---------|
| `general` | 720P | 101410 |
| `general` | 1080P | 101430 |
| `general` | 2K | 101450 |
| `general` | 4K | 101470 |
| `anime` | 720P | 101510 |
| `anime` | 1080P | 101530 |
| `anime` | 2K | 101550 |
| `anime` | 4K | 101570 |
| `live_action` | 720P | 101520 |
| `live_action` | 1080P | 101540 |
| `live_action` | 2K | 101560 |
| `live_action` | 4K | 101580 |

---

### §5.1 Top Speed Codec Transcoding (transcode)

> ⚠️ Use `transcode --quality` for Top Speed Codec; use `scene-transcode --scene` for scene transcoding (short drama / e-commerce / feed stream) — these are completely different.

```bash
# Same as source (default, high quality low bitrate)
python scripts/vod_process_media.py transcode --file-id <FileId>

# HD 720P
python scripts/vod_process_media.py transcode --file-id <FileId> --quality hd

# SD 540P (cost-saving)
python scripts/vod_process_media.py transcode --file-id <FileId> --quality sd

# Smooth 360P (bandwidth-saving)
python scripts/vod_process_media.py transcode --file-id <FileId> --quality flu

# JSON output
python scripts/vod_process_media.py transcode --file-id <FileId> --quality hd --json
```

---

### §5.1.1 Remuxing (remux)

> ⚠️ **Parameter distinction**:
> - `--target-format`: **Required**, target container format (`mp4` or `hls`)
> - `--tasks-priority`: Task priority (-10 to 10), **note: this is NOT `--priority`** — `--priority` is a scene-transcode-specific parameter

```bash
# Remux to MP4
python scripts/vod_process_media.py remux --file-id 5145403721233902989 --target-format mp4

# Remux to HLS
python scripts/vod_process_media.py remux --file-id 5145403721233902989 --target-format hls

# Remux to HLS + set task priority + pass through source context
python scripts/vod_process_media.py remux --file-id 5145403721233902989 \
    --target-format hls --tasks-priority 5 --session-context "remux-ctx"

# JSON output
python scripts/vod_process_media.py remux --file-id 5145403721233902989 --target-format mp4 --json
```

---

### §5.2 Video Enhancement (enhance)

```bash
# General scene 1080P (default)
python scripts/vod_process_media.py enhance --file-id 5145403721233902989

# Live action / short drama scene 720P
python scripts/vod_process_media.py enhance --file-id 5145403721233902989 --scene live_action --resolution 720

# Live action / short drama scene 4K
python scripts/vod_process_media.py enhance --file-id 5145403721233902989 --scene live_action --resolution 4k

# Anime scene 2K
python scripts/vod_process_media.py enhance --file-id 5145403721233902989 --scene anime --resolution 2k

# Pass through context + set priority
python scripts/vod_process_media.py enhance --file-id 5145403721233902989 --scene live_action --resolution 4k \
    --session-context "enhance-ctx" --tasks-priority 3 --json
```

---

### §5.3 Scene Transcoding (scene-transcode)

> Rule: `--scene` is required, `--resolution` defaults to `1080`, `--priority` defaults to `best`, and the script automatically selects the corresponding template ID.

```bash
# Short drama 1080P overall best (all defaults)
python scripts/vod_process_media.py scene-transcode --file-id 5285485487985271487 --scene short_drama

# E-commerce 720P bitrate first
python scripts/vod_process_media.py scene-transcode --file-id 5285485487985271487 \
    --scene ecommerce --resolution 720 --priority bitrate

# Feed stream 480P quality first
python scripts/vod_process_media.py scene-transcode --file-id 5285485487985271487 \
    --scene feed --resolution 480 --priority quality

# Specify sub-application + JSON output
python scripts/vod_process_media.py scene-transcode --file-id 5285485487985271487 \
    --scene short_drama --sub-app-id 251007502 --json

# dry-run preview request
python scripts/vod_process_media.py scene-transcode --file-id 5285485487985271487 \
    --scene short_drama --resolution 1080 --priority best --dry-run
```

---

### §5.4 AI Content Analysis (ai-analysis)

```bash
# Basic analysis
python scripts/vod_process_media.py ai-analysis --file-id 5145403721233902989 --definition 10

# Specify sub-application + JSON output
python scripts/vod_process_media.py ai-analysis --file-id 5145403721233902989 --definition 10 \
    --sub-app-id 251007502 --json

# Submit only, do not wait
python scripts/vod_process_media.py ai-analysis --file-id 5145403721233902989 --definition 10 --no-wait
```

---

### §5.5 AI Content Recognition (ai-recognition)

```bash
# Basic recognition
python scripts/vod_process_media.py ai-recognition --file-id 5145403721233902989 --definition 10

# Specify sub-application + JSON output
python scripts/vod_process_media.py ai-recognition --file-id 5145403721233902989 --definition 10 \
    --sub-app-id 251007502 --json
```

---

### §5.6 Snapshot (snapshot)

```bash
# Basic snapshot
python scripts/vod_process_media.py snapshot --file-id 5145403721233902989 --definition 10

# Multiple watermarks + specify snapshot time offsets
python scripts/vod_process_media.py snapshot --file-id 5145403721233902989 --definition 10 \
    --ext-time-offset-set "5s,10s,15s" --watermark-set "10001,10002"
```

---

### Error Code Descriptions (Scene Transcoding)


| Error Type | Cause | Handling Suggestion |
|---------|------|---------|
| `InvalidParameterValue.Definition` | Invalid template ID | Check whether the scene/resolution/policy combination is valid |
| `ResourceNotFound.FileId` | File does not exist | Check whether the FileId is correct |
| `FailedOperation` | Task submission failed | Check account permissions and balance |
| `LimitExceeded` | Rate limit exceeded | Default: 20 requests/second |