# vod_create_scene_aigc_video_task — Detailed Parameters and Examples
> This file is generated by splitting references, corresponding script: `scripts/vod_create_scene_aigc_video_task.py`

### ⚠️ Common Parameter Errors

| Incorrect Usage | Correct Usage | Description |
|---------|---------|------|
| `--storage-mode Permanent` | `--output-storage-mode Permanent` | The storage mode parameter name starts with `--output-` |
| `--scene-type showcase` | `--scene-type product_showcase` | Scene type must use the full enum value |
| `--camera ZoomIn` | `--camera-movement ZoomIn` | The camera motion parameter name is `--camera-movement` |
| `--duration 10` | `--duration 4/6/8` | Video duration only supports 4, 6, and 8 seconds |
| `--aspect-ratio 1:1` | `--aspect-ratio 16:9 or 9:16` | Product showcase scene only supports 16:9 and 9:16 |
| Generating without providing FileId | Upload image first to obtain FileId | Must ask the user when no image is provided; placeholders are not allowed |

## Parameter Reference
## §12 Scene-based AIGC Video Generation Parameters (vod_create_scene_aigc_video_task.py)


Based on the `CreateSceneAigcVideoTask` API, supports scene-based video generation such as product 360-degree showcase.

### Common Parameters (`generate` subcommand)

> ⚠️ `--sub-app-id`, `--region`, `--no-wait`, and `--max-wait` are only supported by the `generate` subcommand; `--json` and `--dry-run` are supported by both `generate` and `query` subcommands.

| Parameter | Type | Description |
|------|------|------|
| `--sub-app-id` | int | VOD application ID (**required**; can also be set via environment variable `TENCENTCLOUD_VOD_SUB_APP_ID`) |
| `--region` | string | Region, default `ap-guangzhou` |
| `--json` | flag | JSON format output |
| `--dry-run` | flag | Preview request parameters without calling the API |
| `--no-wait` | flag | Submit task only without waiting for result (auto-wait by default) |
| `--max-wait` | int | Maximum wait time (seconds, default 1800) |

### generate Parameters (Create Scene-based Video Generation Task)

#### Required Parameters

| Parameter | Type | Required | Description |
|------|------|------|------|
| `--scene-type` | enum | ✅ | AI video generation scene type: `product_showcase` (product 360-degree showcase) |

#### Input File Parameters

| Parameter | Type | Required | Description |
|------|------|------|------|
| `--input-files` | string list | - | Input image list, format: `File:FileId` or `Url:URL` |

> **Input Image Requirements**:
> - Supported image formats: jpg, jpeg, png, webp
> - Recommended to use images smaller than 7MB

#### Product Showcase Scene Parameters (scene-type=product_showcase)

| Parameter | Type | Required | Description |
|------|------|------|------|
| `--camera-movement` | enum | - | Camera movement mode: `AutoMatch` (auto match), `ZoomIn` (zoom in), `ZoomOut` (zoom out), `GlideRight` (glide right), `GlideLeft` (glide left), `CraneDown` (crane down) |

#### Output Configuration Parameters (Output Config)

| Parameter | Type | Description |
|------|------|------|
| `--output-storage-mode` | enum | Storage mode: `Permanent` (permanent storage), `Temporary` (temporary storage, default) |
| `--media-name` | string | Output filename (max 64 characters) |
| `--class-id` | int | Category ID (default 0) |
| `--expire-time` | string | Expiration time (ISO 8601 format, e.g. `2026-12-31T23:59:59+08:00`) |
| `--aspect-ratio` | enum | Video aspect ratio: `16:9`, `9:16` |
| `--duration` | int | Video duration (seconds): `4`, `6`, `8` |

#### Other Optional Parameters

| Parameter | Type | Description |
|------|------|------|
| `--prompt` | string | User-defined prompt |
| `--session-id` | string | Deduplication identifier (max 50 characters; returns an error if duplicated within 3 days) |
| `--session-context` | string | Source context for passing through user request information (max 1000 characters) |
| `--tasks-priority` | int | Task priority (-10 to 10; higher value means higher priority, default 0) |
| `--ext-info` | string | Reserved field; use for special purposes |

### query Parameters (Query Task)

| Parameter | Type | Required | Description |
|------|------|------|------|
| `--task-id` | string | ✅ | Task ID |
| `--sub-app-id` | int | - | Sub-application ID (can also be set via environment variable `TENCENTCLOUD_VOD_SUB_APP_ID`) |
| `--region` | string | - | Region, default `ap-guangzhou` |
| `--json` | flag | - | JSON format output |
| `--dry-run` | flag | - | Preview request parameters without calling the API |

### Data Structure Reference

#### Scene Info — Scene Information

```json
{
  "Type": "product_showcase",
  "Product Showcase Config": {
    "Camera Movement": "AutoMatch"
  }
}
```

| Field | Type | Description |
|------|------|------|
| `Type` | string | Scene type: `product_showcase` |
| `ProductShowcaseConfig` | object | Product showcase configuration (valid when Type=product_showcase) |

#### Product Showcase Config — Product Showcase Configuration

| Field | Type | Description |
|------|------|------|
| `CameraMovement` | string | Camera movement mode: `AutoMatch`, `ZoomIn`, `ZoomOut`, `GlideRight`, `GlideLeft`, `CraneDown` |

#### File Infos — File Information

```json
[
  {"Type": "File", "FileId": "3704211xxx509819"},
  {"Type": "Url", "Url": "https://example.com/img.jpg"}
]
```

| Field | Type | Description |
|------|------|------|
| `Type` | string | Type: `File` (VOD media file), `Url` (accessible URL) |
| `FileId` | string | Media file ID (required when Type=File) |
| `Url` | string | File URL (required when Type=Url) |

#### Output Config — Output Configuration

```json
{
  "Storage Mode": "Temporary",
  "Media Name": "output.mp4",
  "ClassId": 0,
  "Expire Time": "2026-12-31T23:59:59+08:00",
  "Aspect Ratio": "16:9",
  "Duration": 6
}
```

| Field | Type | Description |
|------|------|------|
| `StorageMode` | string | Storage mode |
| `MediaName` | string | Output filename |
| `ClassId` | int | Category ID |
| `ExpireTime` | string | Expiration time |
| `AspectRatio` | string | Video aspect ratio: `16:9`, `9:16` |
| `Duration` | int | Video duration (seconds): 4, 6, 8 |

### Task Status Reference

| Status | Description |
|------|------|
| `PROCESSING` | Processing |
| `SUCCESS` | Succeeded |
| `FAIL` | Failed |

### API Mapping

| Function | API | Documentation Link |
|------|---------|---------|
| Create scene-based AIGC video task | `CreateSceneAigcVideoTask` | https://cloud.tencent.com/document/api/266/127542 |
| Query task details | `DescribeTaskDetail` | https://cloud.tencent.com/document/api/266/33431 |

### Error Code Reference

| Error Type | Cause | Recommended Action |
|---------|------|---------|
| Missing Parameter | Missing required parameter | Check that Sub AppId and Scene Info are provided |
| Invalid Parameter Value.Scene Type | Scene type not supported | Use a supported scene type: product_showcase |
| Invalid Parameter Value.Aspect Ratio | Aspect ratio not supported | Product showcase scene supports 16:9, 9:16 |
| Invalid Parameter Value.Duration | Video duration not supported | Supports 4, 6, 8 seconds |
| Invalid Parameter Value.Camera Movement | Camera movement mode not supported | Use supported values: AutoMatch/ZoomIn/ZoomOut/GlideRight/GlideLeft/CraneDown |
| Invalid Parameter Value.File Infos | Input image format error | Check that the image format is jpg/jpeg/png/webp |
| Invalid Parameter | Parameter format error | Check that the File Infos format is Type:Value |
| Limit Exceeded | Rate limit exceeded | Default 20 requests/second |

---


---


## Usage Examples
## §12 Scene-based AIGC Video Generation (vod_create_scene_aigc_video_task.py)


Based on the `CreateSceneAigcVideoTask` API, supports scene-based video generation such as product 360-degree showcase.

### §12.1 Product 360-Degree Showcase Scene

#### Basic Product Showcase (Auto Camera)
```bash
python scripts/vod_create_scene_aigc_video_task.py generate \
    --sub-app-id 251007502 \
    --scene-type product_showcase \
    --input-files "File:3704211509819" \
    --camera-movement AutoMatch
```

#### Zoom-In Camera Effect
```bash
python scripts/vod_create_scene_aigc_video_task.py generate \
    --sub-app-id 251007502 \
    --scene-type product_showcase \
    --input-files "File:3704211509819" \
    --camera-movement ZoomIn \
    --aspect-ratio 16:9 \
    --duration 6
```

#### Zoom-Out Camera Effect
```bash
python scripts/vod_create_scene_aigc_video_task.py generate \
    --sub-app-id 251007502 \
    --scene-type product_showcase \
    --input-files "File:3704211509819" \
    --camera-movement ZoomOut \
    --aspect-ratio 9:16 \
    --duration 8
```

#### Horizontal Pan Camera
```bash
# GlideRight
python scripts/vod_create_scene_aigc_video_task.py generate \
    --sub-app-id 251007502 \
    --scene-type product_showcase \
    --input-files "File:3704211509819" \
    --camera-movement GlideRight \
    --aspect-ratio 16:9

# GlideLeft
python scripts/vod_create_scene_aigc_video_task.py generate \
    --sub-app-id 251007502 \
    --scene-type product_showcase \
    --input-files "File:3704211509819" \
    --camera-movement GlideLeft \
    --aspect-ratio 16:9
```

#### Crane-Down Camera Effect
```bash
python scripts/vod_create_scene_aigc_video_task.py generate \
    --sub-app-id 251007502 \
    --scene-type product_showcase \
    --input-files "File:3704211509819" \
    --camera-movement CraneDown \
    --aspect-ratio 9:16 \
    --duration 4
```

#### Using a URL Image
```bash
python scripts/vod_create_scene_aigc_video_task.py generate \
    --sub-app-id 251007502 \
    --scene-type product_showcase \
    --input-files "Url:https://example.com/product.jpg" \
    --camera-movement AutoMatch \
    --aspect-ratio 16:9
```

#### Permanently Store the Generated Video
```bash
python scripts/vod_create_scene_aigc_video_task.py generate \
    --sub-app-id 251007502 \
    --scene-type product_showcase \
    --input-files "File:3704211509819" \
    --camera-movement AutoMatch \
    --aspect-ratio 16:9 \
    --duration 6 \
    --output-storage-mode Permanent \
    --media-name "Product 360 Showcase Video" \
    --class-id 100
```

#### Specify Expiration Time
```bash
python scripts/vod_create_scene_aigc_video_task.py generate \
    --sub-app-id 251007502 \
    --scene-type product_showcase \
    --input-files "File:3704211509819" \
    --camera-movement ZoomIn \
    --aspect-ratio 16:9 \
    --duration 6 \
    --expire-time "2026-12-31T23:59:59+08:00"
```

#### Add a Custom Prompt
```bash
python scripts/vod_create_scene_aigc_video_task.py generate \
    --sub-app-id 251007502 \
    --scene-type product_showcase \
    --input-files "File:3704211509819" \
    --camera-movement AutoMatch \
    --aspect-ratio 16:9 \
    --duration 6 \
    --prompt "Premium product showcase with professional lighting effects"
```

---

### §12.2 Query Task

#### Basic Query
```bash
python scripts/vod_create_scene_aigc_video_task.py query \
    --task-id "251441341-Aigc Video Task-abc123"
```

#### Query with Specified Sub-application
```bash
python scripts/vod_create_scene_aigc_video_task.py query \
    --task-id "251441341-Aigc Video Task-abc123" \
    --sub-app-id 251007502
```

#### JSON Format Output
```bash
python scripts/vod_create_scene_aigc_video_task.py query \
    --task-id "251441341-Aigc Video Task-abc123" \
    --json
```

---