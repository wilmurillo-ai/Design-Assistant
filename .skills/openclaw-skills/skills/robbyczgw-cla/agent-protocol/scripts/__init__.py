"""
Agent Protocol - Python Library
Import this to use agent protocol from your skills.

Usage from other skills:
    import sys
    from pathlib import Path
    sys.path.insert(0, str(Path(__file__).parent.parent / "agent-protocol" / "scripts"))
    from publish import publish_event
"""

# Note: Use direct imports from scripts/ directory
# Example: from publish import publish_event
