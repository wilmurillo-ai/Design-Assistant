# 版本治理规则

## 目标

版本号要让人一眼看懂。  
也要方便后面持续迭代。

## 建议规则

- `v1.0.x`：内容补全和格式修正
- `v1.1.x`：结构升级和多语言底座
- `v1.2.x`：语言覆盖增强和模板增强
- `v2.x`：如果出现目录结构或产品形态的大调整，再升主版本

## 每次发版建议同步更新

- 文档版本号
- 修订说明
- 适配的官方 SDK 版本
- 最后复核时间

## 不建议的做法

- 只改文档，不改版本号
- 改了 metadata，不写修订说明
- 不区分“格式修改”和“能力变更”
