# 多维表标注 Skill（独立包）

本文件夹为 **仅标注** 的 OpenClaw Skill，不包含小爱同步逻辑。

- **Skill 名称**：`bitable_data_labeling`
- **入口**：`python label_skill.py`
- **入参**：`bitable_url`、`app_id`、`app_secret`、`limit`（可选，默认 100）
- **输出**：`labeling_updated_count`

上传到 Clawhub 时请上传本文件夹（`label_skill/`）作为该 Skill 的根目录。
