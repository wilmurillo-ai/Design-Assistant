interface:
  display_name: "UI Element Ops"
  short_description: "Parse, find, wait, click, and calibrate UI"
  default_prompt: "Use $ui-element-ops to parse UI screenshots, find/wait for elements, perform UI actions, and calibrate coordinates."
