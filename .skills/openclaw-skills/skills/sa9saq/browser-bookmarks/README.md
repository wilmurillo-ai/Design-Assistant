# Browser Bookmarks

A skill for organizing browser bookmarks, detecting duplicates, and checking for dead links.

## Features

- Parse Chrome/Firefox bookmark HTML exports
- Detect duplicate URLs across folders
- Check for dead/broken links via HTTP HEAD requests
- Suggest organizational improvements

## No API Keys Required

Uses standard `curl` for link validation.
