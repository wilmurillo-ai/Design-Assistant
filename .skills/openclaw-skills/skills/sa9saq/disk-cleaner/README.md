# Disk Cleaner

Analyze disk usage, detect large files, and get cleanup suggestions.

## Features

- Filesystem usage overview
- Top largest files and directories
- Stale/old file detection
- Common cleanup target identification (caches, logs, temp)

## No API Keys Required

Uses standard Unix tools: `du`, `df`, `find`.
