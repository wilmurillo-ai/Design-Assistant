# Uptime Monitor

Monitor website uptime and measure response times.

## Features

- Up/down status checking
- Detailed response time breakdown (DNS, connect, TLS, total)
- HTTP status code reporting
- Bulk URL monitoring
- Repeated checks with summary

## No API Keys Required

Uses `curl` for all checks.
