# DNS Lookup

Detailed DNS record queries and propagation checking.

## Features

- All record types (A, AAAA, MX, TXT, CNAME, NS, SOA, PTR)
- Multi-server propagation verification
- Reverse DNS lookups
- WHOIS integration

## No API Keys Required

Uses `dig`, `nslookup`, and optionally `whois`.
