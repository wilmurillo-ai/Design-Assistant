# Config Diff

Compare configuration files and suggest merge strategies.

## Features

- Unified and semantic diffs
- YAML, JSON, TOML, INI support
- Merge suggestions for conflicts
- Environment file comparison

## No API Keys Required

Uses `diff`, optionally `jq` and `yq`.
