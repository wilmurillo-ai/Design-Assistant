# Image Optimizer

Compress, resize, and convert images using ImageMagick CLI tools.

## Features

- Quality-controlled compression
- Resize to specific dimensions
- Format conversion (PNG, JPEG, WebP, AVIF)
- Batch processing

## No API Keys Required

Uses ImageMagick and optional tools (`cwebp`, `optipng`).
