{
  "id": "feishu-cards",
  "name": "Feishu Cards",
  "version": "0.1.0",
  "description": "Send interactive card forms via Feishu. Requires the built-in feishu plugin for card action callbacks.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "accountId": {
        "type": "string",
        "description": "Feishu account ID to use (defaults to 'default')"
      }
    }
  }
}
