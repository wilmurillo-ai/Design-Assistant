[project]
name = "ioc-patrol-report"
version = "0.1.0"
description = "IOC 智能巡检报告生成器"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "psycopg2-binary>=2.9.0",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
]

[tool.uv]
dev-dependencies = []
