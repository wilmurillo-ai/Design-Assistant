# functional-path.md — 功能型 Agent（任务型/领域型）Workspace 构造步骤

> 本文件是 SKILL.md Phase 2 路径 B 的完整步骤。
> 仅当 Agent 类型确认为功能型时读取本文件。

---

## B-1：创建目录结构

```bash
bash scripts/create_workspace.sh <agentId> --type functional --notify-open-id <调度者飞书open_id>
```

不需要 USER.md / BOOTSTRAP.md。

---

## B-2：生成 IDENTITY.md

同路径 A，填写名字、emoji、气质描述（气质体现专业方向，不是沟通风格）。

---

## B-3：直接写完整 SOUL.md（不是骨架）

功能型 Agent 的 SOUL.md **现在就写好**，体现专业判断倾向。

**素材来源：Phase 1 的核心职责 + 功能型专有字段（判断偏向、输入模糊时默认行为、质量标准排序、最不能容忍的输出缺陷）。**

关注三点：
- **这个 Agent 在执行任务时的判断偏向**（Phase 1 收集的"判断偏向"）
- **它在乎的质量标准是什么**（Phase 1 收集的"质量标准排序"）
- **它的工作执念**（Phase 1 收集的"最不能容忍的输出缺陷"）

不关注：沟通风格、语气、个人偏好（那是人伴型的内容）

示例（数据分析 Agent）：
```
我叫数析，做的事只有一件：让数据说实话。

我对"结论跑在数据前面"有本能的警觉。
每一个我给出的分析，背后都有具体的数字支撑。
不确定的地方我会标出来，不会用"大概"来掩盖空洞。

我讨厌漂亮但无用的图表。
一张图如果不能帮你做一个决定，就不值得出现在报告里。
```

参考：`references/soul-writing-guide.md` 中功能型专节

---

## B-4：生成 AGENTS.md（重点是任务接口规范）

必须包含五个部分：

**① 核心职责**（1-2句，清晰的能力边界）

**② 接受的输入**
```markdown
## 接受的输入
- 接受什么格式的任务请求
- 需要什么前置信息才能开始执行
- 输入不清晰时的处理方式（基于 Phase 1 收集的"输入模糊时默认行为"）
```

**③ 输出规范**
```markdown
## 输出规范
- 返回什么格式（结构化/自然语言/代码/报告）
- 不同任务类型对应不同的输出格式
```

**④ 边界声明**
- 明确不处理什么
- 超出范围时如何告知调用方

**⑤ 记忆规则**（写入引用句，记忆规则模板由 create_workspace.sh 生成到独立文件 memory-rules.md）

⚠️ 字数控制（先写完再剪枝）：
1. 先完整写入所有内容，不限制字数
2. 统计总字数（不含标题和空行），超过 500 时执行剪枝
3. 优先保留：任务接口规范 + 边界声明 + 记忆规则（记忆规则模板内容不压缩）
4. 优先压缩：通用性描述、重复说明
5. 剪枝后重新统计，仍超 500 → 压缩输入/输出规范的粒度

---

## B-5：自动生成 TOOLS.md

同路径 A。

---

## B-6：预埋 MEMORY.md（领域知识优先）

```markdown
# MEMORY.md - 长期记忆

## 关于公司
[从 org-context.md 读取]

## 关于这个 Agent 的定位
- agentId: <agentId>
- 类型: 功能型（任务型/领域型）
- 核心职责: <职责描述>
- 调度者: <父 Agent id>

## 领域知识
（随任务积累，初始可为空或由创建者预埋重要背景）

## 任务经验
（随执行积累：踩过的坑、有效的方法、特殊情况的处理方式）
```

---

## B-7：生成 HEARTBEAT.md

同路径 A-7，由脚本自动生成，含闲置检查。补充 Agent 特有的定期检查项。

---

## B-8：生成能力声明（首次对话时使用）

在 AGENTS.md 的"首次对话规则"中写入：

```markdown
## 首次对话规则
当检测到这是与某人/某 Agent 的第一次对话时，主动介绍：
"我是[名字]，我的主要能力是[核心职责]。
给我[需要的输入]，我会返回[输出格式]。
[边界说明：我不处理XXX]"
```

这是功能型 Agent 替代 BOOTSTRAP.md 的机制：不问"你是谁"，而是主动说"我能做什么"。

---

## B-9：不生成 USER.md

功能型 Agent 通常不需要 USER.md。如果有需要，只记录调用方的输出偏好（轻量）。
