# 沪深京数据 / 财务数据

---

#### 现金流表

接口：`list_stock_cash_flows`　`GET`

通过股票代码查询沪深京股票的现金流量数据，包括母公司期初/期末金额、合并期初/期末金额等核心财务指标，支持按报告日期范围筛选，适用于财务分析和投资决策。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | ✅ | string | 股票代码 | `002594` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | — |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py stock/cash-flows stockCode=002594
```

---

#### 利润表

接口：`list_stk_income_state`　`GET`

通过股票代码查询指定股票的利润详情，包括母公司期初数、期末数，合并期初数、期末数等核心财务数据，适用于财务分析和股票投资决策。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | ✅ | string | 股票代码 | `002594` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-03-30` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py stock/income-statements stockCode=002594
```

---

#### 资产负债表

接口：`list_stock_balance_sheet`　`GET`

通过股票代码查询指定沪深京股票的资产负债表数据，包含母公司期初数、期末数、合并期初数、期末数等核心财务指标，同时提供报告日期、公司性质、科目代码等辅助信息，便于大模型进行财务分析和风险评估。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | ✅ | string | 股票代码 | `002594` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py stock/balance-sheets stockCode=002594
```

---

#### 利润表（单季度）

接口：`list_stk_income_state_q`　`GET`

通过股票代码、报告期起止日期（格式为yyyy-MM-dd）及分页参数，查询指定沪深京股票的季度利润表数据，返回包括营业收入、营业成本、营业利润、净利润、每股收益等核心财务指标，以及报告类型、会计期间、发布日期等关键信息，便于进行公司财务分析和业绩评估。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | ✅ | string | 股票代码 | `002594` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-03-30` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py stock/income-statements-q stockCode=002594
```

---

#### 现金流表（单季度）

接口：`list_stock_cash_flows_q`　`GET`

通过股票代码、报告期起止日期（格式为yyyy-MM-dd）及分页参数，查询指定沪深京股票的季度现金流量表数据，包含经营活动、投资活动和筹资活动产生的现金流入流出明细、净额以及期初期末现金余额等核心财务指标，用于分析公司的现金创造能力、资金运用效率和财务健康状况。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | ✅ | string | 股票代码 | `002594` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | — |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py stock/cash-flows-q stockCode=002594
```

---

#### 资产负债表（单季度）

接口：`list_stk_balance_sheet_q`　`GET`

通过股票代码、报告期开始日期和结束日期，查询沪深京上市公司按季度发布的资产负债表数据，包含货币资金、应收账款、存货、固定资产、短期借款、长期借款、实收资本、未分配利润等详细的资产、负债及所有者权益科目，以及报告发布日期、报告类型和会计期间等信息，用于财务分析和偿债能力评估。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | ✅ | string | 股票代码 | `002594` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py stock/balance-sheets-q stockCode=002594
```

---

#### 杜邦分析

接口：`list_stk_dupont_analysis`　`GET`

通过股票代码和报告期范围（格式为yyyy-MM-dd）查询指定A股、B股或科创板股票的杜邦分析数据，包括净资产收益率、归母净利率、资产周转率、权益乘数等核心财务分解指标，以及营业收入、平均总资产、平均归母股东权益等基础财务数据，用于深度财务分析和盈利能力评估。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | ✅ | string | 股票代码 | `002594` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | — |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py stock/dupont-analysis stockCode=002594
```

---

#### 个股财务指标申万行业排名(单季度)

接口：`list_stk_fin_ind_sw_rnk_q`　`GET`

通过指定股票代码和报告期时间范围（格式为yyyy-MM-dd），查询单只股票在申万行业分类下的季度财务指标排名数据，包括股票及行业基础信息、报告期信息，以及总资产收益率、净资产收益率、营业收入、毛利率、净利率、归母净利润及其同比增长率、研发费用及其同比增长率和占比等多项核心财务指标在所属行业内的排名，用于分析个股在行业内的相对财务表现和竞争力。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | ✅ | string | 股票代码 | `002594` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-03-30` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py stock/fin-ind-sw-rnk-q stockCode=002594
```

---

#### 财务衍生指标

接口：`list_stk_fin_der_inds`　`GET`

通过股票代码和日期范围查询指定股票的财务衍生指标，包括带息债务、息税前利润、息税折旧摊销前利润、扣非归母净利润、每股收益（摊薄/扣除）、每股营业收入、每股经营现金流、销售毛利率、销售净利率、摊薄净资产收益率、扣除摊薄净资产收益率、总资产净利率、投入资本回报率、总资产周转率、存货周转率、应收票据及账款周转率、应收账款周转率、经营现金流流动负债比、EBIT利息保障倍数、预收款项营收比、销售收现营收比、经营现金流净额营收比、销售费用总营收比、管理费用率、财务费用率、净利率等核心财务比率和每股数据，以及股票名称、代码、发布日期和截止日期，适用于深度财务分析和投资决策。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | ✅ | string | 股票代码 | `002594` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py stock/fin-der-inds stockCode=002594
```

---

#### 财务指标（每股指标）

接口：`list_per_share_indicators`　`GET`

通过股票代码和日期范围查询指定股票的每股财务指标数据，包括每股收益、每股营业收入、每股经营现金流和每股股东自由现金流，同时返回报告期截止日期和发布时间，用于分析公司的每股盈利能力、收入质量、现金流状况和股东回报水平。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | ✅ | string | 股票代码 | `002594` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py stock/per-share-indicators stockCode=002594
```

---

#### 财务指标（成长能力）

接口：`get_stock_fin_ind_growth`　`GET`

通过股票代码查询沪深京股票的成长能力财务指标，包括营业收入同比、净利润同比和扣非净利润同比，用于评估公司的财务增长趋势。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | ✅ | string | 股票代码 | `002594` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py stock/financial-indicators-growth stockCode=002594
```

---

#### 财务指标（盈利能力）

接口：`list_fin_ind_profit`　`GET`

通过股票代码和可选的报告期时间范围（格式为yyyy-MM-dd），查询指定沪深京股票的财务盈利能力核心指标，包括销售毛利率、销售净利率、摊薄ROE、扣非摊薄ROE、平均ROA和平均ROIC，同时提供报告披露日期和报告期截止日，用于分析公司的盈利能力和资本回报效率。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | ✅ | string | 股票代码 | `002594` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py stock/financial-indicators-profitab stockCode=002594
```

---

#### 财务指标（营运能力）

接口：`list_fin_ind_operating`　`GET`

通过股票代码和日期范围查询沪深京股票的运营能力财务指标，包括固定资产合计周转率、流动资产周转率和应收账款周转率，同时提供股票名称、财报披露日期和报告期截止日期，用于分析企业资产运营效率。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | ✅ | string | 股票代码 | `002594` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py stock/financial-indicators-operatin stockCode=002594
```

---

#### 财务指标（偿债能力）

接口：`list_fin_ind_solvency`　`GET`

通过股票代码、开始日期和结束日期查询指定沪深京股票的偿债能力财务指标，包括经营现金流流动负债比和EBIT利息保障倍数等核心数据，用于评估企业的财务风险和偿债能力。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | ✅ | string | 股票代码 | `002594` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py stock/financial-indicators-solvency stockCode=002594
```

---

#### 财务指标（收现能力）

接口：`list_fin_ind_cash_collect`　`GET`

通过股票代码和可选的报告期起止日期（格式为yyyy-MM-dd），查询沪深京A股或B股公司的财务收现能力指标，包括预收款项营收占比、销售收现营收占比和经营现金流营收占比，以及对应的股票信息、报告期截止日期和发布日期，用于分析企业现金流质量和营收实现效率。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | ✅ | string | 股票代码 | `002594` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py stock/financial-indicators-cash-col stockCode=002594
```

---

#### 业绩快报

接口：`list_stock_earn_bulletins`　`GET`

通过股票代码和日期范围查询沪深京股票的业绩快报数据，包括营业收入、净利润、每股收益、净资产收益率等核心财务指标的本期值、上年同期值及同比变化，以及报告类型、会计区间、发布日期、截止日期等关键信息，适用于快速获取和分析上市公司定期财务表现。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | ✅ | string | 股票代码 | `002594` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | — |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py stock/earnings-bulletins stockCode=002594
```

---

#### 财务指标（单季度成长能力）

接口：`list_stock_fin_ind_grow_q`　**`POST`**

通过股票代码查询沪深京股票的季度成长能力财务指标，包括营业收入同比等核心增长数据，并可指定财报披露日期范围进行筛选，用于评估公司的财务增长趋势。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | — | string | 股票代码【与stockCodes组成多选一参数，必须且只能传递其中一个】 | `002594` |
| `stockCodes` | — | array | 股票代码列表【与stockCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: stockCode, stockCodes, beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py stock/financial-indicators-grow_q --method POST
```

---

#### 财务指标（单季度成长能力）

接口：`list_stock_fin_ind_grow_q`　**`POST`**

通过股票代码查询沪深京股票的季度成长能力财务指标，包括营业收入同比等核心增长数据，并可指定财报披露日期范围进行筛选，用于评估公司的财务增长趋势。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | — | string | 股票代码【与stockCodes组成多选一参数，必须且只能传递其中一个】 | `002594` |
| `stockCodes` | — | array | 股票代码列表【与stockCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: stockCode, stockCodes, beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py stock/financial-indicators-grow-q --method POST
```

---

#### 财务衍生指标(单季度)

接口：`list_stk_fin_der_inds_q`　**`POST`**

通过股票代码和日期范围查询指定股票的财务衍生指标，包括带息债务、息税前利润、息税折旧摊销前利润、扣非归母净利润、每股收益（摊薄/扣除）、每股营业收入、每股经营现金流、销售毛利率、销售净利率、摊薄净资产收益率、扣除摊薄净资产收益率、总资产净利率、投入资本回报率、总资产周转率、存货周转率、应收票据及账款周转率、应收账款周转率、经营现金流流动负债比、EBIT利息保障倍数、预收款项营收比、销售收现营收比、经营现金流净额营收比、销售费用总营收比、管理费用率、财务费用率、净利率等核心财务比率和每股数据，以及股票名称、代码、发布日期和截止日期，适用于深度财务分析和投资决策。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | — | string | 股票代码【与stockCodes组成多选一参数，必须且只能传递其中一个】 | `002594` |
| `stockCodes` | — | array | 股票代码列表【与stockCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: stockCode, stockCodes, beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py stock/fin-der-inds-q --method POST
```

---

#### 财务指标（单季度每股指标）

接口：`list_per_share_indica_q`　**`POST`**

通过股票代码和日期范围查询指定股票的每股财务指标数据，包括每股收益、每股营业收入、每股经营现金流和每股股东自由现金流，同时返回报告期截止日期和发布时间，用于分析公司的每股盈利能力、收入质量、现金流状况和股东回报水平。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | — | string | 股票代码【与stockCodes组成多选一参数，必须且只能传递其中一个】 | `002594` |
| `stockCodes` | — | array | 股票代码列表【与stockCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: stockCode, stockCodes, beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py stock/per-share-indicators-q --method POST
```

---

#### 财务指标（单季度盈利能力）

接口：`list_fin_ind_profit_q`　**`POST`**

通过股票代码和可选的报告期时间范围（格式为yyyy-MM-dd），查询指定沪深京股票的财务盈利能力核心指标，包括销售毛利率、销售净利率、摊薄ROE、扣非摊薄ROE、平均ROA和平均ROIC，同时提供报告披露日期和报告期截止日，用于分析公司的盈利能力和资本回报效率。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | — | string | 股票代码【与stockCodes组成多选一参数，必须且只能传递其中一个】 | `002594` |
| `stockCodes` | — | array | 股票代码列表【与stockCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: stockCode, stockCodes, beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py stock/fin-indicators-profitab-q --method POST
```

---

#### 财务指标（单季度营运能力）

接口：`list_fin_ind_operating_q`　**`POST`**

通过股票代码和日期范围查询沪深京股票的运营能力财务指标，包括固定资产合计周转率、流动资产周转率和应收账款周转率，同时提供股票名称、财报披露日期和报告期截止日期，用于分析企业资产运营效率。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | — | string | 股票代码【与stockCodes组成多选一参数，必须且只能传递其中一个】 | `002594` |
| `stockCodes` | — | array | 股票代码列表【与stockCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: stockCode, stockCodes, beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py stock/fin-indicators-operatin-q --method POST
```

---

#### 财务指标（单季度偿债能力）

接口：`list_fin_ind_solvency_q`　**`POST`**

通过股票代码、开始日期和结束日期查询指定沪深京股票的偿债能力财务指标，包括经营现金流流动负债比和EBIT利息保障倍数等核心数据，用于评估企业的财务风险和偿债能力。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | — | string | 股票代码【与stockCodes组成多选一参数，必须且只能传递其中一个】 | `002594` |
| `stockCodes` | — | array | 股票代码列表【与stockCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: stockCode, stockCodes, beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py stock/fin-indicators-solvency-q --method POST
```

---

#### 财务指标（单季度收现能力）

接口：`list_fin_ind_cash_coll_q`　**`POST`**

通过股票代码和可选的报告期起止日期（格式为yyyy-MM-dd），查询沪深京A股或B股公司的财务收现能力指标，包括预收款项营收占比、销售收现营收占比和经营现金流营收占比，以及对应的股票信息、报告期截止日期和发布日期，用于分析企业现金流质量和营收实现效率。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `stockCode` | — | string | 股票代码【与stockCodes组成多选一参数，必须且只能传递其中一个】 | `002594` |
| `stockCodes` | — | array | 股票代码列表【与stockCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '600519']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd）。 最小值:2020-01-01; | `2020-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: stockCode, stockCodes, beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py stock/fin-indicators-cash-coll-q --method POST
```

---
