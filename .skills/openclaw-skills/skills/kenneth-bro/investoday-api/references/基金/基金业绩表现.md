# 基金 / 基金业绩表现

---

#### 基金历史净值

接口：`list_fund_nav_history`　**`POST`**

通过基金代码和日期范围查询指定基金的历史净值数据，返回包括基金代码、名称、净值日期、单位净值、累计净值以及发布日期等核心信息，支持分页查询，适用于分析基金的历史表现和净值走势。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `fundCode` | — | string | 基金代码【与fundCodes组成多选一参数，必须且只能传递其中一个】 | `000001` |
| `fundCodes` | — | array | 基金代码列表【与fundCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '000006']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd） | `2025-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: fundCode, fundCodes, beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py fund/nav/history --method POST
```

---

#### 基金历史复权净值

接口：`list_fund_adj_navs`　**`POST`**

根据基金代码和日期范围查询基金的历史复权单位净值数据，返回包括基金代码、名称、净值日期、复权单位净值及发布日期等核心信息，支持分页查询，适用于分析基金历史表现和净值走势。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `fundCode` | — | string | 基金代码【与fundCodes组成多选一参数，必须且只能传递其中一个】 | `000001` |
| `fundCodes` | — | array | 基金代码列表【与fundCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '000006']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd） | `2025-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: fundCode, fundCodes, beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py fund/adjusted-navs --method POST
```

---

#### 货币基金收益情况

接口：`list_currency_yield_his`　**`POST`**

通过基金代码和日期范围（格式为yyyy-MM-dd HH:mm:ss）查询货币基金的收益情况，返回包括基金代码、名称、份额等级、截止日期、万份基金单位收益、七日年化收益率和基金资产净值等核心数据，适用于分析货币基金的历史收益表现和资产规模。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `fundCode` | — | string | 基金代码【与fundCodes组成多选一参数，必须且只能传递其中一个】 | `000001` |
| `fundCodes` | — | array | 基金代码列表【与fundCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '000006']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd） | `2025-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: fundCode, fundCodes, beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py fund/currency-yield-history --method POST
```

---

#### 基金评价同类平均指标

接口：`get_fund_peer_avg_metric`　**`POST`**

通过基金代码查询指定基金在多个时间维度（如成立至今、今年以来、最近1周、1月、3月、6月、1年、3年、5年）的全面评价指标，包括该基金的累计收益率、阿尔法收益、年化波动率、最大回撤、下行波动率、夏普比率、索提诺比率、信息比率、特雷诺比率、跟踪误差、贝塔系数、VaR等核心风险收益指标，同时提供各指标在同类基金中的平均值和排名，便于大模型进行基金的横向对比和深度绩效评估。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `fundCode` | — | string | 基金代码【与fundCodes组成多选一参数，必须且只能传递其中一个】 | `000300` |
| `fundCodes` | — | array | 基金代码列表【与fundCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '000006']` |

**接口示例**

```bash
# 可选参数: fundCode, fundCodes
python skills/scripts/call_api.py fund/eval-peer-avg-ind --method POST
```

---

#### 基金回报率

接口：`list_fund_return_rate`　**`POST`**

通过基金代码和日期范围查询指定基金的历史回报率数据，返回包括日回报率、周回报率、月回报率、季度回报率、半年回报率、年回报率以及今年以来回报率等多维度收益指标，用于分析基金在不同时间区间的业绩表现。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `fundCode` | — | string | 基金代码【与fundCodes组成多选一参数，必须且只能传递其中一个】 | `000001` |
| `fundCodes` | — | array | 基金代码列表【与fundCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '000006']` |

**接口示例**

```bash
# 可选参数: fundCode, fundCodes
python skills/scripts/call_api.py fund/return-rate --method POST
```

---

#### 基金经理任职收益

接口：`list_fund_mgr_perf`　**`POST`**

通过基金代码、基金经理姓名或任职状态，查询公募基金经理的任职收益数据，包括基金经理代码、姓名、任职天数、任职状态、任职收益以及其管理的基金代码、名称和数据截止日期（yyyy-MM-dd HH:mm:ss），用于评估基金经理的历史业绩表现。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `fundCode` | — | string | 基金代码【与fundCodes组成多选一参数，必须且只能传递其中一个】 | `000001` |
| `fundCodes` | — | array | 基金代码列表【与fundCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '000006']` |
| `managerName` | — | string | 基金经理姓名 | `张三` |
| `employmentStatus` | — | integer | 任职状态 | `1` |

**接口示例**

```bash
# 可选参数: fundCode, fundCodes, managerName, employmentStatus
python skills/scripts/call_api.py fund-manager/performance --method POST
```

---

#### 基金经理区间回报

接口：`list_fund_mgr_returns`　**`POST`**

通过基金经理姓名和日期范围查询其在不同时间区间（如本月以来、近一月、近三月、近六月、今年以来、近一年、近三年、近五年）的投资回报率，返回包含基金经理ID、姓名、数据日期、投资类型及各区间回报率的核心数据，适用于评估基金经理的历史业绩表现。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `fundManagerName` | ✅ | string | 基金经理姓名 | `000001` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd） | `2025-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py fund-manager/interval-returns --method POST fundManagerName=000001
```

---

#### 归因分析

接口：`get_fund_perfor_attr`　**`POST`**

通过基金代码查询指定开放式基金（类型为J、K）的综合业绩评估指标，包括基金基本信息（ID、名称、代码）、数据截止日期（yyyy-MM-dd HH:mm:ss格式），以及涵盖成立至今、今年以来、近一周、近一月、近三月、近六月、近一年、近三年、近五年等多个时间维度的年化收益率、累计收益率、Alpha收益、最大回撤、波动率、夏普比率、索提诺比率、卡玛比率、信息比率、特雷诺比率、月度胜率、选股能力、择时能力、跟踪误差等数十项核心风险收益指标，用于全面评估基金的历史表现、风险水平和投资能力。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `fundCode` | — | string | 基金代码【与fundCodes组成多选一参数，必须且只能传递其中一个】 | `000001` |
| `fundCodes` | — | array | 基金代码列表【与fundCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '000006']` |

**接口示例**

```bash
# 可选参数: fundCode, fundCodes
python skills/scripts/call_api.py fund/performance-attribution --method POST
```

---

#### 业绩比较基准行情

接口：`list_perf_benchmark_quote`　**`POST`**

根据指定的基准指数代码和日期范围（格式为 yyyy-MM-dd HH:mm:ss），查询基金业绩比较基准的历史行情数据，包括基准指数代码、名称、截止日期、收盘价、涨跌额和涨跌幅，用于基金业绩分析和比较。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `benchmarkIndexCode` | — | string | 基准指数代码【与benchmarkIndexCodes组成多选一参数，必须且只能传递其中一个】 | `000001` |
| `benchmarkIndexCodes` | — | array | 基准指数代码列表【与benchmarkIndexCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '000006']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd） | `2025-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: benchmarkIndexCode, benchmarkIndexCodes, beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py fund/perf-benchmark-quote --method POST
```

---

#### 基金与指数回报相关系数

接口：`list_fund_idx_ret_corr`　**`POST`**

通过基金代码和日期范围（格式为yyyy-MM-dd HH:mm:ss）查询基金与指数的回报相关系数，返回包括基金代码、名称、日期、内部编码、关联的指数代码以及6个月、1年、3年、5年等多个时间维度的相关系数，用于分析基金与市场指数的历史关联性和风险敞口。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `fundCode` | — | string | 基金代码【与fundCodes组成多选一参数，必须且只能传递其中一个】 | `000001` |
| `fundCodes` | — | array | 基金代码列表【与fundCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '000006']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd） | `2025-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: fundCode, fundCodes, beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py fund/index-return-correlations --method POST
```

---

#### 基金分红

接口：`list_fund_divs`　**`POST`**

通过基金代码和时间范围查询基金分红信息，包括分红年度、分红对象、单位基金收益、分红比例、分红总额等关键数据，适用于基金分红分析和投资决策。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `fundCode` | — | string | 基金代码【与fundCodes组成多选一参数，必须且只能传递其中一个】 | `000001` |
| `fundCodes` | — | array | 基金代码列表【与fundCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '000006']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd） | `2025-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: fundCode, fundCodes, beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py fund/dividend --method POST
```

---

#### 基金拆分折算

接口：`list_fund_share_splits`　**`POST`**

通过基金代码和日期范围（格式为yyyy-MM-dd HH:mm:ss）查询基金份额拆分与折算的历史记录，返回包括拆分日、拆分比例、拆分对象、拆分后净值、累计净值披露说明及拆分说明等核心信息，用于分析基金份额变动和净值调整情况。

**输入参数**

| 参数名 | 必填 | 类型 | 说明 | 示例 |
|--------|:----:|------|------|------|
| `fundCode` | — | string | 基金代码【与fundCodes组成多选一参数，必须且只能传递其中一个】 | `000001` |
| `fundCodes` | — | array | 基金代码列表【与fundCode组成多选一参数，必须且只能传递其中一个】 | `['000001', '000006']` |
| `beginDate` | — | string | 开始日期（格式yyyy-MM-dd） | `2025-01-01` |
| `endDate` | — | string | 结束日期（格式yyyy-MM-dd） | `2025-01-01` |
| `pageNum` | — | integer | 页码。 最小值:1; | `1` |
| `pageSize` | — | integer | 页长。 最小值:1; 最大值:500; | `10` |

**接口示例**

```bash
# 可选参数: fundCode, fundCodes, beginDate, endDate, pageNum, pageSize
python skills/scripts/call_api.py funds/share-splits --method POST
```

---
