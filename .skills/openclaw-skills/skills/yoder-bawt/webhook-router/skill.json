{
  "name": "webhook-router",
  "version": "0.1.0",
  "description": "Webhook routing and handler framework. Register endpoints, route incoming webhooks, and process payloads with custom handlers. Use when: (1) Setting up webhook receivers, (2) Routing payloads to handlers, (3) Processing GitHub, Stripe, or custom webhooks.",
  "author": "yoder-bawt",
  "license": "MIT",
  "keywords": ["webhook", "routing", "handlers", "api", "integration"],
  "requires": { "bins": ["bash"] },
  "scripts": {
    "router": "bash router.sh",
    "register": "bash register.sh",
    "test": "bash test.sh"
  },
  "files": ["SKILL.md", "router.sh", "register.sh", "test.sh", "handlers/github.sh", "handlers/custom.sh"]
}
