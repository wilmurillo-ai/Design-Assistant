# 脚本清单 — capability

## 共享依赖

- 无。本模块不调用外部业务接口。

## 脚本列表

- 无业务脚本。能力咨询直接依据 `SKILL.md` 与 `examples/capability/README.md` 返回标准回答。

## 使用方式

- 不执行脚本。

## 输出说明

- 输出当前 skill 的能力边界说明，不回显内部实现细节，不触发接口调用。

## 规范

1. 不调用 API
2. 不虚构命令或工具
3. 不在能力咨询场景中排查鉴权
