interface:
  display_name: "Volcengine DNS Operations"
  short_description: "Query and update DNS records with checks"
  default_prompt: "Use $volcengine-network-dns to update and verify these DNS records."
