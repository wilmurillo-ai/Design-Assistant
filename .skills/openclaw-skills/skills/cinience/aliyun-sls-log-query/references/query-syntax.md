# SLS 查询与分析语法要点

## 基本格式

- 分析语句必须配合查询语句使用，格式为：
  `查询语句|分析语句`

## 查询语句

- 用于过滤日志，例如：`status:200`
- `*` 表示不做过滤

## 分析语句

- 用于统计聚合，例如：
  `* | SELECT status, count(*) AS PV GROUP BY status`

## 结果与限制

- 使用 `LIMIT` 控制返回行数与分析结果规模。
