[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "workcrm"
version = "0.1.0-alpha.4"
description = "WorkCRM OpenClaw skill (local-first CRM with explicit confirmation flow)"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
