# mvn-skill

A full-capability Maven passthrough skill for OpenClaw/ClawHub.

## Features

- Supports all Maven arguments and goals via passthrough
- Optional wrapper argument: `--dir <path>`
- Works for regular builds, plugin goals, and info commands like `mvn -v`

## Requirements

- Node.js
- Maven (`mvn`) in PATH
