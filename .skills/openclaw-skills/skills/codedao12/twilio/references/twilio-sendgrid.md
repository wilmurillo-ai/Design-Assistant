# SendGrid (Email)

## 1) Base URL
- `https://api.sendgrid.com`

## 2) Auth
- Use a SendGrid API key in the `Authorization: Bearer` header.

## 3) Mail Send
- Endpoint: `POST /v3/mail/send`
- Use this for transactional email sending.
