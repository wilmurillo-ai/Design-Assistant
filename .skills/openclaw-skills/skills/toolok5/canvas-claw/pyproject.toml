[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "canvas-claw"
version = "0.1.0"
description = "OpenClaw skill package for AI-video-agent phase-one generation flows."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-p no:asyncio"
