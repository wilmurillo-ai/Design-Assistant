# Local entity aliases

Use these names when working with this Home Assistant instance:

- `light.wiz_rgbw_tunable_1173fc` — desk lamp
- `light.wiz_rgbw_tunable_44a6a8` — ceiling light

When the user says "desk lamp" or "ceiling light", map to the corresponding `entity_id` above.
