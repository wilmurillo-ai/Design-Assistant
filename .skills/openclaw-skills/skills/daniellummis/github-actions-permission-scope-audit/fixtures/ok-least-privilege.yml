name: ok-least-privilege
on:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      checks: read
    steps:
      - uses: actions/checkout@v4
      - run: echo "test"
