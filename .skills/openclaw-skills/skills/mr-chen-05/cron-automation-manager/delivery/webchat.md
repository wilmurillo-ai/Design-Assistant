# Webchat Delivery

Use this when the user wants results returned in the current OpenClaw chat session.

```
delivery:
  mode: announce
  channel: webchat
```

Notes:
- Useful for quick testing or environments without external messaging integrations.
