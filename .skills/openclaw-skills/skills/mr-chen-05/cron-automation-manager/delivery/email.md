# Email Delivery

Use this configuration when the user wants notifications via email.

```
delivery:
  mode: announce
  channel: email
  to: <email_address>
```

Notes:
- Requires SMTP or email service configured in the environment.
