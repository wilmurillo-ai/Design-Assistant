{
  "name": "roster",
  "version": "1.5.0",
  "description": "Creates weekly shift rosters (KW-JSON) from CSV availability data and pushes them to GitHub. Optimized for field sales teams with driver logistics, trainer assignments, and automatic PDF generation.",
  "author": "netsnek",
  "license": "MIT",
  "tags": ["productivity", "scheduling", "german", "roster", "shift-planning", "csv", "github"],
  "permissions": ["network", "exec"],
  "entry": "SKILL.md",
  "minOpenClawVersion": "0.8.0",
  "scripts": [
    "scripts/get-employees.sh",
    "scripts/update-employees.sh",
    "scripts/push-to-github.sh",
    "scripts/trigger-build.sh",
    "scripts/trigger-publish.sh"
  ],
  "env": [
    {
      "name": "GITHUB_TOKEN",
      "description": "GitHub Personal Access Token with repo and actions:write permissions",
      "required": true
    },
    {
      "name": "ROSTER_REPO",
      "description": "GitHub repository in 'owner/repo' format (e.g. 'myorg/my-roster-repo')",
      "required": true
    }
  ],
  "config": {
    "carCapacity": {
      "description": "Maximum number of people per car (including driver)",
      "default": 5
    }
  }
}
