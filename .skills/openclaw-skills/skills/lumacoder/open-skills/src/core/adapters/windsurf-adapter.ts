import { CursorAdapter } from './cursor-adapter.js';
import type { SkillMeta } from '../../types/index.js';

export class WindsurfAdapter extends CursorAdapter {
  override generateHeaderContent(skills: SkillMeta[]): string {
    return `# Windsurf Rules\n\n> Generated by open-skills for ${skills.length} skill(s)\n\n`;
  }
}
