# pydantic-airtable API surface

This reference was generated by introspecting the installed `pydantic_airtable` package available to the agent.

## Top-level exports

- `APIError`
- `AirtableClient`
- `AirtableConfig`
- `AirtableError`
- `AirtableField`
- `AirtableFieldType`
- `AirtableManager`
- `AirtableModel`
- `BaseHTTPClient`
- `ConfigurationError`
- `FieldTypeResolver`
- `RecordNotFoundError`
- `ValidationError`
- `airtable_field`
- `airtable_model`
- `configure`
- `configure_from_env`
- `get_global_config`
- `get_version`
- `get_version_info`
- `set_global_config`

## AirtableConfig

```python
AirtableConfig(access_token: str, base_id: str, table_name: Optional[str] = None)
```

Fields:

- `access_token: str`
- `base_id: str`
- `table_name: Optional[str]`

## AirtableModel methods

```python
all(**filters) -> List[AirtableModel]
bulk_create(data_list: List[Dict[str, Any]]) -> List[AirtableModel]
create(**data) -> AirtableModel
create_table(table_name: Optional[str] = None) -> Dict[str, Any]
delete(self) -> Dict[str, Any]
find_by(**filters) -> List[AirtableModel]
first(**filters) -> Optional[AirtableModel]
get(record_id: str) -> AirtableModel
save(self) -> AirtableModel
sync_table(table_name: Optional[str] = None, create_missing_fields: bool = True, update_field_types: bool = False) -> Dict[str, Any]
```

Useful inherited Pydantic methods are also available, including `model_dump`, `model_dump_json`, `model_validate`, `model_json_schema`, `dict`, `json`, `schema`, and parsing/construct helpers.

## AirtableManager methods

```python
create_base(self, name: str, tables: List[Dict[str, Any]]) -> Dict[str, Any]
create_record(self, fields: Dict[str, Any], table_name: Optional[str] = None, base_id: Optional[str] = None) -> Dict[str, Any]
create_table(self, name: str, fields: List[Dict[str, Any]], base_id: Optional[str] = None) -> Dict[str, Any]
create_table_from_model(self, model_class: Type, table_name: Optional[str] = None, base_id: Optional[str] = None) -> Dict[str, Any]
delete_base(self, base_id: str) -> Dict[str, Any]
delete_record(self, record_id: str, table_name: Optional[str] = None, base_id: Optional[str] = None) -> Dict[str, Any]
delete_table(self, table_id: str, base_id: Optional[str] = None) -> Dict[str, Any]
get_base_schema(self, base_id: Optional[str] = None) -> Dict[str, Any]
get_record(self, record_id: str, table_name: Optional[str] = None, base_id: Optional[str] = None) -> Dict[str, Any]
get_records(self, table_name: Optional[str] = None, base_id: Optional[str] = None, **params) -> Dict[str, Any]
get_table_schema(self, table_name: str, base_id: Optional[str] = None) -> Dict[str, Any]
list_bases(self) -> List[Dict[str, Any]]
sync_model_to_table(self, model_class: Type, table_name: Optional[str] = None, base_id: Optional[str] = None, create_missing_fields: bool = True, update_field_types: bool = False) -> Dict[str, Any]
update_record(self, record_id: str, fields: Dict[str, Any], table_name: Optional[str] = None, base_id: Optional[str] = None) -> Dict[str, Any]
update_table(self, table_id: str, updates: Dict[str, Any], base_id: Optional[str] = None) -> Dict[str, Any]
validate_model_against_table(self, model_class: Type, table_name: Optional[str] = None, base_id: Optional[str] = None) -> Dict[str, Any]
```

## AirtableClient methods

```python
batch_create(self, records: List[Dict[str, Any]]) -> List[Dict[str, Any]]
batch_update(self, updates: List[Dict[str, Any]]) -> List[Dict[str, Any]]
create_record(self, fields: Dict[str, Any]) -> Dict[str, Any]
delete_record(self, record_id: str) -> Dict[str, Any]
get_record(self, record_id: str) -> Dict[str, Any]
list_records(self, fields: Optional[List[str]] = None, filter_by_formula: Optional[str] = None, max_records: Optional[int] = None, page_size: Optional[int] = None, sort: Optional[List[Dict[str, str]]] = None, view: Optional[str] = None) -> List[Dict[str, Any]]
update_record(self, record_id: str, fields: Dict[str, Any]) -> Dict[str, Any]
```

## Layer selection cheat sheet

- Use `AirtableModel` for typed application code and convenient CRUD.
- Use `AirtableManager` for bases, tables, schema sync, and validation.
- Use `AirtableClient` for low-level record calls and batch operations.
