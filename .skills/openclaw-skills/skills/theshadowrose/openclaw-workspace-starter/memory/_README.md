# Daily Memory

Your agent creates files here named `YYYY-MM-DD.md` with the day's events, decisions, and context.

These are raw logs — unfiltered, everything that happened. Your agent periodically reviews these and distills the important stuff into `MEMORY.md` (long-term memory).

You shouldn't need to edit these manually, but they're plain markdown if you want to.
