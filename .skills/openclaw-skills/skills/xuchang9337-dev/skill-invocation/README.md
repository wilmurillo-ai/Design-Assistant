# Skill Invocation System

Decision framework for OpenClaw "skill selection and invocation flow".

Core rules:
- Use `TOOLS.md` to match candidate skills.
- When multiple candidates match, pick the most specific one.
- If unsure, read the candidate skill's `SKILL.md`.
- Final fallback: `if in doubt, use it`.

