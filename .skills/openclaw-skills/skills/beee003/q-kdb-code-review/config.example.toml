[skills.q-kdb-code-review]
# Required: Get a free key at https://as-trai.com
ASTRAI_API_KEY = "your_key_here"

# BYOK: Add provider keys for best routing (optional)
# ANTHROPIC_API_KEY = ""
# OPENAI_API_KEY = ""

# Review strictness: standard | strict | security
# - standard: bugs, type errors, join semantics, null handling
# - strict:   + performance (attributes, peach, vector ops), style
# - security: + IPC handlers, eval injection, .z.pw, port exposure
REVIEW_STRICTNESS = "standard"
