# Agent ROS Bridge Configuration with Authentication Enabled

bridge:
  name: "agent_ros_bridge"
  
  transports:
    websocket:
      port: 8766
      host: "0.0.0.0"
      auth:
        enabled: true
        jwt_secret: "8GpxD7Zp6L6bGl_bHbzLmNNaEpyKE3Sh1xEzO-dJfAo"
        jwt_expiry_hours: 24
        api_keys:
          "ak_test123":
            user_id: "test_user"
            roles: ["operator"]
  
  connectors:
    ros:
      auto_detect: false
      endpoints: []

  security:
    enabled: false
  
  telemetry:
    enabled: false
