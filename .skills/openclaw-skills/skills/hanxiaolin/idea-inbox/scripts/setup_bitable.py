#!/usr/bin/env python3
from __future__ import annotations

"""One-time setup helper for idea-inbox.

Creates a Bitable app + uses the OpenClaw Feishu tools at runtime (not here).
This script is only a placeholder reference for what the agent will do; the
actual creation is handled by the agent using feishu_* tools.

We keep this file so ClawHub users can inspect the intended setup steps.
"""

print("This skill uses OpenClaw Feishu tools to create its Bitable on first run.")
