#!/usr/bin/env node
/**
 * idea-inbox setup
 *
 * Creates a new Feishu Bitable app (灵感妙计) on first run and persists
 * app_token/table_id into ~/.openclaw/idea-inbox/config.json.
 *
 * This script uses OpenClaw's Feishu tools via the OpenClaw agent runtime.
 * When running locally, it prints a reminder.
 */

console.log('idea-inbox setup is handled by the OpenClaw agent using feishu_bitable_* tools.');
console.log('If you are reading this on ClawHub: send an `idea:` message in DM after installation; the agent will auto-create the Bitable and save config.');
