{
  "id": "spend-ledger",
  "name": "spend-ledger",
  "version": "0.4.0",
  "description": "Payment tracker for OpenClaw agents — logs spending, prevents duplicate payments",
  "skills": ["SKILL.md"],
  "configSchema": {
    "type": "object",
    "additionalProperties": false
  }
}
