#!/usr/bin/env python3
"""
第6轮迭代 - 激进优化策略
目标：提示词55%+，代码50%+
"""

# 新增激进优化规则
AGGRESSIVE_PROMPT_RULES = [
    # 超短替换
    (r'因为|由于|鉴于', '因'),
    (r'所以|因此|于是', '故'),
    (r'但是|然而|可是', '但'),
    (r'如果|假如|若是', '若'),
    (r'必须|务必|一定', '须'),
    (r'可以|能够|可能', '可'),
    (r'需要|要求|需求', '需'),
    (r'应该|应当|应', '应'),
    (r'已经|已然|业已', '已'),
    (r'正在|现在|当前', '正'),
    (r'将要|快要|即将', '将'),
    (r'完成|结束|完结', '毕'),
    (r'开始|着手|启动', '始'),
    (r'结果|结局|成果', '果'),
    (r'问题|疑问|难题', '题'),
    (r'方法|方式|办法', '法'),
    (r'情况|情形|状况', '况'),
    (r'内容|内含|包含', '容'),
    (r'信息|消息|资讯', '息'),
    (r'数据|资料|材料', '数'),
    # 删除填充词
    (r'[\s]*的[\s]*', '的'),
    (r'[\s]*了[\s]*', '了'),
    (r'[\s]*是[\s]*', '是'),
    (r'[\s]*有[\s]*', '有'),
]

# 代码激进优化
AGGRESSIVE_CODE_RULES = [
    # 简化常见模式
    (r'len\((\w+)\)\s*>\s*0', r'\1'),  # if len(x) > 0: -> if x:
    (r'len\((\w+)\)\s*==\s*0', r'not \1'),  # if len(x) == 0: -> if not x:
    (r'(\w+)\s*\+=\s*1', r'\1+=1'),  # x += 1 紧凑格式
    (r'(\w+)\s*-=\s*1', r'\1-=1'),
    # 单字母变量（局部作用域）
    (r'for\s+(\w{3,})\s+in\s+range', r'for i in range'),  # 简化循环变量
]

print("="*60)
print("🚀 第6轮迭代策略 - 激进优化")
print("="*60)
print("\n目标:")
print("  • 提示词优化: 47% → 55%+")
print("  • 代码优化: 40% → 50%+")
print("\n策略:")
print("  1. 单字替换（因为→因，所以→故）")
print("  2. 删除填充词（的/了/是/有）")
print("  3. 代码模式简化（len(x)>0 → x）")
print("  4. 紧凑格式（x += 1 → x+=1）")
print("="*60)
