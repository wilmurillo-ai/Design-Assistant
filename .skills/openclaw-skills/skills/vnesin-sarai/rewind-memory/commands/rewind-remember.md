# /rewind-remember

Store a note in memory.

## Usage

```
/rewind-remember <text>
```

## Behaviour

Run `rewind remember "<text>"` to store the provided text as a note in the memory system. This writes to L0 (sensory buffer) and extracts entities for L3 (knowledge graph).

Print the confirmation output from the command.
