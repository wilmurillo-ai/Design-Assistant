# /rewind-status

Show active memory layers and statistics.

## Usage

```
/rewind-status
```

## Behaviour

Run `rewind health` and display the output. This shows:
- Current tier (free/pro/enterprise)
- Orchestrator status
- Bio layer status
- Per-layer health (L0 through L6)
- Chunk counts and database sizes where available
