# Coding Task: status --json 模式 + 统一错误码

## Cognitive Mode

先准确理解任务意图，再动手实现。
- 只完成 Contract 直接要求的工作，不主动扩展需求。
- 如果发现问题超出 Contract 范围，停止扩写范围，改为在 field report 中说明。
- 优先复用现有代码、模式和项目约定，不要为展示能力引入额外复杂度。

## Scope

### Allowed Files
- packages/cli/src/commands/status.ts
- packages/core/src/errors.ts
- packages/core/src/index.ts

## Task Deliverables

- packages/core/src/errors.ts：NexumError class（extends Error），包含 code: string 字段；ErrorCode 枚举，包含：TASK_NOT_FOUND、CONTRACT_NOT_FOUND、INVALID_VERDICT、CONFIG_INVALID、GIT_ERROR、SESSION_TIMEOUT
- packages/cli/src/commands/status.ts：新增 --json flag，输出 JSON 格式的任务列表（含 id/name/status/iteration/acp_session_key/acp_stream_log 字段）；方便编排者程序化解析
- packages/core/src/index.ts：导出 NexumError 和 ErrorCode
- 各 CLI 命令中的 throw new Error(...) 改为 throw new NexumError(message, ErrorCode.XXX)
- pnpm build 通过

## Criteria Preview

- [C1] nexum status --json 输出合法 JSON 数组，包含所有任务的 id/name/status 字段 (method: integration: 在有 active-tasks.json 的目录运行，验证 JSON 输出 ; threshold: pass)
- [C2] NexumError 包含 code 字段，ErrorCode 枚举包含所有定义的错误码 (method: review: 检查 errors.ts 实现 ; threshold: pass)
- [C3] pnpm build 通过 (method: integration: 运行 pnpm build ; threshold: pass)

## Relevant Lessons

（无）

## Commit Instructions

完成实现后执行以下提交命令：

```bash
git add -- packages/cli/src/commands/status.ts packages/core/src/errors.ts packages/core/src/index.ts && git commit -m "feat(nx2-004): implement status --json 模式 + 统一错误码"
```

## Completeness Principle

- 交付必须可运行、可验证，不要留下"下一步再补"的半成品。
- 必须覆盖所有 deliverables；完成一部分不算完成。
- 改动应保持最小且闭环：代码、测试、文档更新应与任务边界一致。
- 不要为了通过评估伪造结果、跳过失败路径或隐藏已知问题。

## Field Report

完成后填写：

```markdown
## Field Report
- task: status --json 模式 + 统一错误码
- changed_files: [列出实际修改文件]
- deliverables_done: [逐条对应 deliverables 说明完成情况]
- tests_or_checks: [运行过的测试、命令或人工检查]
- blockers: [若无则写 none]
- notes_for_evaluator: [提醒 evaluator 关注的实现点；若无则写 none]
```
