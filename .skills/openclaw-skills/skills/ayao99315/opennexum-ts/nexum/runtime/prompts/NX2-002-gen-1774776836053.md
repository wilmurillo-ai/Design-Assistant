# Coding Task: nexum init 命令 + nexum.config.ts 配置加载

## Cognitive Mode

先准确理解任务意图，再动手实现。
- 只完成 Contract 直接要求的工作，不主动扩展需求。
- 如果发现问题超出 Contract 范围，停止扩写范围，改为在 field report 中说明。
- 优先复用现有代码、模式和项目约定，不要为展示能力引入额外复杂度。

## Scope

### Allowed Files
- packages/cli/src/commands/init.ts
- packages/cli/src/index.ts
- packages/core/src/config.ts
- packages/core/src/index.ts
- packages/core/src/__tests__/config.test.ts

## Task Deliverables

- packages/core/src/config.ts：NexumConfig 类型定义（notify.target、notify.botToken、agents 映射表 agentId→{cli:'codex'|'claude', model?:string}）；loadConfig(projectDir) 读取 nexum/config.json，返回 NexumConfig；resolveAgentCli(config, agentId) 返回对应的 CLI 类型
- packages/cli/src/commands/init.ts：nexum init 命令，在当前目录创建：nexum/active-tasks.json（空 tasks 数组）、nexum/config.json（含默认 agents 映射：codex→{cli:'codex'}、claude→{cli:'claude'}、eval→{cli:'codex'}）、docs/nexum/contracts/.gitkeep、nexum/runtime/eval/.gitkeep；如果目录已存在跳过并提示
- packages/cli/src/index.ts：注册 init 命令
- config.test.ts：测试 loadConfig 读取合法配置、缺失文件时返回默认值、resolveAgentCli 正确映射
- pnpm build && pnpm test 通过

## Criteria Preview

- [C1] nexum init 在新目录创建所有必要文件和目录 (method: unit: 在临时目录运行 init，验证文件存在 ; threshold: pass)
- [C2] nexum init 在已初始化目录幂等运行（不报错，不覆盖） (method: unit: 二次运行 init，验证已有文件未被修改 ; threshold: pass)
- [C3] resolveAgentCli 对未知 agentId 返回默认 codex (method: unit: 传入 'unknown-agent'，验证返回 'codex' ; threshold: pass)
- [C4] pnpm build && pnpm test 通过 (method: unit: 运行构建和测试 ; threshold: pass)

## Relevant Lessons

（无）

## Commit Instructions

完成实现后执行以下提交命令：

```bash
git add -- packages/cli/src/commands/init.ts packages/cli/src/index.ts packages/core/src/config.ts packages/core/src/index.ts packages/core/src/__tests__/config.test.ts && git commit -m "feat(nx2-002): implement nexum init 命令 + nexum.config.ts 配置加载"
```

## Completeness Principle

- 交付必须可运行、可验证，不要留下"下一步再补"的半成品。
- 必须覆盖所有 deliverables；完成一部分不算完成。
- 改动应保持最小且闭环：代码、测试、文档更新应与任务边界一致。
- 不要为了通过评估伪造结果、跳过失败路径或隐藏已知问题。

## Field Report

完成后填写：

```markdown
## Field Report
- task: nexum init 命令 + nexum.config.ts 配置加载
- changed_files: [列出实际修改文件]
- deliverables_done: [逐条对应 deliverables 说明完成情况]
- tests_or_checks: [运行过的测试、命令或人工检查]
- blockers: [若无则写 none]
- notes_for_evaluator: [提醒 evaluator 关注的实现点；若无则写 none]
```
