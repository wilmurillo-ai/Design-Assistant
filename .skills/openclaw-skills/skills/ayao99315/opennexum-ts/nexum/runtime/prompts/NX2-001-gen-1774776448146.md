# Coding Task: 完善 Task 类型定义 + core 类型修复

## Cognitive Mode

先准确理解任务意图，再动手实现。
- 只完成 Contract 直接要求的工作，不主动扩展需求。
- 如果发现问题超出 Contract 范围，停止扩写范围，改为在 field report 中说明。
- 优先复用现有代码、模式和项目约定，不要为展示能力引入额外复杂度。

## Scope

### Allowed Files
- packages/core/src/types.ts
- packages/core/src/tasks.ts
- packages/core/src/__tests__/tasks.test.ts

## Task Deliverables

- packages/core/src/types.ts：Task interface 补充以下字段（均可选）：acp_session_key、acp_stream_log、started_at、completed_at、eval_result_path、base_commit、last_error；TaskStatus 枚举补充 Evaluating = 'evaluating' 和 Cancelled = 'cancelled'
- packages/core/src/tasks.ts：updateTask 的 patch 参数类型改为 Partial<Task>，移除任何 Record<string,unknown> 类型断言
- packages/core/src/types.ts：新增 EvalVerdict 类型导出：type EvalVerdict = 'pass' | 'fail' | 'escalated'
- packages/core/src/__tests__/tasks.test.ts：新增测试，验证 updateTask 能正确写入 acp_session_key 和 acp_stream_log 字段
- pnpm build 和 pnpm test 均通过，无 TypeScript 编译错误

## Criteria Preview

- [C1] Task interface 包含所有新字段，均为可选类型 (method: review: 检查 types.ts 的 Task interface ; threshold: pass)
- [C2] TaskStatus 包含 Evaluating 和 Cancelled (method: review: 检查 TaskStatus 枚举 ; threshold: pass)
- [C3] updateTask patch 参数类型为 Partial<Task>，无类型断言绕过 (method: review: 检查 tasks.ts 的 updateTask 函数签名 ; threshold: pass)
- [C4] pnpm build && pnpm test 通过 (method: unit: 运行构建和测试 ; threshold: pass)

## Relevant Lessons

（无）

## Commit Instructions

完成实现后执行以下提交命令：

```bash
git add -- packages/core/src/types.ts packages/core/src/tasks.ts packages/core/src/__tests__/tasks.test.ts && git commit -m "feat(nx2-001): implement 完善 Task 类型定义 + core 类型修复"
```

## Completeness Principle

- 交付必须可运行、可验证，不要留下"下一步再补"的半成品。
- 必须覆盖所有 deliverables；完成一部分不算完成。
- 改动应保持最小且闭环：代码、测试、文档更新应与任务边界一致。
- 不要为了通过评估伪造结果、跳过失败路径或隐藏已知问题。

## Field Report

完成后填写：

```markdown
## Field Report
- task: 完善 Task 类型定义 + core 类型修复
- changed_files: [列出实际修改文件]
- deliverables_done: [逐条对应 deliverables 说明完成情况]
- tests_or_checks: [运行过的测试、命令或人工检查]
- blockers: [若无则写 none]
- notes_for_evaluator: [提醒 evaluator 关注的实现点；若无则写 none]
```
