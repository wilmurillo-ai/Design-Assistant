# Coding Task: spawn/eval/complete 集成 config agent 映射

## Cognitive Mode

先准确理解任务意图，再动手实现。
- 只完成 Contract 直接要求的工作，不主动扩展需求。
- 如果发现问题超出 Contract 范围，停止扩写范围，改为在 field report 中说明。
- 优先复用现有代码、模式和项目约定，不要为展示能力引入额外复杂度。

## Scope

### Allowed Files
- packages/cli/src/commands/spawn.ts
- packages/cli/src/commands/eval.ts
- packages/cli/src/commands/complete.ts
- packages/cli/src/__tests__/spawn.test.ts

## Task Deliverables

- packages/cli/src/commands/spawn.ts：runSpawn 从 nexum/config.json 读取 config，用 resolveAgentCli(config, contract.generator) 决定实际使用的 CLI；SpawnPayload 新增 agentCli: 'codex'|'claude' 字段，供编排者 sessions_spawn 时选择正确的 agentId
- packages/cli/src/commands/eval.ts：runSpawnEval 同样用 resolveAgentCli(config, contract.evaluator) 决定 evaluator CLI；SpawnPayload 的 agentId 使用 resolveAgentCli 结果
- packages/cli/src/commands/complete.ts：retry 时的 retryPayload 同样包含 agentCli 字段
- spawn.test.ts：新增测试，验证 config 中 eval→codex 映射被正确解析到 SpawnPayload.agentCli
- pnpm build && pnpm test 通过

## Criteria Preview

- [C1] SpawnPayload 包含 agentCli 字段，值来自 resolveAgentCli (method: review: 检查 SpawnPayload 类型和 runSpawn 实现 ; threshold: pass)
- [C2] contract.evaluator = 'eval' 时，agentCli 解析为 config 中配置的值（默认 'codex'） (method: unit: mock config，验证 eval 映射 ; threshold: pass)
- [C3] pnpm build && pnpm test 通过 (method: unit: 运行构建和测试 ; threshold: pass)

## Relevant Lessons

（无）

## Commit Instructions

完成实现后执行以下提交命令：

```bash
git add -- packages/cli/src/commands/spawn.ts packages/cli/src/commands/eval.ts packages/cli/src/commands/complete.ts packages/cli/src/__tests__/spawn.test.ts && git commit -m "feat(nx2-003): implement spawn/eval/complete 集成 config agent 映射"
```

## Completeness Principle

- 交付必须可运行、可验证，不要留下"下一步再补"的半成品。
- 必须覆盖所有 deliverables；完成一部分不算完成。
- 改动应保持最小且闭环：代码、测试、文档更新应与任务边界一致。
- 不要为了通过评估伪造结果、跳过失败路径或隐藏已知问题。

## Field Report

完成后填写：

```markdown
## Field Report
- task: spawn/eval/complete 集成 config agent 映射
- changed_files: [列出实际修改文件]
- deliverables_done: [逐条对应 deliverables 说明完成情况]
- tests_or_checks: [运行过的测试、命令或人工检查]
- blockers: [若无则写 none]
- notes_for_evaluator: [提醒 evaluator 关注的实现点；若无则写 none]
```
