# Test cases for csv-to-json skill

tests:
  - name: "Convert simple CSV"
    input: "name,age\nAlice,30\nBob,25"
    expected:
      has_field: data
      has_field: row_count
      has_field: columns
      metadata_has: [skill, trace_id, timestamp]
