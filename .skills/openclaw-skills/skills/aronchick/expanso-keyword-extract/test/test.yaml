# Test cases for keyword-extract skill

tests:
  - name: "Extract keywords from text"
    input: "Machine learning and artificial intelligence are transforming technology."
    expected:
      has_field: keywords
      has_field: phrases
      has_field: topics
      metadata_has: [skill, trace_id, timestamp]
