# Test cases for json-to-csv skill

tests:
  - name: "Convert JSON array"
    input: '[{"name":"Alice","age":30}]'
    expected:
      has_field: csv
      has_field: row_count
      has_field: columns
      metadata_has: [skill, trace_id, timestamp]
