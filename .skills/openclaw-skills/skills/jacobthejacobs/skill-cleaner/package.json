{
  "name": "skill-cleaner",
  "version": "2.4.0",
  "description": "Hardened security utility to verify suspicious skills via VirusTotal and the Security Bridge.",
  "keywords": ["security", "virus-total", "trust", "cleaner"],
  "scripts": {
    "start": "node --import tsx scripts/clean.ts"
  },
  "dependencies": {
    "tsx": "^4.21.0"
  },
  "openclaw": {
    "requires": {
      "env": ["VIRUSTOTAL_API_KEY"],
      "bin": ["openclaw"]
    },
    "category": "security",
    "priority": "high"
  }
}
