# Seed Postcards

These 3 postcards are **example/seed data** from the initial skill design phase. They use an older format (YAML front matter + `pc-NNN` naming) that differs from the production format (`NNN-slug.md`, no front matter).

**Do not use these as format references.** The canonical format is defined in `references/postcard-format.md`.

Production postcards are stored in the runtime directory: `wanderclaw/postcards/`

Moved from `postcards/` to `assets/seed-postcards/` in iteration #8 for cleanliness.
