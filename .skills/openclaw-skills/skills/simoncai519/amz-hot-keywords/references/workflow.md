# Workflow for amz-hot-keywords\n\nThis document describes the full end‑to‑end process performed by the `amz-hot-keywords` skill. It is intended for developers and advanced users who need to understand or modify the scraper.\n\n## Prerequisites\n- Python 3.9+ installed on the host.\n- Google Chrome browser (any recent version).\n- Internet access to `https://www.amz123.com`.\n- Python dependencies: `selenium`, `pandas`, `python-dotenv` (optional for credentials). Install with:\n  ```bash\n  pip install selenium pandas python-dotenv\n  ```\n- (Optional) ChromeDriver is auto‑managed by Selenium 4.6+. No manual driver needed.\n\n## Step‑by‑Step Procedure\n1. **Argument Parsing**\n   - The script uses `argparse` to read CLI flags: `--keyword`, `--max-results`, `--output-dir`, `--format`, `--headless`.\n   - Validation ensures `--max-results` ≤ 200 and `--format` is either `csv` or `json`.\n\n2. **Launch Selenium Chrome**\n   - If `--headless true`, Chrome runs without UI.\n   - Selenium options set a reasonable timeout (30 s) and a custom user‑agent to avoid simple bot blocks.\n\n3. **Navigate to AMZ123**\n   - URL: `https://www.amz123.com/usatopkeywords`\n   - Wait for the search input element (`SELECTORS['search_input']`).\n\n4. **Perform Search**\n   - Input the supplied `--keyword` string, submit the form, and wait for results to load.\n   - The page updates a table of related search terms.\n\n5. **Scrape Table Rows**\n   - The script defines a `SELECTORS` dict containing CSS selectors for the rows and individual columns (keyword, current rank, last week rank).\n   - It iterates over rows, extracts text, converts rank strings to integers, and stores them in a list of dicts.\n   - Scraping stops when the desired `--max-results` count is reached or the table ends.\n\n6. **Calculate Trend**\n   - For each entry, compare `current_rank` with `last_rank`:\n     - `current_rank < last_rank` → `up`\n     - `current_rank > last_rank` → `down`\n     - `current_rank == last_rank` → `flat`\n     - `last_rank` missing → `new`\n\n7. **Create Output DataFrame**\n   - A `pandas.DataFrame` is built with columns: `search_term`, `current_rank`, `last_rank`, `trend`.\n   - The DataFrame is sorted by `current_rank` ascending (most popular first).\n\n8. **Write Output File**\n   - Filename pattern: `amz123_hotwords_<keyword>_<YYYYmmdd_HHMMSS>.<ext>` where `<ext>` matches the chosen format.\n   - For CSV, `df.to_csv(path, index=False)`.\n   - For JSON, `df.to_json(path, orient="records", force_ascii=False, lines=False)`.\n   - The script prints the absolute path of the created file to stdout for downstream callers.\n\n9. **Cleanup**\n   - Selenium driver quits, releasing Chrome resources.\n   - Any temporary files (e.g., debug screenshots) are removed unless `--headless false` was used for troubleshooting.\n\n## Troubleshooting Tips\n- **Empty results** – The page layout may have changed. Open the browser with `--headless false` and inspect the HTML. Update `SELECTORS` accordingly.\n- **Timeouts** – Increase Selenium's implicit wait (`driver.implicitly_wait(30)`) or network timeout if the site is slow.\n- **CAPTCHA / bot detection** – Add realistic delays (`time.sleep(random.uniform(1,3))`) between actions or rotate user‑agents.\n- **Chrome version mismatch** – Ensure Chrome and Selenium are compatible; upgrade Selenium if Chrome auto‑updates.\n\n## Extending the Skill\n- **Additional columns** – The page also shows search volume and click‑through rate; add selectors and map them into the DataFrame.\n- **Multiple locales** – AMZ123 supports other country pages; make the base URL a parameter.\n- **Authentication** – If ABA data becomes gated, integrate credential handling via `python-dotenv` and pass cookies to Selenium.\n\n---