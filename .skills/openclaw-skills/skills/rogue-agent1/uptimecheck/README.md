# uptimecheck ✅

Lightweight URL uptime monitor. Check if your endpoints are up, measure response times, track history. Zero dependencies.

## Usage
```bash
python3 uptimecheck.py check https://github.com https://google.com
python3 uptimecheck.py check -f urls.txt --save
python3 uptimecheck.py history --url github.com
```
