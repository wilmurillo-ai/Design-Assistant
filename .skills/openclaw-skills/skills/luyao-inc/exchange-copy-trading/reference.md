# 交易所跟单 — 参考与扩展

## Bitget（当前）

| 项目 | 说明 |
|------|------|
| 场景 | 合约跟单设置页（示例） |
| 示例 URL | `https://www.bitget.fit/zh-CN/copy-trading/setting/bfb7477187b73155a395/futures` |
| 前置 | Web 已登录；可用 USDT ≥ 50（以产品与页面规则为准） |
| 自动化动作 | 填入跟单金额 → 点击跟单/确认（及可能出现的二次确认） |

**域名说明**：请以用户或官方提供的域名为准；发布到 ClawHub 的技能不保证链接长期有效。页面改版时需更新 SKILL.md 中的步骤描述。

## Binance（预留）

- 状态：**未实现**，待补充官方跟单/跟单员设置页 URL 与步骤。
- 上线时在 `SKILL.md` 增加 `### Binance` 并在此表填入链接与注意点。

## OKX（预留）

- 状态：**未实现**，待补充官方跟单入口与步骤。
- 同上维护方式。

## 维护清单（扩展新所时）

- [ ] 在 `reference.md` 增加一行交易所与 URL。
- [ ] 在 `SKILL.md` 正文增加该所专属小节（前置条件、具体点击顺序）。
- [ ] 视需要 bump `version` 并重新发布到 Clawhub。
