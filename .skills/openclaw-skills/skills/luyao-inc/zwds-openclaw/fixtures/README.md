# fixtures

预生成的命盘 JSON（`_fixture` 元数据 + CLI 的 `success` / `data` / `meta`）。

- **新增或更新**：在 `zwds-cli` 目录执行 `npm run save-fixture`（参数见上级 `examples.md`）。
- **读盘**：对模型 @ 本目录下某 `.json` 即可；解盘只依据 `data` 与必要的 `meta`。
