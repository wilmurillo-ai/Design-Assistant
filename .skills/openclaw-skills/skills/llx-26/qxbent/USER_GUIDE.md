# 启信宝企业查询 Skill - 用户指南

欢迎使用启信宝企业信息查询 Skill！本指南将帮助你快速开始使用。

## 第一步：获取 API Token

在使用本 Skill 前，你需要获取启信宝 API Token。

**如何获取：**
- 联系启信宝平台管理员获取 API Token
- Token 格式类似：`ent_xxx_xxxxxxxxxx`

## 第二步：配置 Token

**⚠️ 重要安全提示**

Token 是访问 API 的敏感凭证，请务必：
- ✅ 必须使用环境变量配置
- ❌ 不要在对话中提供 token
- ❌ 不要将 token 提交到代码仓库
- 🔒 Token 仅存储在本地系统环境变量中，不会上传到云端

### 配置环境变量

这是唯一推荐的安全方式，配置一次后永久有效。

#### Windows 用户

1. 按 `Win + R`，输入 `sysdm.cpl`，按回车
2. 点击"高级" → "环境变量"
3. 在"用户变量"中点击"新建"
4. 变量名：`QXBENT_API_TOKEN`
5. 变量值：粘贴你的 Token
6. 点击"确定"保存
7. **重启** Claude、OpenClaw 等 AI 应用

#### Mac 用户

打开终端，编辑配置文件：

```bash
# 如果使用 bash
echo 'export QXBENT_API_TOKEN="your_token_here"' >> ~/.bashrc
source ~/.bashrc

# 如果使用 zsh (Mac 默认)
echo 'export QXBENT_API_TOKEN="your_token_here"' >> ~/.zshrc
source ~/.zshrc
```

#### Linux 用户

打开终端，编辑配置文件：

```bash
echo 'export QXBENT_API_TOKEN="your_token_here"' >> ~/.bashrc
source ~/.bashrc
```

配置完成后，重启你的 AI 应用（Claude、OpenClaw 等），环境变量即可生效。

## 第三步：开始使用

配置完成后，你就可以开始查询企业信息了！

### 基本查询示例

**查询企业工商信息：**
```
查询上海合合信息科技发展有限公司的工商信息
```

AI 会返回：
- 企业名称
- 统一社会信用代码
- 法定代表人
- 注册资本
- 成立日期
- 经营状态
- 所属行业
- 注册地址
- 经营范围
- 等等...

**查询企业股东：**
```
帮我查一下上海合合信息科技发展有限公司的股东信息
```

AI 会返回：
- 股东名称
- 持股比例
- 认缴出资额
- 认缴出资日期
- 等等...

**查询主要人员：**
```
查询上海合合信息科技发展有限公司的主要人员
```

AI 会返回：
- 人员姓名
- 职务
- 持股比例
- 等等...

**查询变更记录：**
```
查看上海合合信息科技发展有限公司最近的变更记录
```

AI 会返回最近的工商变更记录。

## 高级技巧

### 1. 企业名称不完整怎么办？

如果你只知道企业简称，AI 会自动帮你找到候选企业：

```
查询胜宏科技的信息
```

AI 会回复：
```
找到多个匹配的企业，请选择：
1. 胜宏科技（惠州）股份有限公司
2. 惠州市胜宏科技研究院有限公司
3. 南通胜宏科技有限公司
4. 武汉市胜宏科技有限公司
5. 北京胜宏科技有限公司
```

你只需要回复：
```
第一个
```

AI 会自动用完整名称重新查询。

### 2. 一次查询多个信息

你可以一次性要求多个信息：

```
查询上海合合信息科技发展有限公司的工商信息、股东信息和主要人员
```

AI 会依次返回所有信息。

### 3. 批量查询

你可以查询多个企业：

```
分别查询以下企业的法定代表人：
1. 上海合合信息科技发展有限公司
2. 阿里巴巴（中国）有限公司
3. 腾讯科技（深圳）有限公司
```

## 常见问题

### Q1: 提示"请提供 API Token"？

**A:** 你还没有配置 Token，请按照"第二步"的说明配置。

### Q2: 提示"API Token 无效"？

**A:** Token 可能错误或已过期，请检查：
1. Token 是否完整复制
2. 是否有多余的空格
3. Token 是否已过期（联系管理员）

### Q3: 查询速度慢？

**A:** 这是正常的，因为需要调用启信宝 API。通常查询时间在 1-3 秒。

### Q4: 查询次数有限制吗？

**A:** 是的，API Token 有调用次数限制，具体限制取决于你的 Token 权限。

### Q5: 可以查询哪些企业？

**A:** 可以查询中国境内所有在工商局注册的企业。

### Q6: 数据更新频率？

**A:** 数据来源于官方工商公示系统，更新频率与官方同步。

## 需要帮助？

如果遇到问题：
1. 检查 Token 是否正确配置
2. 尝试重启 AI 应用
3. 查看项目 README 获取更多信息
4. 在项目 GitHub 提交 Issue

## 隐私和安全

**Token 存储：**
- ✅ Token 仅存储在你的本地系统环境变量中
- ✅ 不会上传到任何云服务或第三方服务器
- ✅ AI 应用从本地环境变量读取，不经过对话传输

**API 访问：**
- 🔒 只访问 `https://external-api.qixin.com/skill/ent/public` API 端点
- 📊 建议申请权限受限的 token（如每日配额限制）
- 🔐 查询记录不会被 skill 保存

**安全建议：**
- ❌ 绝不在对话中提供 token
- ❌ 绝不将 token 提交到代码仓库或分享给他人
- ✅ 定期检查 token 使用情况
- ✅ 如怀疑 token 泄露，立即联系管理员更换

## 开始探索吧！

现在你已经完成配置，可以开始探索企业信息了。试试查询你感兴趣的企业吧！
