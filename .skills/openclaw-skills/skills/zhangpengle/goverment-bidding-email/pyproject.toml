[project]
name = "govb-email"
version = "0.1.0"
description = "政府采购商机邮件发送工具"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "requests",
]

[project.scripts]
govb-email = "govb_email.fetcher:main"

[tool.setuptools.packages.find]
where = ["."]
