[project]
name = "milb-fetcher"
version = "0.1.0"
description = "军工采购商机自动抓取工具"
requires-python = ">=3.8"
dependencies = [
    "requests",
    "pandas",
    "openpyxl",
]

[project.scripts]
milb-fetcher = "milb_fetcher.fetcher:main"

[tool.setuptools.packages.find]
where = ["."]
