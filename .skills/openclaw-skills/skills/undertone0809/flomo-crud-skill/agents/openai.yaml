interface:
  display_name: "Flomo Web CRUD"
  short_description: "Operate live flomo memos via Chrome MCP Web UI automation"
  default_prompt: "Use Chrome MCP tools to search, create, edit, or delete live flomo memos in my logged-in flomo Web session. Use text search first, confirm candidates before writes, and always require second confirmation before delete."
