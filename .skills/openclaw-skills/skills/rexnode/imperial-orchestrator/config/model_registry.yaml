# Imperial Orchestrator - Model Registry
# This file is AUTO-GENERATED by health_check.py from openclaw.json
# Do NOT edit manually — run: python3 scripts/health_check.py --write-registry
#
# If you need to override capabilities or roles for a specific model,
# add entries under "overrides:" section below.

defaults:
  default_context_window: 32768
  default_latency_tier: medium
  default_cost_tier: medium
  prefer_local_for_survival: true

# Auto-discovered models are injected at runtime by health_check.py
# and stored in the state file (.imperial_state.json)
models: {}

# Manual overrides: add model-specific capability/role tweaks here.
# These merge on top of auto-discovered attributes.
overrides:
  # Qwen3.5 abliterated 支持 function calling / tool use
  ollama/huihui_ai/qwen3.5-abliterated:27b-Claude:
    capabilities: [tool_use_stable]
    roles: [ministry-ops, ministry-coding]
  ollma/huihui_ai/qwen3.5-abliterated:27b-Claude:
    capabilities: [tool_use_stable]
    roles: [ministry-ops, ministry-coding]
