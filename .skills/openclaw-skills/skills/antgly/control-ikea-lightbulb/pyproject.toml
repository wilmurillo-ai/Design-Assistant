[project]
name = "control-ikea-lightbulb"
version = "0.0.0"
description = "Control IKEA/TP-Link Kasa smart bulbs on the LAN."
requires-python = ">=3.11, <4.0"
dependencies = [
  "python-kasa>=0.10.2",
]

[tool.uv]
# No dev dependencies for now; keep this section for easy extension.
