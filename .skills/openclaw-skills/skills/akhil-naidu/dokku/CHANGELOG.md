# Changelog

All notable changes to this skill are documented here. Version numbers align with `clawhub publish --version`.

## [1.0.0] - 2025-02-01

### Added

- Initial release.
- SKILL.md with frontmatter and section index.
- Command sections: apps, config, domains, git, run, logs, ps, plugin, certs, nginx, storage, network.
- Lifecycle sections: install (Dokku installation), upgrade (upgrade + migration guides), cleanup (Docker prune + container/app cleanup).
- Background run guidance: `dokku run:detached`, exec with backgrounding for long deploys.
