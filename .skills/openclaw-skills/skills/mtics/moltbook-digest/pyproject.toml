[project]
name = "moltbook-digest"
version = "0.1.0"
description = "Collect Moltbook evidence and generate analysis-ready outputs."
requires-python = ">=3.10"
dependencies = [
  "litellm>=1.60.8",
  "pyyaml>=6.0.2",
]

[tool.uv]
package = false
