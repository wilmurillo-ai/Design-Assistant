// Store utilities - now mostly handled by daemon
// This file remains for type exports and any offline utilities

import type { Message, Peer } from '../types.js';

export type { Message, Peer };

// Re-export for backwards compatibility if needed
