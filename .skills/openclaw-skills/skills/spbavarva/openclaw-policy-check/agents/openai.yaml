interface:
  display_name: "OpenClaw Policy Check"
  short_description: "Preflight scan for risky code patterns"
  default_prompt: "Use $openclaw-policy-check to run a preflight scan on this repo and summarize risky findings."

policy:
  allow_implicit_invocation: true
