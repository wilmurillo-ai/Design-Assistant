interface:
  display_name: "OpenClaw Quick Scan"
  short_description: "Run fast OpenClaw security triage scans"
  default_prompt: "Use $openclaw-shield-quickscan to scan this folder and summarize the highest-risk findings."

policy:
  allow_implicit_invocation: true
