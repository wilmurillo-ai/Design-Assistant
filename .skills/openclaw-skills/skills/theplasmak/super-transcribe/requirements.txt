# Backend requirements are installed separately via each backend's setup script
# super-transcribe lazily loads backends on first use — only the chosen backend is installed

# faster-whisper backend (scripts/backends/faster-whisper/requirements.txt):
#   faster-whisper>=1.2.1
#   + PyTorch with CUDA (installed by setup.sh if GPU detected)

# parakeet backend (scripts/backends/parakeet/requirements.txt):
#   nemo_toolkit[asr]
#   soundfile
#   pyyaml
#   omegaconf
#   + PyTorch with CUDA (installed by setup.sh if GPU detected)
