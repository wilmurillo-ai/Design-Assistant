interface:
  display_name: "Git Backup"
  short_description: "Create a local Git bundle backup"
  default_prompt: "Create a local Git bundle backup of the workspace repository and report the path and size."
