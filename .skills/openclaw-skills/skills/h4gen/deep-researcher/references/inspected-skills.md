# Inspected Upstream Skills

Directly inspected from ClawHub:

- `deepresearchwork` latest `1.0.0`
- `tavily-search` latest `1.0.0`
- `perplexity-deep-search` latest `1.0.0`
- `literature-search` latest `1.0.3`

## Mapping note for requested stack

Requested tool: `semantic-scholar`

ClawHub status during inspection: no exact `semantic-scholar` slug found.

Operational mapping used in this meta-skill:
- Use `literature-search` as the academic retrieval layer because its scope explicitly includes Semantic Scholar alongside PubMed/arXiv/IEEE/ACM/others.

This mapping must be disclosed in final output whenever used.
