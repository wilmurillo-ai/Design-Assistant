# OpenClaw Integration Entry

Use `prompts/openclaw-system.md` as system prompt text.

Optional but recommended for strict Chinese output:

- add `.cursor/skills/bangboo-voice/reference-lexicon.md` into memory/knowledge.

Fallback behavior without lexicon remains available in the system prompt.
