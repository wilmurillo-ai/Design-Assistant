#!/bin/bash
# Gather cron job information
# Uses the cron tool output format

# This script outputs a placeholder - actual cron list comes from the cron tool
# The agent should use: cron action:"list" includeDisabled:true

echo '{"note": "Use cron tool directly: cron action:list includeDisabled:true"}'
