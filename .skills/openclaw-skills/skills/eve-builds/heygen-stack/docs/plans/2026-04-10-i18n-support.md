# i18n Support Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Make heygen-stack skills work correctly for non-English users without regressing English quality.

**Architecture:** Add a Language Detection step at skill entry, thread `user_language` through voice selection and script generation, convert hardcoded English user-facing messages to semantic descriptions the LLM adapts to the user's language, and keep all API-level technical directives (motion verbs, frame check corrections, style blocks) in English. Build an eval suite that tests both English and non-English scenarios to catch regressions.

**Tech Stack:** Markdown skill files, HeyGen v3 API, JSON eval scenarios

---

### Task 1: Build i18n Eval Suite

**Files:**
- Create: `evals/i18n/scenarios.json`
- Create: `evals/i18n/rubric.md`
- Create: `evals/i18n/README.md`

The eval suite tests skill behavior across 4 languages: English (baseline), Japanese, Spanish, Korean. Each scenario simulates a user request and defines expected behaviors.

- [ ] **Step 1: Create eval directory structure**

```bash
mkdir -p evals/i18n/baselines
```

- [ ] **Step 2: Write scenarios.json**

```json
{
  "version": "1.0",
  "languages": ["en", "ja", "es", "ko"],
  "scenarios": [
    {
      "id": "routing-full-producer",
      "skill": "heygen-stack",
      "description": "Vague video request triggers Full Producer mode",
      "inputs": {
        "en": "make a video about our new product launch",
        "ja": "新製品の発売についてのビデオを作って",
        "es": "haz un video sobre el lanzamiento de nuestro nuevo producto",
        "ko": "신제품 출시에 대한 비디오를 만들어줘"
      },
      "expected": {
        "mode": "Full Producer",
        "starts_at": "Discovery",
        "user_facing_language": "{input_language}",
        "api_prompt_language": "{input_language} content + English technical directives"
      }
    },
    {
      "id": "routing-quick-shot",
      "skill": "heygen-stack",
      "description": "Quick shot / skip questions request",
      "inputs": {
        "en": "just generate a video, skip the questions",
        "ja": "質問はスキップして、ビデオを生成して",
        "es": "solo genera un video, salta las preguntas",
        "ko": "질문 건너뛰고 비디오 바로 만들어"
      },
      "expected": {
        "mode": "Quick Shot",
        "starts_at": "Generate",
        "user_facing_language": "{input_language}"
      }
    },
    {
      "id": "routing-buddy",
      "skill": "heygen-stack",
      "description": "Buddy pipeline trigger",
      "inputs": {
        "en": "bring my buddy to life",
        "ja": "バディに命を吹き込んで",
        "es": "dale vida a mi buddy",
        "ko": "내 버디를 살려줘"
      },
      "expected": {
        "mode": "Buddy Pipeline",
        "starts_at": "buddy-to-avatar/SKILL.md",
        "user_facing_language": "{input_language}"
      }
    },
    {
      "id": "discovery-interview",
      "skill": "heygen-video-producer",
      "description": "Discovery questions asked in user's language",
      "inputs": {
        "en": "I want to make a product demo video",
        "ja": "製品デモビデオを作りたいです",
        "es": "quiero hacer un video de demostración de producto",
        "ko": "제품 데모 비디오를 만들고 싶어요"
      },
      "expected": {
        "discovery_questions_language": "{input_language}",
        "gathers_all_10_fields": true,
        "language_field_detected": true
      }
    },
    {
      "id": "voice-selection-language",
      "skill": "heygen-avatar-designer",
      "description": "Voice selected matches user's language",
      "inputs": {
        "en": "create my avatar, I speak English",
        "ja": "アバターを作って、日本語を話します",
        "es": "crea mi avatar, hablo español",
        "ko": "아바터를 만들어줘, 한국어를 해요"
      },
      "expected": {
        "voice_api_language_filter": "{input_language_code}",
        "voice_settings_locale": "{locale_code}",
        "user_facing_language": "{input_language}"
      }
    },
    {
      "id": "test-video-prompt",
      "skill": "heygen-avatar-designer",
      "description": "Phase 5 test video uses user's language",
      "inputs": {
        "en": "test the avatar",
        "ja": "アバターをテストして",
        "es": "prueba el avatar",
        "ko": "아바터 테스트해줘"
      },
      "expected": {
        "test_prompt_language": "{input_language}",
        "not_hardcoded_english": true
      }
    },
    {
      "id": "script-generation",
      "skill": "heygen-video-producer",
      "description": "Script generated in user's language",
      "inputs": {
        "en": "make a 30 second video about AI trends",
        "ja": "AIトレンドについての30秒ビデオを作って",
        "es": "haz un video de 30 segundos sobre tendencias de IA",
        "ko": "AI 트렌드에 대한 30초 비디오를 만들어줘"
      },
      "expected": {
        "script_language": "{input_language}",
        "style_block_language": "en",
        "motion_verbs_language": "en",
        "frame_check_language": "en"
      }
    },
    {
      "id": "prompt-craft-mixed",
      "skill": "heygen-video-producer",
      "description": "Final prompt has content in user language + English technical directives",
      "inputs": {
        "en": "product launch video, cinematic style, 60 seconds",
        "ja": "製品発売ビデオ、シネマティックスタイル、60秒",
        "es": "video de lanzamiento de producto, estilo cinematográfico, 60 segundos",
        "ko": "제품 출시 비디오, 시네마틱 스타일, 60초"
      },
      "expected": {
        "script_content_language": "{input_language}",
        "script_framing_directive_language": "en",
        "style_block_language": "en",
        "framing_correction_language": "en",
        "motion_verbs_language": "en"
      }
    },
    {
      "id": "buddy-species-voice",
      "skill": "buddy-to-avatar",
      "description": "Buddy voice prompt uses user's language",
      "inputs": {
        "en": "bring my penguin buddy Bramble to life, CHAOS:77 WISDOM:27",
        "ja": "ペンギンのバディBrambleに命を吹き込んで、CHAOS:77 WISDOM:27",
        "es": "dale vida a mi buddy pingüino Bramble, CHAOS:77 WISDOM:27",
        "ko": "펭귄 버디 Bramble을 살려줘, CHAOS:77 WISDOM:27"
      },
      "expected": {
        "voice_language_filter": "{input_language_code}",
        "intro_script_language": "{input_language}",
        "appearance_prompt_language": "en",
        "user_facing_language": "{input_language}"
      }
    },
    {
      "id": "error-messages",
      "skill": "heygen-avatar-designer",
      "description": "Error messages in user's language",
      "inputs": {
        "en": "create an avatar (but API key is missing)",
        "ja": "アバターを作って（APIキーがない場合）",
        "es": "crea un avatar (pero falta la clave API)",
        "ko": "아바터를 만들어줘 (API 키가 없는 경우)"
      },
      "expected": {
        "error_message_language": "{input_language}",
        "technical_details_language": "en_acceptable"
      }
    }
  ]
}
```

- [ ] **Step 3: Write rubric.md**

Create `evals/i18n/rubric.md`:

```markdown
# i18n Eval Rubric

## Scoring Dimensions

### 1. Routing Accuracy (binary: pass/fail)
Did the correct skill/mode fire regardless of input language?
- PASS: Correct mode detected
- FAIL: Wrong mode or no mode detected

### 2. Language Consistency (binary: pass/fail)
Are user-facing outputs in the user's language?
- PASS: Discovery questions, status messages, confirmations all in user's language
- FAIL: Any user-facing message in English when user is non-English

### 3. Prompt Quality (1-5 scale)
Is the API prompt coherent and well-structured?
- 5: Indistinguishable from English baseline quality
- 4: Minor awkwardness but fully functional
- 3: Noticeable quality drop but video would be acceptable
- 2: Significant issues — garbled mixed-language or lost context
- 1: Broken — prompt would produce a bad or failed video

### 4. Voice Match (binary: pass/fail)
Did the skill select/filter voices in the correct language?
- PASS: Voice API call includes correct language filter + locale
- FAIL: Defaults to English or no language filter

### 5. Technical Directive Integrity (binary: pass/fail)
Are English-only directives preserved in English?
- PASS: Motion verbs, style blocks, frame check corrections, script framing directive all in English
- FAIL: Any technical directive translated (would break Video Agent)

### 6. Feature Parity (checklist)
Do all pipeline stages execute?
- [ ] Mode Detection works
- [ ] Discovery gathers all 10 fields
- [ ] Script generation completes
- [ ] Prompt Craft produces structured output
- [ ] Frame Check runs (if avatar_id set)
- [ ] Style block appended
- [ ] Script framing directive present

## Composite Score

Per scenario: (Routing + Language Consistency + Voice Match + Technical Directive Integrity) / 4 binary checks + Prompt Quality (1-5) + Feature Parity (0-7 checklist items)

**Overall per language:** Average across all scenarios.

**Regression threshold:** English baseline must not drop. Non-English must score within 80% of English baseline on Prompt Quality.

## How to Run

1. For each scenario in scenarios.json, invoke the skill in dry-run mode with the input for each language
2. Capture the skill's output: mode detection result, discovery questions, script, prompt, voice API calls
3. Score each dimension per the rubric above
4. Record in baselines/ as `{date}-{language}.json`
```

- [ ] **Step 4: Write README.md**

Create `evals/i18n/README.md`:

```markdown
# i18n Evaluation Suite

Tests that heygen-stack skills work correctly for non-English users.

## Quick Start

Run all scenarios in dry-run mode for each language and score against the rubric.

```
# For each scenario, invoke the skill with dry-run flag:
# "dry run: [scenario input for language X]"
# Score the output against evals/i18n/rubric.md
# Save results to evals/i18n/baselines/
```

## Structure

- `scenarios.json` — Test inputs per language per skill
- `rubric.md` — Scoring criteria and thresholds
- `baselines/` — Saved score snapshots for regression comparison
- `README.md` — This file

## Languages Tested

| Code | Language | Script Type |
|------|----------|-------------|
| en | English | Latin (baseline) |
| ja | Japanese | CJK |
| es | Spanish | Latin (non-English) |
| ko | Korean | Hangul |

## When to Run

- Before and after any i18n changes
- Before any PR that modifies skill routing, Discovery, voice selection, or prompt construction
```

- [ ] **Step 5: Commit eval suite**

```bash
git add evals/
git commit -m "feat: add i18n eval suite with scenarios, rubric, and baseline structure"
```

---

### Task 2: Add Language Detection to Root Skill

**Files:**
- Modify: `SKILL.md` (root router)

Add a Language Detection step that runs before Mode Detection. This detects the user's language from their input and stores it as `user_language` for downstream use.

- [ ] **Step 1: Add Language Detection section after UX Rules in SKILL.md**

Insert after the UX Rules section (after line ~67) and before Mode Detection:

```markdown
## Language Awareness

**Detect the user's language from their first message.** Store as `user_language` (e.g., `en`, `ja`, `es`, `ko`, `zh`, `fr`, `de`, `pt`). This happens automatically from the input — no extra question needed.

**Rules:**
1. **Communicate with the user in their language.** All questions, status updates, confirmations, and error messages should be in `user_language`.
2. **Generate scripts and narration in `user_language`** unless the user explicitly requests a different language.
3. **Technical directives stay in English.** Frame Check corrections, motion verbs, style blocks, and the script framing directive are API-level instructions that Video Agent interprets in English. Never translate these.
4. **Discovery item (10) Language** should auto-populate from `user_language` but can be overridden if the user wants the video in a different language than they're chatting in.
5. **Voice selection must match the video language.** Filter voices by `language` parameter and set `voice_settings.locale` on API calls.
```

- [ ] **Step 2: Make Mode Detection language-agnostic**

Replace the Mode Detection table intro to add:

```markdown
**Language-agnostic routing:** The signals below describe user *intent*, not literal keywords. Match intent regardless of input language. A user saying "ビデオを作って" (Japanese for "make a video") is the same signal as "make a video about X".
```

- [ ] **Step 3: Update Discovery to use detected language**

In the Discovery section, update item (10):

```markdown
**Gather:** (1) Purpose, (2) Audience, (3) Duration, (4) Tone, (5) Distribution (landscape/portrait), (6) Assets, (7) Key message, (8) Visual style, (9) Avatar, (10) Language (auto-detected from user input; confirm if video language should differ from chat language).
```

- [ ] **Step 4: Add language note to Pipeline section**

Add after the Pipeline intro:

```markdown
- **Language:** Script and narration in the video language (from Discovery item 10). Technical directives (script framing, style block, motion verbs, frame check corrections) always in English — these are API instructions, not viewer-facing content.
```

- [ ] **Step 5: Convert First Look quoted messages to semantic descriptions**

Replace the quoted English messages with semantic descriptions. Change:

```markdown
> "Before we make your first video, let's set up your avatar so you have a consistent look across all your videos. This takes about a minute."
```

To:

```markdown
Tell the user you'll set up their avatar first for a consistent look across videos, and that it takes about a minute. Communicate in `user_language`.
```

- [ ] **Step 6: Commit**

```bash
git add SKILL.md
git commit -m "feat: add language detection and i18n awareness to root skill"
```

---

### Task 3: i18n for heygen-video-producer

**Files:**
- Modify: `heygen-video-producer/SKILL.md`

- [ ] **Step 1: Add language-agnostic note to Mode Detection**

After the Mode Detection table, add:

```markdown
**Language-agnostic routing:** These signals describe user *intent*, not literal keywords. Match intent regardless of input language.
```

- [ ] **Step 2: Update Discovery to reference user_language**

After item (10) Language in Discovery, add:

```markdown
**(10) Language** is auto-detected from the user's input. Confirm with the user if the video should be in a different language than they're chatting in. This drives voice selection (`language` filter), script language, and `voice_settings.locale`.
```

- [ ] **Step 3: Add language note to Script section**

After "### Structure by Type", add:

```markdown
**Script language:** Write the script in the video language (from Discovery item 10). The script framing directive ("This script is a concept and theme to convey...") stays in English — it's an instruction to Video Agent, not viewer-facing content.
```

- [ ] **Step 4: Add language note to Prompt Craft section**

In Construction Rules, add after rule 7:

```markdown
8. **Language separation.** Script content and narration in the video language. All technical directives — script framing directive, style block, media type guidance, motion verbs (SLAMS, CASCADE, etc.), and frame check corrections — stay in English. Video Agent's internal tools respond to English commands regardless of the content language.
```

- [ ] **Step 5: Add non-English example to official-prompt-guide reference**

This is informational — the skill should note:

After "Default to Full Producer" add:

```markdown
**Non-English videos:** The same pipeline applies. Scripts are written in the video language. Example: a Japanese compliance training video would have the script in Japanese, but the style block, motion verbs, and frame check corrections remain in English.
```

- [ ] **Step 6: Commit**

```bash
git add heygen-video-producer/SKILL.md
git commit -m "feat: add i18n language separation rules to video producer"
```

---

### Task 4: i18n for heygen-avatar-designer

**Files:**
- Modify: `heygen-avatar-designer/SKILL.md`

- [ ] **Step 1: Convert all hardcoded quoted messages to semantic descriptions**

Replace quoted English strings throughout with semantic descriptions. Key changes:

**Phase 0 — existing avatars prompt (line ~37-48):**
Change the quoted messages to semantic descriptions:
```markdown
Present the existing avatars as numbered options and ask which to use or whether to create new. If no existing avatars found, explain they have none and offer the Claude Buddy shortcut or manual creation. Communicate in `user_language`.
```

**Phase 1 — identity extraction (line ~149):**
Change `"What do you look like? Age, hair, general vibe?"` to:
```markdown
Ask about their appearance naturally (age, hair, general vibe). Ask about their voice (calm, energetic, accent). Communicate in `user_language`.
```

**Reference Photo Nudge (line ~137):**
Change quoted prompt to:
```markdown
Ask if they have a reference photo, explaining that photos give better face consistency than text descriptions. Communicate in `user_language`.
```

**Phase 3 — Voice (line ~223-225):**
Change `"Want me to find a voice based on your description, or browse the catalog yourself?"` to:
```markdown
Ask whether they want voice design (describe what they want) or catalog browsing. Communicate in `user_language`.
```

**Phase 4 — save confirmation (line ~283):**
Change quoted confirmation to:
```markdown
Confirm the avatar is saved and that other skills will pick it up automatically. Communicate in `user_language`.
```

- [ ] **Step 2: Add language filter to voice API calls**

In Phase 3, update both Path A and Path B:

**Path A — Voice Design:**
After the `POST /v3/voices` call, add:
```markdown
**Language matching:** The voice design prompt should specify the target language. Example for Japanese: `"A calm, warm female voice. Professional but approachable. Japanese speaker."` This ensures semantic search returns voices in the correct language.
```

**Path B — Voice Browse:**
Update the filter instruction:
```markdown
2. Filter by gender, language (`user_language`), and personality match
```

Update the example curl:
```bash
curl -s "https://api.heygen.com/v3/voices?type=public&engine=starfish&language={user_language}&gender=female&limit=20" \
  -H "X-Api-Key: $HEYGEN_API_KEY"
```

- [ ] **Step 3: Fix Phase 5 test video prompt**

Change:
```json
"prompt": "Hi, I'm <name>. Nice to meet you!"
```

To:
```markdown
Generate a short greeting in the video language (from Discovery or `user_language`). Example: for English "Hi, I'm <name>. Nice to meet you!", for Japanese "<name>です。はじめまして！", for Spanish "Hola, soy <name>. ¡Mucho gusto!". The greeting should feel natural in the target language.
```

- [ ] **Step 4: Add voice_settings.locale to Avatar Video section**

In the AVATAR file convention, add a new field:
```markdown
- Voice Locale: <BCP-47 locale, e.g. en-US, ja-JP, es-ES, ko-KR>
```

- [ ] **Step 5: Commit**

```bash
git add heygen-avatar-designer/SKILL.md
git commit -m "feat: add i18n support to avatar designer — language-aware voice, messages, test prompt"
```

---

### Task 5: i18n for buddy-to-avatar

**Files:**
- Modify: `buddy-to-avatar/SKILL.md`
- Modify: `references/buddy-species-map.md`

- [ ] **Step 1: Update buddy-to-avatar voice mapping**

In `buddy-to-avatar/SKILL.md`, Phase 2 Voice Mapping section, add:

```markdown
**Voice language:** The voice design prompt must specify the user's language. Replace "English" in the construction pattern with the detected `user_language`. Example for Korean: `"Fast-talking, unpredictable cadence. Gender neutral voice, Korean."` This ensures voice search returns voices in the correct language.
```

- [ ] **Step 2: Update buddy-species-map voice construction**

In `references/buddy-species-map.md`, update the Construction Pattern:

Change:
```
"[Primary stat descriptor], [secondary stat descriptor]. [Species] personality.
[Gender] voice, English.
Think: [one-line character analogy]."
```

To:
```
"[Primary stat descriptor], [secondary stat descriptor]. [Species] personality.
[Gender] voice, {video_language}.
Think: [one-line character analogy]."
```

Update the example too:
```
"Fast-talking, unpredictable cadence, wild energy shifts, excitable.
Slightly thoughtful undertone but overwhelmed by chaos.
Gender neutral voice, {video_language} (e.g., English, Japanese, Korean).
Think: an overexcited cartoon sidekick who talks before thinking."
```

- [ ] **Step 3: Update script tone and sign-offs**

In `references/buddy-species-map.md`, Stat -> Video Script Tone section, change sign-off descriptions to semantic:

```markdown
| Dominant Stat | Script Tone | Sign-off Style |
|---|---|---|
| DEBUGGING | Analytical, breaks down own stats like code review | A nerdy self-deprecating quip about creating bugs (in video language) |
| PATIENCE | Warm, takes time with each stat, reassuring | A gentle reassurance that they'll be there (in video language) |
| CHAOS | Manic, rapid-fire, jumps between topics | An excited promise of wild adventures (in video language) |
| WISDOM | Reflective, frames stats as lessons learned | A wry observation about listening to code (in video language) |
| SNARK | Deadpan, self-deprecating, roasts own low stats | A dismissive "you're welcome" (in video language) |
```

- [ ] **Step 4: Add language note to Phase 4 (intro video)**

In `buddy-to-avatar/SKILL.md`, Phase 4 section, add:

```markdown
**Script language:** Generate the intro video script in `user_language`. Appearance prompts to the avatar creation API stay in English (image generation works best with English prompts). But the video narration script — including the hook, stat brag, speed-round, personality quote, and sign-off — should all be in the video language.
```

- [ ] **Step 5: Convert user-facing messages to semantic descriptions**

Change the skill announcement from:
```markdown
> 🐧 **Using: buddy-to-avatar** — bringing your Claude Code Buddy to life
```

To:
```markdown
Announce the skill activation, mentioning you're bringing their Claude Code Buddy to life. Communicate in `user_language`.
```

Change the Phase 3 confirmation prompt from quoted text to:
```markdown
Present the buddy's details (name, appearance prompt, style, voice concept) and ask for approval. Communicate in `user_language`.
```

- [ ] **Step 6: Commit**

```bash
git add buddy-to-avatar/SKILL.md references/buddy-species-map.md
git commit -m "feat: add i18n support to buddy-to-avatar — language-aware voice, scripts, messages"
```

---

### Task 6: i18n for reference files

**Files:**
- Modify: `references/motion-vocabulary.md`
- Modify: `references/prompt-styles.md`
- Modify: `references/reviewer-prompt.md`
- Modify: `references/api-reference.md`

- [ ] **Step 1: Add English-only note to motion-vocabulary.md**

Add at the top, after the intro paragraph:

```markdown
> **Language note:** Motion verbs MUST remain in English regardless of the video's content language. Video Agent's rendering engine responds to these specific English verbs. Do not translate "SLAMS", "CASCADE", "COUNTS UP", etc. They are API-level commands, not viewer-facing text.
```

- [ ] **Step 2: Add English-only note to prompt-styles.md**

Add after "How to use" section:

```markdown
> **Language note:** Style blocks stay in English regardless of the video's content language. They are technical directives to Video Agent's rendering engine, not viewer-facing text. The video's script and narration should be in the video language, but the STYLE block at the end is always English.
```

- [ ] **Step 3: Update reviewer-prompt.md for language awareness**

Add a new review criterion after criterion 10:

```markdown
### 11. Language Consistency
- If the video language is not English, is the script/narration in the correct language?
- Are technical directives (style block, motion verbs, frame check) still in English?
- Is there a clean separation between content language and directive language?
- FAIL if the script is in English but the user requested a non-English video.
- FAIL if technical directives were translated out of English.
```

- [ ] **Step 4: Add locale note to api-reference.md**

In the Voice Design section, after the example curl, add:

```markdown
**Language matching:** Include the target language in the voice design prompt (e.g., "Japanese speaker", "Korean native"). For voice browsing, use the `language` query parameter:
```bash
curl -s "https://api.heygen.com/v3/voices?type=public&language=ja&limit=20" \
  -H "X-Api-Key: $HEYGEN_API_KEY"
```

In the Avatar Video section, update the `voice_settings` example:
```markdown
| `voice_settings` | object | `{speed: 1.0, pitch: 0, locale: "en-US"}` — set locale to match video language (e.g., `ja-JP`, `es-ES`, `ko-KR`) |
```

- [ ] **Step 5: Commit**

```bash
git add references/motion-vocabulary.md references/prompt-styles.md references/reviewer-prompt.md references/api-reference.md
git commit -m "feat: add i18n language notes to reference files"
```

---

### Task 7: Add non-English example prompt

**Files:**
- Modify: `references/prompt-craft.md`

- [ ] **Step 1: Add a Japanese example prompt**

After the "Brand Story (Animated)" example in the Example Prompts section, add:

```markdown
### Product Introduction (Japanese)
```
日本のナレッジワーカー向けにHeyGenを紹介する1分間のビデオを作成してください。
トーキングアバターモデルの特徴を説明し、具体的な活用例を3つ含めてください。
ナレーターは選択されたプレゼンターが説明します。

CRITICAL ON-SCREEN TEXT (display literally):
- "HeyGen アバター V"
- "3分で動画作成"
- "API連携対応"

STYLE — SWISS PULSE (Müller-Brockmann): Black/white + electric blue #0066FF.
Grid-locked. Helvetica Bold. Animated counters. Diagonal accents.
Grid wipe transitions.
```

Note: The script content is in Japanese but the STYLE block and CRITICAL ON-SCREEN TEXT label remain in English — these are Video Agent directives.
```

- [ ] **Step 2: Add note about multilingual prompts**

Before the Example Prompts section, add:

```markdown
### Non-English Videos

The same prompt structure applies regardless of language. Key rules:
1. **Script/narration:** In the video language
2. **Style block:** Always English (Video Agent directive)
3. **Motion verbs:** Always English (SLAMS, CASCADE, etc.)
4. **Critical on-screen text:** In whatever language should appear on screen
5. **Scene labels:** English (Scene 1, Scene 2) — these are structural, not rendered
```

- [ ] **Step 3: Commit**

```bash
git add references/prompt-craft.md
git commit -m "feat: add non-English example prompt and multilingual prompt guide"
```

---

### Task 8: Update CHANGELOG and VERSION

**Files:**
- Modify: `CHANGELOG.md`
- Modify: `VERSION`

- [ ] **Step 1: Bump version**

Update `VERSION` from `1.2.7` to `1.3.0` (minor version bump for new feature).

- [ ] **Step 2: Update CHANGELOG**

Add to top of CHANGELOG.md:

```markdown
## 1.3.0 — i18n Support

### Added
- **Language Detection** — Skills now detect the user's language from input and communicate in that language
- **Language-aware voice selection** — Voice API calls filter by detected language; `voice_settings.locale` set automatically
- **Non-English script generation** — Scripts and narration generated in the video language
- **i18n eval suite** — Test scenarios across English, Japanese, Spanish, Korean with scoring rubric
- **Japanese example prompt** in prompt-craft reference
- **Language consistency criterion** in reviewer prompt

### Changed
- Mode Detection is now explicitly language-agnostic (semantic intent matching, not English keyword matching)
- User-facing messages converted from hardcoded English strings to semantic descriptions the LLM adapts to the user's language
- Buddy voice construction pattern uses `{video_language}` instead of hardcoded "English"
- Buddy sign-off lines converted to semantic descriptions instead of English idioms
- Test video prompt (Phase 5) generates greeting in video language instead of hardcoded English

### Documentation
- Added `> Language note` callouts to motion-vocabulary.md and prompt-styles.md explaining English-only directives
- Updated api-reference.md with `voice_settings.locale` guidance and language filter examples
- All reference files now document the content-language vs directive-language separation
```

- [ ] **Step 3: Update version references in SKILL files**

Search and replace `1.2.7` with `1.3.0` in User-Agent headers across:
- `heygen-avatar-designer/SKILL.md`
- `heygen-video-producer/SKILL.md`
- `SKILL.md` (frontmatter `version:`)

- [ ] **Step 4: Commit**

```bash
git add CHANGELOG.md VERSION SKILL.md heygen-avatar-designer/SKILL.md heygen-video-producer/SKILL.md
git commit -m "chore: bump version to 1.3.0, update changelog for i18n support"
```

---

### Task 9: Self-Review and PR

- [ ] **Step 1: Verify all changes**

```bash
git diff main --stat
git diff main -- SKILL.md | head -200
git diff main -- heygen-video-producer/SKILL.md | head -200
git diff main -- heygen-avatar-designer/SKILL.md | head -200
git diff main -- buddy-to-avatar/SKILL.md | head -100
git diff main -- references/ | head -200
```

- [ ] **Step 2: Check for consistency**

Verify:
- `user_language` is referenced consistently (not `user_lang` or `language` in some places)
- All quoted English messages have been converted to semantic descriptions
- No technical directives were accidentally marked for translation
- Version is `1.3.0` everywhere

- [ ] **Step 3: Push and create PR**

```bash
git push -u origin feat/i18n-support
gh pr create --title "feat: i18n support for non-English users" --body "..."
```
