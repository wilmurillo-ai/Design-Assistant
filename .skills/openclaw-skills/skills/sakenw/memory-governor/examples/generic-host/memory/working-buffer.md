+++
target_class = "working_buffer"
schema_version = "0.1"
updated_at = "2026-03-31T00:00:00Z"
task_ref = ""
buffer_status = "active"
+++

# Working Buffer

This file is the local fallback for `working_buffer`.

## Breadcrumbs

- recovery hints
- temporary next-step context
- short-lived execution breadcrumbs

## Rules

- short-lived by default
- do not promote directly from here to long-term memory without extraction
