+++
target_class = "working_buffer"
schema_version = "0.1"
updated_at = "2026-03-31T00:00:00Z"
task_ref = ""
buffer_status = "active"
+++

# working-buffer.md

Fallback template for the `working_buffer` target class.

Use this when no preferred adapter is available.

## Breadcrumbs

- temporary recovery breadcrumbs
- fragile intermediate conclusions
- notes needed to resume interrupted work

## Rules

- short-lived by default
- do not treat as long-term memory
- promote only after distillation
