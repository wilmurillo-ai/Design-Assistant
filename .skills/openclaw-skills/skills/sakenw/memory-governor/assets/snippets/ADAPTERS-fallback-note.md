## Adapter Note

This workspace treats optional skills as adapters, not as mandatory dependencies.

If a preferred adapter is unavailable:

- keep the target class
- switch to the documented fallback
- do not rewrite the memory model just because one adapter is missing
