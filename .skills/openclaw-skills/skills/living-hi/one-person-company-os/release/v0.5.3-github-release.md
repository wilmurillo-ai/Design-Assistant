# v0.5.3 - English-First Storefront Polish

`v0.5.1` fixed the bilingual runtime.
`v0.5.2` fixed the public metadata summary.

`v0.5.3` improves the storefront itself.

This patch release:

- rewrites the English README sections that previously showed raw Chinese workspace paths without enough English framing
- turns `SKILL.md` into an English-first bilingual storefront so English users can understand the product contract quickly
- reorders ClawHub-facing copy so English readers see an understandable front page without weakening Chinese support

No runtime contract changes are introduced in this patch. The product behavior remains the same; the public presentation is now stronger.
