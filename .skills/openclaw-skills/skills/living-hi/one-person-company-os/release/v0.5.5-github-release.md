# v0.5.5 - Outcome-Driven Storefront Hook

`v0.5.4` made the storefront shorter.

`v0.5.5` makes it more compelling.

This patch release changes the public summary from a descriptive capability list to a clearer promise:

- start from one AI product idea
- turn it into a run-ready solo company

The change is limited to storefront copy only. Runtime behavior is unchanged.
