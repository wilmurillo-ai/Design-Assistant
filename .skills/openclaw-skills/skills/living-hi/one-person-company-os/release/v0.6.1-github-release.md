# v0.6.1: support-surface upgrade and stage-inference fix

This patch release improves real founder execution quality after the `v0.6.0` business-loop rebuild.

Highlights:

- fixed stage inference so cash updates no longer push build-stage companies into operate or grow prematurely
- stopped `update_cash.py` from overwriting the founder's main arena automatically
- upgraded generated `product/01`, `sales/01`, and `ops/01` files from duplicated status views into actionable checklists
- strengthened release validation to explicitly generate and verify deployment and production DOCX artifacts

This version was exercised against a realistic narrow-founder scenario while keeping the package itself vertical-neutral and reusable across different startup directions.
