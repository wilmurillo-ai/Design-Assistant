# v0.5.2 - Bilingual Listing Metadata Polish

`v0.5.1` fixed the real bilingual runtime and document path.

The remaining mismatch was public metadata:

- the ClawHub runtime package was bilingual
- the GitHub docs were bilingual
- but the live listing summary still skewed Chinese-only

`v0.5.2` fixes that last gap by updating the published skill description so both Chinese and English users can understand the package from the listing page itself.

No runtime behavior changes are included in this patch.
