{
  "schemaVersion": 1,
  "name": "searxng-web",
  "description": "web search via local searxng (no brave api key). provides searxng_search(query, count).",
  "tools": [
    {
      "name": "searxng_search",
      "description": "search the web via local searxng and return normalized results.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "query": { "type": "string" },
          "count": { "type": "integer", "default": 5, "minimum": 1, "maximum": 10 }
        },
        "required": ["query"]
      },
      "runner": { "type": "node", "entrypoint": "searxng_search.js" }
    }
  ]
}
