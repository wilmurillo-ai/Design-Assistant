# 安全与合规提示

1. 信用卡申请是正式金融业务，涉及高敏感个人信息与授权。
2. OpenClaw 的 memory 是工作区磁盘文件，不应把旧记录默认当作当前准确资料。
3. 对于身份证号、住址、收入、单位等字段，必须逐项让用户确认。
4. 不建议在 skill 中静默扫描全部记忆内容并直接写入官网表单。
5. 不建议用 `user` 浏览器 profile 处理无关站点，因为 existing-session 模式会继承该浏览器会话的登录态。
6. 最终提交、验证码、短信 OTP、人脸识别、电子签名必须由用户本人完成。
7. 推荐结论和自动化预填都不能替代银行实际审批。
