interface:
  display_name: "Rust Rebuilder"
  short_description: "Incremental Rust rewrite with upstream sync guardrails."
  default_prompt: "Use $rust-rebuilder to rewrite this project to Rust with dependency preflight checks, backend Rust guardrails, staged parity validation, and upstream synchronization."
