/**
 * OpenClaw plugin entry point.
 * OpenClaw's plugin scanner looks for index.ts in the extensions root
 * and executes TypeScript directly via its built-in TS runtime.
 */
export { default } from "./src/openclaw-entry.js";
