interface:
  display_name: "Calendar Hold Sync"
  short_description: "Mirror calendar events into private Busy holds"
  default_prompt: "Use $calendar-hold-sync to reconcile source Google calendars into target Busy holds using gog with safe dry-run controls."
