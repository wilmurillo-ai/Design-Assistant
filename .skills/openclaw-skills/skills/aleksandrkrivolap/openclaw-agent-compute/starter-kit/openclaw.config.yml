# OpenClaw config (draft placeholder)
# TODO: align this structure with OpenClaw's real config schema.

tools:
  - name: compute
    kind: http
    base_url: ${MCP_COMPUTE_URL}
    auth:
      type: bearer
      token: ${MCP_COMPUTE_API_KEY}
