interface:
  display_name: "Wyckoff Trading Agent"
  short_description: "一键安装 CLI → 注册 → 配置数据源 → 威科夫量价分析 → 持仓管理 → 信号推荐，全链路自动编排。"
  default_prompt: "检测本地 wyckoff CLI 环境，缺失则引导安装和配置；就绪后按威科夫系统提示词执行完整能力链路：获取北京时间判定交易时段，多源抓数与回退，融合 CSV/图片/组合输入，输出可审计的加仓/减仓/持有/退出/换仓建议；也可直接执行持仓查看、信号查询、推荐查询等 CLI 操作命令。"
