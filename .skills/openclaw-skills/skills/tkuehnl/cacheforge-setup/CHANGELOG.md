# Changelog

## Unreleased

- Aligned provider presets to `openrouter`, `anthropic`, and `custom` (OpenAI-compatible).
- Added `openclaw-snippet`, `openclaw-apply`, and `openclaw-validate` workflow updates.
- Added invite-only + email verification onboarding notes.

## 0.1.0

- Initial release.
