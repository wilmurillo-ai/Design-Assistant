# Domain Selector

## Graph report intent

Keywords:
- `graph report`, `kpi`, `chart`, `drilldown`, `interaction`, `heatmap`, `waterfall`, `map`

Use:
- `CGraphReport` for controlled dialog
- `CTable.graphReport` for integrated entry from table toolbar

## Detail page intent

Keywords:
- `detail page`, `sections`, `tabs`, `search + ai`, `related table`

Use:
- `CDetailInfoPage`
- `useDetailInfo` for custom orchestration

## Agent settings intent

Keywords:
- `llm settings`, `baseUrl`, `apiKey`, `model`, `prompt template`

Use:
- `CCustomizeAgent`
