# 各识别类型的字段说明（elements 内容）

根据 ocrType 不同，返回的 `elements` 对象包含以下字段：

## QUOTA_INVOICE (定额发票)
- `title`: 发票名称
- `invoiceCode`: 发票代码
- `invoiceNo`: 发票代码
- `amountUpper`: 发票号码
- `amountLower`: 开票日期
