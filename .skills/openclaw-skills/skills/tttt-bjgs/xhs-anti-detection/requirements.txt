# xhs-anti-detection Skill 依赖
# 安装: pip3 install -r requirements.txt

# 图像处理
opencv-python>=4.8.0
Pillow>=10.0.0

# OCR（文字识别）
pytesseract>=0.3.10

# 元数据处理
# 注意: exiftool 需要系统安装（brew/apt），不是 Python 包
# macOS: brew install exiftool
# Ubuntu: sudo apt-get install libimage-exiftool-perl

# 数值计算
numpy>=1.24.0

# JSON 处理（内置）
# 其他（内置）
# pathlib, subprocess, shutil, random, datetime, json 等均为 Python 标准库
