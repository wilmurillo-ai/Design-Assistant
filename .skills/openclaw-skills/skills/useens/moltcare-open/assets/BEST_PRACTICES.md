# BEST_PRACTICES.md - Agent 效率最佳实践

> 🚀 最小 Token 成本，最大工作价值

---

## 任务分层 (L0-L3)

| 层级 | 任务类型 | 执行方式 | Token |
|------|----------|----------|-------|
| **L0** | 数据采集、定时任务 | 纯脚本 | 零 |
| **L1** | 查询展示、格式化 | 纯脚本 | 零 |
| **L2** | 阈值判断、异常检测 | 脚本+条件触发 | 按需 |
| **L3** | 分析、决策、创意 | AI 调用 | 正常 |

**决策流程**:
```
任务输入 → 纯数据? → L0/L1 脚本
         → 可规则化? → L2 条件触发
         → 需推理? → L3 AI
```

---

## 核心原则

**能用脚本，绝不用 AI。**

❌ **禁止**: AI 轮询 API、AI 做数学计算、全量数据给 AI  
✅ **正确**: 脚本预处理 → AI 只看摘要/异常

---

## 优化技巧

### 1. 批处理
❌ "这条正常吗?" ×1000  
✅ "1000 条中脚本筛选出 3 条异常，请分析"

### 2. 缓存策略
| 类型 | 有效期 | 适用 |
|------|--------|------|
| 内存 | 分钟 | 高频查询 |
| 文件 | 小时 | 中频数据 |
| 持久化 | 天 | 历史数据 |

### 3. 增量更新
```javascript
const diff = computeDiff(prev, current);
if (diff.length === 0) return "无变化";
return analyzeChanges(diff); // 只有变化才调用 AI
```

---

## 每日审查

**触发**: 每天凌晨或用户说"检查token优化"

**检查清单**:
- [ ] 执行 >3 次的任务 → 考虑脚本化
- [ ] 高频查询 → 添加缓存
- [ ] 阈值判断用 AI → 改为 L2
- [ ] 数据格式化用 AI → 改为 L0

**报告模板**: `memory/token-audit-YYYY-MM-DD.md`

---

## 评估指标

| 指标 | 目标 |
|------|------|
| 脚本化率 | >80% |
| 缓存命中率 | >60% |
| 脚本层响应 | <1s |

---

*精简版 v3.2 | 原 467 行 → 现 70 行 (压缩 85%)*
