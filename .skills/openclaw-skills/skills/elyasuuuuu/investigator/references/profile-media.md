# Profile media

When a public profile page exposes a profile image or Open Graph image, capture that as a weak supporting signal.

## Good uses
- include the public image URL in structured results
- compare obvious avatar reuse across platforms
- use as supporting evidence, not identity proof

## Rules
- only use publicly exposed image URLs
- do not perform invasive media scraping
- do not treat a shared avatar alone as strong proof
