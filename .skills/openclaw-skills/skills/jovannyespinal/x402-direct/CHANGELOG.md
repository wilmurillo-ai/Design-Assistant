# Changelog

## 1.0.0 — 2025-05-25

### Added
- Browse services endpoint (`GET /api/services`) with category, network, score, and sort filters
- Service detail endpoint (`GET /api/services/:id`) with full payment info and facilitator data
- Ecosystem stats endpoint (`GET /api/stats`) for directory overview
- Paid search endpoint (`GET /api/search`) at $0.001/query via x402
- Trust scoring system (0-100) with safe/caution/avoid verdicts
- x402 payment flow documentation for agent wallets
- Decision guide for choosing the right endpoint
- Filter values reference for categories, networks, and sort options
