[project]
name = "predictfish"
version = "0.1.0"
description = "Predict project success using MiroFish swarm intelligence"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["lib"]
