[project]
name = "notion-agent"
version = "0.1.0"
description = "Notion integration for OpenClaw"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
]

[tool.hatch.build.targets.wheel]
packages = ["lib"]
