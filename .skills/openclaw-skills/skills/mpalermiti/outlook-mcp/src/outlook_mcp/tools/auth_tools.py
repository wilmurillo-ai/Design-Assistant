"""Auth tool helpers.

Auth tools are wired directly in server.py as @mcp.tool() functions.
This module exists as the declared package entry point but delegates
to AuthManager methods via the server wiring.
"""
