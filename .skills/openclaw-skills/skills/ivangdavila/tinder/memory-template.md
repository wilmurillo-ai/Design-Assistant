# Memory Template - Tinder

Create `~/tinder/memory.md` with this structure:

```markdown
# Tinder Memory

## Status
status: ongoing
version: 1.0.0
last: YYYY-MM-DD
integration: pending | complete | paused | never_ask

## Context
- Activation preference:
- Current Tinder goal:
- Success metric:
- Tone defaults:
- Date pace preference:
- Out-of-scope lines:
- Preferred output shape:

## Notes
- Durable profile lessons
- Match patterns worth remembering
- Boundaries and safety defaults

---
*Updated: YYYY-MM-DD*
```

## Status Values

| Value | Meaning | Behavior |
|-------|---------|----------|
| `ongoing` | Context still evolving | Keep learning the user's funnel and style |
| `complete` | Activation and approach are stable | Focus on execution and small refinements |
| `paused` | User wants minimal setup | Help without pushing deeper tracking |
| `never_ask` | User wants no setup prompts | Do not ask integration questions unless requested |

## File Templates

Create `~/tinder/profile-notes.md`:

```markdown
# Profile Notes

## Current Stack
- Goal:
- Strongest photo:
- Weakest photo:
- Bio signal:
- Prompt or interest signal:
- Biggest mismatch:
- Next test:
```

Create `~/tinder/matches.md`:

```markdown
# Matches

## YYYY-MM-DD - Match
- Stage:
- Why it matched:
- Current momentum:
- Open loop:
- Recommended next move:
```

Create `~/tinder/experiments.md`:

```markdown
# Experiments

## Test
- Changed:
- Hypothesis:
- Result:
- Keep or revert:
```

Create `~/tinder/dates.md`:

```markdown
# Dates

## Pending Plan
- Match:
- Proposed plan:
- Timing window:
- Acceptance signal:
- Risk or hesitation:
```

Create `~/tinder/goals.md`:

```markdown
# Goals

## Active Goal
- Objective:
- Current bottleneck:
- Weekly target:
- What to review next:
```

## Key Principles

- Store only durable lessons that improve profile choices, message quality, or date conversion.
- Keep one explicit goal active so coaching stays tied to outcomes.
- Prefer summaries and stages over raw chat dumps.
- Keep third-party information minimal and need-to-know.
- Update `last` whenever activation rules, tone defaults, or dating boundaries change.
