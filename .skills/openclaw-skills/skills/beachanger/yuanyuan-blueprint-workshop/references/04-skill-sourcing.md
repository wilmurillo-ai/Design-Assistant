# Skill Sourcing Rules

Default order:
reuse local → search ClawHub → vet/scan → install if suitable → self-build if necessary.

Do not blindly install many skills.
The point is to generate a capability completion plan, not to bloat the agent.
