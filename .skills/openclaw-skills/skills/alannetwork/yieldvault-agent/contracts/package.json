{
  "name": "yield-vault-contracts",
  "version": "1.0.0",
  "description": "Smart contract stub for yield farming agent on BNB testnet",
  "main": "deploy.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:testnet": "hardhat run scripts/deploy.js --network bnbTestnet",
    "verify": "hardhat verify --network bnbTestnet",
    "check-balance": "hardhat run scripts/check-balance.js --network bnbTestnet",
    "test": "hardhat test",
    "clean": "hardhat clean",
    "flatten": "hardhat flatten contracts/YieldVault.sol",
    "generate-abi": "node scripts/generate-abi.js"
  },
  "keywords": [
    "yield-farming",
    "smart-contract",
    "bnb-testnet",
    "solidity",
    "hardhat"
  ],
  "author": "Yield Farming Agent",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "@openzeppelin/hardhat-upgrades": "^3.0.0",
    "hardhat": "^2.21.0",
    "dotenv": "^16.0.3",
    "viem": "^2.0.0"
  },
  "dependencies": {
    "web3": "^4.0.0",
    "ethers": "^6.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
