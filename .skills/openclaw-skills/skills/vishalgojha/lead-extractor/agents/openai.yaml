interface:
  display_name: "Lead Extractor RE-India"
  short_description: "Extract leads and classify inventory vs buyer requirement posts."
  default_prompt: "Extract structured real-estate leads from parsed messages, include record_type, and avoid any writes or outbound actions."
