interface:
  display_name: "Lead Storage RE-India"
  short_description: "Store validated normalized leads with supervisor confirmation."
  default_prompt: "Insert validated leads, including record type and priority metadata, only when a supervisor confirmation token is present."
