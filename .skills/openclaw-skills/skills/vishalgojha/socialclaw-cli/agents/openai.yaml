interface:
  display_name: "Social Flow"
  short_description: "Agentic control plane for safe Meta workflows via social CLI."
  default_prompt: "Use social-flow to plan multi-step Meta operations, route through Gateway or Studio where needed, and gate risky actions with explicit confirmations."
