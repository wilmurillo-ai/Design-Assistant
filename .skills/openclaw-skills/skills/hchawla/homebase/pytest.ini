[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
# Treat all warnings as errors (opt-in per module)
filterwarnings =
    ignore::DeprecationWarning
