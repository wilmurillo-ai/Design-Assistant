{
  "id": "nima-recall-live",
  "name": "NIMA Live Recall",
  "description": "Queries experiential memory on every message via before_agent_start",
  "version": "2.0.9",
  "kind": "hook",
  "events": [
    "before_agent_start",
    "before_compaction"
  ],
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "description": "Enable live recall system"
      },
      "identity_name": {
        "type": "string",
        "description": "Agent identity name (default: agent)"
      },
      "skipSubagents": {
        "type": "boolean",
        "description": "Skip subagent sessions (default: true)"
      },
      "allowSubagentRecall": {
        "type": "boolean",
        "description": "Allow read-only NIMA recall for subagents (skipSubagents still blocks writes). Default: false"
      }
    }
  }
}