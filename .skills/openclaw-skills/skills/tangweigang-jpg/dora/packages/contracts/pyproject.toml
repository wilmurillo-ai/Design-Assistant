[project]
name = "doramagic-contracts"
version = "0.1.0"
description = "Doramagic shared artifact schemas and data models"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["doramagic_contracts"]
