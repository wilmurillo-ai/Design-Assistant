# 知识积木 v2 — 电商与购物
# 用途：跨平台比价监控、库存追踪、订单状态查询

# === 信封层（给系统看）===
id: ecommerce-v1
name: 电商与购物
version: 1.0.0
category:
  - 电商
  - 价格监控
  - 购物助手
tags:
  - 比价
  - 库存追踪
  - 订单查询
  - Amazon
  - Keepa
  - 价格历史
  - ASIN
  - EAN
  - UPC
  - 反爬
capability_type: poll
data_source: rss

inputs:
  product_id:
    type: string
    required: true
    description: "商品唯一标识，支持 ASIN（亚马逊，10位字母数字）、EAN（13位数字）、UPC（12位数字）"
    validation: "len(value) in (10, 12, 13) and value.replace('-', '').isalnum()"
  id_type:
    type: enum
    required: true
    description: "商品 ID 类型，决定调用哪个 API"
    enum_values:
      - ASIN
      - EAN
      - UPC
      - URL
  target_price:
    type: float
    required: false
    default: 0.0
    description: "目标价格阈值（本地货币），降到此价格以下时触发通知；0 表示始终返回价格"
    validation: "value >= 0"
  currency:
    type: enum
    required: false
    default: "USD"
    description: "价格货币标准化目标币种，所有来源价格统一转换为此币种"
    enum_values:
      - USD
      - EUR
      - GBP
      - JPY
      - CNY
      - CAD
      - AUD
  include_third_party:
    type: boolean
    required: false
    default: false
    description: "是否包含第三方卖家价格（通常低于官方价但需确认卖家可信度）"
  poll_interval_min:
    type: integer
    required: false
    default: 60
    description: "价格轮询间隔（分钟），频率过高易触发平台反爬机制"
    validation: "15 <= value <= 1440"

outputs:
  current_price:
    type: float
    description: "当前最低价格，已按 currency 标准化"
  price_drop:
    type: boolean
    description: "是否触发目标价格阈值"
  in_stock:
    type: boolean
    description: "商品是否有库存"
  price_history:
    type: "list[dict]"
    description: "历史价格记录列表，每项含 date/price/source 字段（Keepa 付费可获取完整历史）"
  message:
    type: string
    description: "格式化的价格通知消息，可直接传给 notify 积木"

requires: []
conflicts_with: []
compatible_with:
  - telegram-notifier-v1

# === 内容层（给 LLM 看）===
core_capability: >
  通过 Amazon Product Advertising API 5.0（条件免费）或价格追踪 RSS feed 监控商品价格和库存；
  使用 Keepa API（$19/月）获取完整价格历史和即时价格变化通知；
  所有价格统一货币标准化，内置反爬退避策略。

constraints:
  - "Amazon Product Advertising API 5.0（PA-API）使用条件：
     必须是 Amazon Associates（联盟营销）成员，且账号在过去 180 天有合格销售记录；
     无销售记录时 API 密钥被暂停；调用端点：https://webservices.amazon.com/paapi5/getitems；
     每秒最多 1 次请求，每天 8640 次；请求必须用 AWS Signature V4 签名"
  - "免费替代方案：价格追踪网站 RSS feed（camelcamelcamel.com/product/<ASIN>.rss）
     无需认证，每小时更新一次价格；用 feedparser 解析，价格在 description 字段中，
     需用正则 r'Current price: [$]([0-9.]+)' 提取"
  - "Keepa API（付费，$19/月）端点：https://api.keepa.com/product?key=<API_KEY>&asin=<ASIN>&domain=1；
     返回完整价格历史数组，时间戳为 Keepa 时间（Unix 时间戳 / 60 - 21564000），
     转换公式：datetime.fromtimestamp((keepa_time + 21564000) * 60)"
  - "价格货币标准化：使用 frankfurter.app API（免费，无需认证）获取实时汇率；
     GET https://api.frankfurter.app/latest?from={source_currency}&to={target_currency}；
     汇率缓存 1 小时有效，避免频繁调用；非工作日汇率使用最近一个工作日数据"
  - "商品 ID 标准化：ASIN 为 10 位字母数字（大写），如 B08N5WRWNW；
     EAN 为 13 位纯数字，UPC 为 12 位纯数字；
     URL 类型需从亚马逊 URL 中提取 ASIN（正则 r'/dp/([A-Z0-9]{10})'）；
     EAN/UPC 转 ASIN 需通过 PA-API 的 SearchItems 接口，不可假设映射关系固定"
  - "PA-API 5.0 已官方宣布将于 2026 年 4 月 30 日废弃，必须迁移到 Amazon Creators API；
     2025 年 11 月起 Creators API 新增要求：过去 30 天内必须有 10 次合格销售，
     否则返回 AssociateNotEligible (403) 错误；
     迁移时 AWS Access Key/Secret Key 不可复用，需在 Associates Central 重新生成 Creators API 凭证；
     请求体字段名大小写发生变化（如 ItemIds → itemIds），不更新将导致 400 解析错误"
  - "反爬军备竞赛（2025 持续升级）：Cloudflare 于 2025 年 7 月起默认屏蔽 AI 爬虫，
     并推出按爬取次数计费的付费爬取模式（pay-per-crawl）；
     机器学习反爬系统一次规则调整可能静默禁用所有现有爬虫，且没有任何错误提示；
     禁止直接爬取 amazon.com 产品页面；所有价格数据必须通过官方 API 或第三方服务获取；
     第三方价格追踪网站（camelcamelcamel/Keepa/PriceCharting）本身承担爬取风险，通过其 API/RSS 获取是合规的"
  - "轮询频率与告警疲劳的双重陷阱：频率过高触发反爬屏蔽；频率过低错过价格变化；
     同时告警阈值设置不当导致告警疲劳（每次 0.01 元波动均通知）；
     建议对关键 SKU 采用分级频率策略（核心商品每 30 分钟，长尾每 4 小时）；
     价格变化通知设置最低变动幅度阈值（建议 ≥ 5% 或 ≥ 目标价格的 3%）"
  - "库存状态来源：PA-API 返回的 Offers.Listings[0].Availability.Type 字段；
     'NOW' 表示有货，'TOMORROW'/'DAYS_1_2' 表示备货中，'WEEKS_1_2' 及以上视为缺货；
     第三方卖家的 include_third_party=true 时合并 Offers 列表取最低价"
  - "SP-API 2026 年 4 月 30 日起对 GET 请求收费：
     Amazon SP-API 从 2026-04-30 起按 GET 调用次数收费，POST/PUT/PATCH 免费；
     频繁轮询价格/库存的系统将产生直接 API 成本；
     必须改用 Reports API 批量拉取数据替代逐个 GET，一次报告请求可替代数千次 API 调用"
  - "Amazon 买家选项框（Buy Box）数据被 Amazon 从 Vendor Central 和 SP-API 移除：
     品牌商无法通过官方 API 获取 Buy Box 赢得率数据，只能依赖第三方爬虫服务；
     监控第三方卖家违反 MAP 定价时，Amazon 自身也是最大的 MAP 违规方；
     价格监控系统必须包含对 Amazon 官方定价的监控，不只是第三方卖家"
  - "SP-API 各端点限速不一致且会不提前通知地变更：
     Orders API 仅允许 1 请求/分钟（突发 1 次），GetListingOffersBatch 仅 0.1 请求/秒；
     管理多个卖家账号时每个账号有独立的速率桶，需要 50 个独立的 token bucket；
     Amazon 会在不通知的情况下修改限速参数，导致原本正常的流水线突然大量 429"
  - "EAN/UPC 转 ASIN 映射不唯一：同一 EAN 条码可能对应多个 ASIN（不同卖家创建了重复 listing）；
     PA-API SearchItems 返回的第一个结果不一定是目标商品；
     必须用商品名称/品牌做二次匹配过滤，不可直接使用 SearchItems 首条结果"
  - "价格更新延迟最长数小时：
     PA-API 返回的价格反映的是亚马逊仓储系统的状态，而非实时交易价格；
     促销活动开始/结束时刻与 API 反映时刻存在最长数小时延迟；
     高频价格变化场景（闪购、秒杀）必须结合 Keepa 实时通知而非纯轮询"
  - "camelcamelcamel RSS 价格字段因站点不同格式各异：
     美国站用 $，英国站用 £，欧洲站用 €，日本站用 ¥，且部分站点价格含逗号千位分隔符；
     正则仅匹配 $ 符号会导致除美国站外所有站点价格解析返回 None 且无错误"
  - "第三方卖家价格低但包含隐藏费用：
     include_third_party=true 时取到的最低价往往是第三方卖家不含运费的价格；
     实际到手价需要加上 shipping_price；PA-API 在 Offers.Listings[0].DeliveryInfo 字段提供配送信息，
     必须合并 price + shipping_price 才能做公平比价"
  - "Keepa 时间戳转换错误导致价格历史图表乱序：
     Keepa 使用私有时间单位（minutes since 2011-01-08 00:00:00 UTC）；
     转换公式：datetime.utcfromtimestamp((keepa_time + 21564000) * 60)；
     用标准 Unix 时间戳直接解读 Keepa 时间会得到 1970 年代的错误日期，
     不可直接将 keepa_time 传给 datetime.fromtimestamp()"
  - "价格监控系统漏报库存竞争（inventory race condition）：
     库存状态为 'NOW' 但下单瞬间因并发抢购而实际缺货；
     用户收到'有货'通知后立即下单却得到'缺货'，严重影响体验；
     通知消息必须明确标注'库存状态仅供参考，实际以下单时为准'"
  - "反爬系统 2025 年升级后静默屏蔽整个 IP 段：
     Cloudflare 2025 年 7 月起对 AI/自动化爬虫默认屏蔽，
     机器学习反爬规则调整后可在不返回任何错误的情况下静默屏蔽爬虫请求；
     必须严格通过 PA-API/RSS/Keepa 等官方渠道获取数据，禁止直接爬取 amazon.com"
  - "PA-API 每秒 1 次请求限制在批量查询场景下极其不足：
     每天 8640 次调用上限仅能监控约 360 个 SKU（每 SKU 每 4 小时查询一次）；
     超出限额后返回 TooManyRequests 错误，且错误率累计到一定程度可导致密钥暂停；
     批量查询场景必须使用 GetItems 接口一次性查询最多 10 个 ASIN，而非逐个查询"
  - "汇率缓存在周末/节假日过期后使用过时数据导致价格换算偏差：
     Frankfurter.app 等免费汇率 API 在非工作日不更新，返回上一个工作日数据；
     缓存有效期设置为 1 小时在工作日合理，但在周末累计 48+ 小时不更新；
     价格通知中必须明确显示汇率更新时间，高波动货币对（USD/JPY/GBP）的偏差尤其明显"
  - "买家选项框（Buy Box）不是最低价：
     Amazon Buy Box 获得者基于价格、配送速度、卖家评分综合评定；
     Buy Box 价格可能高于第三方卖家最低价；
     监控'最低价'和'Buy Box 价格'是两个独立需求，不可混用"
  - "camelcamelcamel RSS 每小时才更新，价格突变期间最大 60 分钟数据空白：
     秒杀/限时折扣活动的开始和结束价格变化可能在 RSS 更新窗口内完全错过；
     RSS 数据源只适合'通知目标价格已到达'的低频场景，不适合精确捕捉价格变动时刻；
     需要精确时间戳的场景（如计算促销持续时长）必须使用 Keepa 历史数据 API"

  - |
      【API 资源库】可用的公开 API：
         免费：
         1. Best Buy (https://bestbuyapis.github.io/api-documentation/#overview) — 产品, Buying 选项, Categories, Recommendations, Stores 和 Commerce，认证：`apiKey`
         2. Digi-Key (https://www.digikey.com/en/resources/api-solutions) — 获取 价格 和 inventory 的 electronic components 作为 well 作为 地点 orders，认证：`OAuth`
         3. Dummy Products (https://dummyproducts-api.herokuapp.com/) — API 到 fetch dummy e-commerce 产品 JSON 数据 带有 占位图 图片，认证：`apiKey`
         4. eBay (https://developer.ebay.com/) — Sell 和 Buy 关于 eBay，认证：`OAuth`
         5. Etsy (https://www.etsy.com/developers/documentation/getting_started/api_basics) — 管理 shop 和 交互 带有 listings，认证：`OAuth`
         注意：以上 API 信息来自 public-apis 开源目录，使用前请确认最新状态
common_failures:
  - severity: HIGH
    pattern: "PA-API 返回价格为 0 或缺失，原因是商品在当前市场（domain）不可购买，
              调用方未检查直接覆盖历史价格，导致价格记录污染"
    mitigation: "price = 0 或 None 时不更新历史记录，记录日志 'price unavailable for {ASIN}'；
                 连续 3 次价格为 0 时标记商品状态为 unavailable"
  - severity: HIGH
    pattern: "价格过期：RSS feed 每小时才更新，实际促销开始/结束时刻与通知延迟最长 60 分钟，
              用户收到通知时促销已结束"
    mitigation: "在通知消息中明确标注数据来源和最后更新时间；
                 高频监控场景（< 60 分钟）必须升级到 Keepa 或 PA-API 实时数据"
  - severity: HIGH
    pattern: "PA-API Associates 账号因 180 天无销售记录被暂停，API 密钥返回 InvalidPartnerTag 错误，
              所有价格查询静默失败"
    mitigation: "捕获 InvalidPartnerTag/InvalidSignature 错误，立即切换到 RSS feed 降级模式，
                 并在输出 message 中标注'使用降级数据源'"
  - severity: MEDIUM
    pattern: "汇率 API 在非工作日返回上一个工作日汇率，货币转换结果与实时汇率有偏差，
              尤其是 USD/JPY 等高波动货币对"
    mitigation: "在价格展示中附加汇率更新时间，标注 'as of YYYY-MM-DD'；
                 价格敏感应用（金额误差 >5% 告警）需使用付费实时汇率 API"
  - severity: MEDIUM
    pattern: "库存信息延迟：PA-API 的 Availability 字段反映的是仓库系统状态，
              实际下单时可能因并发抢购导致缺货（inventory race condition）"
    mitigation: "通知文本必须写明'库存状态仅供参考，以下单时实际状态为准'，
                 不可保证库存准确性"
  - severity: LOW
    pattern: "camelcamelcamel RSS 的价格字段格式在不同地区亚马逊站点不一致（美元用$，欧元用€），
              正则表达式只匹配了 $ 符号，导致欧洲站点价格解析失败"
    mitigation: "价格提取正则扩展为 r'([\\$€£¥]|[A-Z]{3}\\s?)([0-9,]+\\.?[0-9]*)' 并映射符号到货币代码"
  - severity: HIGH
    pattern: "PA-API Creators API 迁移截止日期（2026-04-30）前未完成迁移，
              所有价格查询突然返回 403/404，服务完全中断"
    mitigation: "立即评估迁移优先级：若 Amazon 数据是核心依赖，在 2026-03 前完成迁移；
                 迁移后并行运行旧 PA-API 和新 Creators API 至少 2 周，确认数据一致性；
                 降级方案：迁移期间使用 camelcamelcamel RSS feed 兜底"
  - severity: HIGH
    pattern: "价格异常值污染历史记录：抢购活动中商品价格可能短时出现 $1 或 $0.01 的占位价，
              被直接写入历史数据库后拉低所有统计均值，触发错误的降价通知"
    mitigation: "写入历史记录前做异常价格过滤：若新价格 < 历史均价 × 0.3 或 > 历史均价 × 3，
                 则标记为 suspect=true 并单独存储，不影响主价格记录"
  - severity: HIGH
    pattern: "SP-API 2026-04-30 起 GET 调用开始收费，轮询型价格监控系统运营成本突增：
              每天轮询 1000 个 SKU 每小时一次 = 24000 GET 调用/天，
              按新费率将产生显著月度成本，且没有任何过渡期告警"
    mitigation: "在迁移截止日期前将轮询架构改为事件驱动（Keepa 通知/RSS）；
                 仍需 GET 调用的场景改用 Reports API 批量拉取，大幅减少 GET 调用次数"
  - severity: HIGH
    pattern: "EAN 转 ASIN 映射错误：同一 EAN 对应多个 ASIN，SearchItems 返回第一个结果，
              系统追踪了错误的商品，用户收到完全不同商品的价格通知"
    mitigation: "SearchItems 结果必须通过商品名称/品牌/类目做二次验证；
                 相似度低于 0.7（模糊匹配）的结果应标记为 needs_manual_review 而非自动采纳"
  - severity: MEDIUM
    pattern: "SP-API 限速在多账号场景下超出预期：50 个卖家账号各自的速率桶管理未实现，
              账号 A 的请求失败被错误地用账号 B 的重试逻辑处理，导致账号 B 也被限速"
    mitigation: "实现严格的账号级别 token bucket 隔离；不同账号的请求队列物理分离；
                 429 响应必须携带原始账号 ID 才能路由到正确的重试队列"
  - severity: LOW
    pattern: "Keepa 时间戳未正确转换，价格历史显示 1970 年代数据：
              开发者直接用 datetime.fromtimestamp(keepa_time) 而忽略了 Keepa 的私有时间偏移量"
    mitigation: "Keepa 时间转换必须用：datetime.utcfromtimestamp((keepa_time + 21564000) * 60)；
                 在代码中加注释说明公式来源，防止后续开发者'优化'掉偏移量"
  - severity: HIGH
    pattern: "PA-API 频率超限积累导致密钥被暂停，所有监控任务静默停止：
              每秒超过 1 次请求不会立即返回 TooManyRequests；若持续超限，
              Amazon 会在数小时后暂停密钥，所有查询返回 InvalidSignature 而非频率错误，
              开发者误以为是认证问题而非频率问题"
    mitigation: "实现令牌桶（token bucket）严格限速；PA-API 响应中出现 InvalidSignature 时，
                 先检查请求时钟偏移（AWS SigV4 要求±5分钟内），再检查是否因超限被暂停"

# === 元数据 ===
source: manual
freshness_date: "2026-03-29"
quality_score: 76.0
usage_count: 0
evidence_refs:
  - "https://webservices.amazon.com/paapi5/documentation/"
  - "https://camelcamelcamel.com/faq#rss"
  - "https://keepa.com/#!discuss/t/api-documentation/5"
  - "https://www.frankfurter.app/docs"
  - "https://www.shopify.com/developers/docs/api/admin-rest"
  - "https://www.keywordrush.com/blog/amazon-pa-api-associatenoteligible-error-is-there-a-new-10-sales-rule/"
  - "https://www.keywordrush.com/blog/amazon-creator-api-what-changed-and-how-to-switch/"
  - "https://trajectdata.com/how-to-bypass-bot-detection-web-scraping-2025/"
  - "https://thunderbit.com/blog/ecommerce-price-monitoring-tools"
