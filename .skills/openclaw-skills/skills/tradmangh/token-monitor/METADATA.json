{
  "slug": "token-monitor",
  "name": "Token Monitor",
  "version": "1.0.3",
  "description": "Monitors OpenClaw token/quota usage via `openclaw models status` and outputs alerts when quotas drop below a threshold. Stores only a local state file to prevent duplicate alerts (no secrets handled).",
  "author": "tradmangh",
  "tags": ["utility", "monitoring", "quota", "tokens", "openclaw"],
  "homepage": "https://github.com/tradmangh",
  "license": "MIT",
  "requires": {
    "bins": ["jq", "bash", "openclaw"],
    "openclaw": ">=2025.1.0"
  },
  "permissions": ["filesystem"],
  "cron": true
}
