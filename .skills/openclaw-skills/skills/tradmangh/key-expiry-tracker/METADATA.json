{
  "slug": "key-expiry-tracker",
  "name": "Key Expiry Tracker",
  "version": "1.0.2",
  "description": "Tracks expiry dates for API keys/client secrets/certificates and alerts before they expire. Does not store or handle credential values—only metadata like name + expiry timestamp.",
  "author": "tradmangh",
  "tags": ["utility", "monitoring", "credentials", "expiry", "alerting"],
  "homepage": "https://github.com/tradmangh",
  "license": "MIT",
  "requires": {
    "bins": ["jq", "bash"],
    "openclaw": ">=2025.1.0"
  },
  "install": {
    "scripts": [
      "chmod +x scripts/check-credentials.sh"
    ]
  },
  "permissions": ["filesystem"],
  "cron": true
}
