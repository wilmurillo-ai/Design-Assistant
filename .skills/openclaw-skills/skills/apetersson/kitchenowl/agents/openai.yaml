interface:
  display_name: "KitchenOwl CLI"
  short_description: "Install and run kitchenowl-cli via pipx and command workflows"
  default_prompt: "Use $kitchenowl-cli to install kitchenowl-cli with pipx and run the right KitchenOwl commands."
