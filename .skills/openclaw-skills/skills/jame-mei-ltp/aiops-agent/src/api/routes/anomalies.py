"""
Anomaly-related API endpoints.

Note: Most anomaly endpoints are in main.py as they need direct agent access.
This module is for any standalone anomaly utilities.
"""

from fastapi import APIRouter

router = APIRouter()


# Placeholder for additional anomaly endpoints that don't need agent access
# Currently, all anomaly endpoints are defined in main.py for direct agent access
