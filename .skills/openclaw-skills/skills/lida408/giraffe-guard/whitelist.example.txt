# Example whitelist for OpenClaw Skill Security Auditor
# Lines starting with # are comments
#
# Format:
#   path/to/file              — whitelist entire file
#   path/to/file:42           — whitelist specific line number
#   path/to/file:rule-name    — whitelist specific rule for a file
#
# Examples:
# skills/openai-whisper-api/scripts/transcribe.sh:sensitive-env-access
# skills/1password/references/cli-examples.md:sensitive-data-access
